# Dual gap inventory reseed (2026-08-08)

Kind: analysis only. Not residual invent beyond the ranked list below.
Not a full isomorphism slogan. Not free/complete/PROVABLY/FullHost/llvm forge.
ASCII only. Date: 2026-08-08.

**Phase:** Phase C step C1 (plan Part 5 Curry-Howard dual deepen).
**Living inventory:** `src/systems/surface-matrix.md`, `src/systems/join-map.md`
**Divergence:** `doc/divergence.md`
**Prior inventory:** `doc/dev/research/superset-surface-inventory-2026-08-03.md`

---

## Living claim pins (do not flip from this note)

| Pin | Living value |
|-----|--------------|
| free / product residual free (claim A) | **true** |
| freestanding product self-host complete (claim B) | **true** |
| PROVABLY / provablyUnlocked | **true** |
| product StillUsesLake / DependsOnLake | **false** |
| host elaborator residual free | **claimed** (F1 dual-ok; remains false / free claimed true) |
| FullHostElaborateRemains | **false** |
| llvmUnlocked | **true** (not full backend / not Rust-native link) |
| proofCompleteClaimed | **true** (not full elaborator parity) |
| Full elaborator parity / mathlib | **unclaimed** |
| Full Idris 2 / Lean 4 core parity | **unclaimed** (progressive matrix only) |

This inventory does **not** claim "Curry-Howard complete" or formal dual-bridge
finished. Six JOIN-ALG examples plus join cite/use pins are foundation, not
isomorphism complete. Dual formal map row is **present-partial** (one named
FailClosedGrade edge), not open and not isomorphism complete.

---

## What already exists (usable-core dual floor)

| Algorithm id | Grade focus | Idris | Lean | Host use (join-map) |
|--------------|-------------|-------|------|---------------------|
| **ConsumeToken** | MULT-1 exact-once | `examples/ConsumeToken.idr` | `examples/ConsumeToken.lean` | HostCompose mint/consume live-flag; Linear Token axioms dual-cite only |
| **ErasedIndex** | MULT-0 erased index/length | `examples/ErasedIndex.idr` | `examples/ErasedIndex.lean` | Erasure mark / isRuntimeAbsent / MULT-0 |
| **UnrestrictedShare** | MULT-OMEGA copy/drop/multi-use | `examples/UnrestrictedShare.idr` | `examples/UnrestrictedShare.lean` | Mult multOmega + Linear.shareNat |
| **FailClosedGrade** | MULT fail-closed unknown tag | `examples/FailClosedGrade.idr` | `examples/FailClosedGrade.lean` | Mult `ofNat?` / `isValidTag`; `failClosedGradeHostUseOk` (Phase C 2026-08-08) |
| **LinearPairSwap** | Two MULT-1 pair swap | `examples/LinearPairSwap.idr` | `examples/LinearPairSwap.lean` | PARTIAL `linearPairSwapHostUseOk` single-live remint reject (2026-08-08) |
| **RuntimeExtract** | RUNTIME-FS extract honesty | `examples/RuntimeExtract.idr` | `examples/RuntimeExtract.lean` | `runtimeExtractHostUseOk` Extract RuntimeClaim FS-only (2026-08-08) |

Gates: `just idris-side` / `just lean-side` (pure Nix presence); optional elaborators.
Join composition: `joinDualCiteOk` + `joinAlgUseOk` -> `joinAlgContractOk`
(`SystemsLean/JoinMap.lean`). Surface matrix dual-cite **six** pairs **present-partial**.
Dual formal map theorems row: **present-partial** (FailClosedGrade named edge).
Not full isomorphism.

---

## Ranked next useful dual gaps

Rank is product usefulness for meet-in-the-middle **usable core**, not LOC
padding. Only open a dual residual when Done when is offline-checkable and a
named host or divergence gap exists. Operator promote only after Phase P
program close (held dual deepen beyond FailClosedGrade).

### Rank 1 -- FailClosedGrade (MULT fail-closed unknown tag) -- **done** (Phase C)

| Field | Content |
|-------|---------|
| **Status** | **done** 2026-08-08; report `.agents/reports/impl-phase-c-dual-deepen-2026-08-08.md` |
| **Why useful** | Product Mult is a closed 0/1/omega set. Freestanding C enum and host `ofNat?` / `isValidTag` reject unknown raw tags (**FAIL-CLOSED-UNKNOWN-GRADE**). |
| **Landed** | Dual sources both sides; JOIN + multiplicity-map + TRUST; JoinMap + SurfaceMatrix dualCiteOk four pairs; pure Nix dual + host presence green |
| **Out of scope** | Full binder-QTT source language; claiming Mult is a full language frontend; full isomorphism |

### Rank 2 -- LinearPairSwap (two MULT-1 resources) -- **done** (2026-08-08)

| Field | Content |
|-------|---------|
| **Status** | **done** held-backend C1; report `.agents/reports/impl-held-backend-dual-rust-a090001e-2026-08-08.md` |
| **Landed** | Dual sources both sides; JOIN + maps + TRUST; JoinMap cite + PARTIAL `linearPairSwapHostUseOk`; SurfaceMatrix dualCiteOk six pairs; pure Nix dual + host presence green |
| **Out of scope** | Multi-token product C stages; classic Lean Mult-1 enforcement |

### Rank 3 -- RuntimeExtract (RUNTIME-FS vs classic claim) -- **done** (2026-08-08)

| Field | Content |
|-------|---------|
| **Status** | **done** held-backend C2; same campaign report |
| **Landed** | Dual sources both sides; JoinMap `runtimeExtractHostUseOk`; dual gates green |
| **Out of scope** | Claiming stock Idris/Lean freestanding; PROVABLY re-open |

### Rank 4 -- AffineDrop (EDGE-AFFINE honesty)

| Field | Content |
|-------|---------|
| **Why useful** | Divergence and Linear host pin **AFFINE-NOT-FIRST-CLASS**: product talk includes at-most-once / drop without use; Idris public grades are 0 / exact-once 1 / unrestricted -- no first-class affine. Dual documents the gap rather than a product grade. |
| **Idris side** | No first-class affine; dual would show intentional non-example vs MULT-1 drop fail under LinearCheck. |
| **Lean side** | Contract sketch only. |
| **Host map** | `Linear.affineNotFirstClassId` honesty pin (not a feature enable). |
| **Offline checkable?** | Yes as honesty dual; do not claim affine product ABI. |
| **Suggested residual Name** | **AffineDrop honesty dual** (only if product opens affine ABI residual) |
| **Out of scope** | First-class affine grade on freestanding wire without named residual. |

### Rank 5 -- Dual formal map theorems (not a new algorithm) -- **partial done** (D1 + D2)

| Field | Content |
|-------|---------|
| **Status** | **present-partial** via named edges `failClosedGrade_host_use_implies_mult_unknown_reject` and `runtimeExtract_host_use_implies_unknown_runtime_reject` in JoinMapTheorems; SurfaceMatrix `rowDualFormalMap` present-partial |
| **Not done** | Isomorphism complete; further edges optional |
| **Out of scope** | "Isomorphism complete" slogan; mathlib as product dependency |

### Explicit non-gaps (do not invent)

| Temptation | Why not |
|------------|---------|
| Fifth dual with no host or divergence name | Surface matrix: do not invent duals without a named product map gap; FailClosedGrade is already the fourth |
| Full Idris / Lean syntax dual of whole language | Day-one parity non-goal; progressive gates only |
| Lean-only model of Idris | AGENTS: real sources on **both** sides |
| Dual that forges free / PROVABLY / FullHost / full llvm backend | Claim pins stay living tip |

---

## Matrix / join honesty after held-backend wave (2026-08-08)

Living expectation now met:

1. Dual trees have six JOIN-ALG ids including LinearPairSwap and RuntimeExtract.
2. Dual pure Nix presence lists six pairs.
3. JoinMap cite + host use for all six (LinearPairSwap PARTIAL; RuntimeExtract Extract FS).
4. Surface matrix dual cite six pairs; dual formal map row **present-partial**.
5. AffineDrop stays held unless product opens affine ABI.

---

## C2 selection for this implement slice

**Implement rank-1: FailClosedGrade dual pair** (offline-checkable; named Mult product gap FAIL-CLOSED-UNKNOWN-GRADE).

Suggested residual Name:

- **Name:** FailClosedGrade dual pair
- **Goal:** Real Idris + Lean dual of unknown Mult tag reject into join/matrix honesty.
- **Done when:** Dual sources both sides; JOIN ids; dual gates green; join-map row; no full-isomorphism claim.
- **Out of scope:** Full binder-QTT language; free/PROVABLY/FullHost/llvm full-backend forge.

---

## Related paths

- `src/idris2/examples/`, `src/lean4/examples/`
- `src/idris2/JOIN.md`, `src/lean4/JOIN.md`
- `src/systems/join-map.md`, `SystemsLean/JoinMap.lean`
- `src/systems/surface-matrix.md`, `SystemsLean/SurfaceMatrix.lean`
- `src/systems/SystemsLean/Mult.lean` (FAIL-CLOSED-UNKNOWN-GRADE)
- `doc/divergence.md`, `doc/shared-ir-sketch.md`
