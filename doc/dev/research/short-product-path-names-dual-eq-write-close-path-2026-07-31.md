# Short product-path names -- DualEqWriteClosePath (2026-07-31)

| From | To |
|------|-----|
| `ProductPathFreestandingPerformDualEqualityWriteClosePath(+Main)` | `DualEqWriteClosePath(+Main)` |

Claim bools unchanged. Lake exe
`slake-freestanding-perform-dual-equality-write-close-path` unchanged.
