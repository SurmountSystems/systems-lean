# Full classic elaborator parity -- measured bar vs present evidence (2026-08-09)

Kind: analysis only. Not residual invent beyond the named queue.
Not free flip. Not DualResidual re-open. Not FullHostElaborateRemains re-true.
Not product free / complete / PROVABLY re-open. Not full Lean 4 / mathlib claim.
Not full LLVM backend. Not Rust-native link.

ASCII only. Plain American English. Date: 2026-08-09.

**Program slice:** Full readiness Phase 2.6 -- Full classic elaborator parity
**measured** residual. This note is the evidence checklist reseed after three
PACKAGE-ELABORATE dual-ok bands closed.

**Prior (do not re-run from zero):**

| Artifact | Role |
|----------|------|
| `full-elaborator-parity-design-2026-08-07.md` | Free F1 != full elaborator parity; gap table |
| `real-elaborator-judgments-design-2026-08-08.md` | J1-J14; L0-L4; E-gates; when parity row may move measured |
| `package-elaborate-l4-entry-2026-08-09.md` | Three PACKAGE-ELABORATE bands + remaining L4 |
| `.agents/plans/plan-fullhost-gap-close-2026-08-08.md` | Multi-wave checklist for full package typecheck |
| `.agents/reports/impl-package-elaborate-l4-kernel-emit-parity-2026-08-09.md` | KernelEmitParity band closed; residual Open this Name |

---

## 1. What "measured" means for this surface-matrix row

Surface-matrix vocabulary (`src/systems/surface-matrix.md`):

| Status | Meaning for this row |
|--------|----------------------|
| **open** | Not claimed; gap open until named residual + evidence |
| **present-partial** | Progressive host surface exists; intentional PARTIAL vs full classic elaborator / Lake-grade package typecheck |
| **measured** | Real fail-closed recipe(s) and/or living claim pin with evidence for the **named row bar** (here: develop-seed package elaborate toward Lake typecheck job); still not full language parity or mathlib unless a separate residual closed that |

From judgments design section 6b (L4 stage):

> After package L4: PACKAGE-ELABORATE dual-ok on **seed library**; surface-matrix
> Full classic elaborator parity may move **measured** only with explicit residual
> + lake proof (never automatic).

From plan-fullhost-gap-close multi-wave checklist **minimum** for "full package
typecheck of SystemsLean develop seed":

1. Package load (L1) -- free bar met
2. Import graph (L2) -- seed modules=206 without-Lake
3. **Module body check real judgments** on full develop seed (not greps alone)
4. Kind-surface then judgments coverage band roll
5. Theorems proof terms elaborate as Lake does on *Theorems develop set
6. Host tools carve OK
7. Same-job evidence (same modules / same job as Lake)
8. Dual-ok redesign + surface-matrix measured + lake proof; FullHost stays false
9. Cold Lake role remains bootstrap / claim-proof / diagnostic
10. mathlib -- day-one non-goal

**Measured bar for this residual (honest develop-seed bar):**

| Required | Meaning |
|----------|---------|
| **M1 Multi-band PACKAGE-ELABORATE** | MultExtractFoundation + MultLinearTypesProof + KernelEmitParity dual-ok with E-bad that L0 package tokens alone still accept |
| **M2 Unit TERM Mult..Extract** | Eight foundation units TERM-SURFACE dual-ok (unit-scoped) |
| **M3 Unit PROOF Mult/Linear/Types** | Three PROOF-SURFACE dual-ok (unit-scoped) |
| **M4 Package-scope beyond inventory** | Real package-scope judgment path (J5-J12 direction / seed-wide L4 dual-ok path) with E-good/E-bad that L0 alone would still accept -- **not** inventory dialect re-list alone |
| **M5 E-lake** | Cold `lake build` on dual-pin / claim surface exit 0 when any claim flips |
| **M6 FullHost / free / product pins** | FullHostElaborateRemains **false**; free/complete/PROVABLY **unchanged**; no "Slake typechecks like Lake" slogan |

**Rule:** M1+M2+M3 alone are **not** enough for **measured**. Residual Out of scope
and plan failure mode ban grepping package inventory dual-ok into measured.
M4 is the missing hard gate for measured. M1-M3 justify **present-partial** only.

---

## 2. Present evidence inventory (verified 2026-08-09)

Without-Lake driver banners (`just host-module-check-without-lake` exit 0;
modules=206):

| Surface | Scope | dualOk |
|---------|-------|--------|
| TERM-SURFACE | Mult-only | true |
| TERM-SURFACE | Linear-only | true |
| TERM-SURFACE | Types-only | true |
| TERM-SURFACE | IrProgram-only | true |
| TERM-SURFACE | IrGraph-only | true |
| TERM-SURFACE | HostCompose-only | true |
| TERM-SURFACE | Erasure-only | true |
| TERM-SURFACE | Extract-only | true |
| PROOF-SURFACE | MultTheorems-only | true |
| PROOF-SURFACE | LinearTheorems-only | true |
| PROOF-SURFACE | TypesTheorems-only | true |
| PACKAGE-ELABORATE | MultExtractFoundation-only | true |
| PACKAGE-ELABORATE | MultLinearTypesProof-only | true |
| PACKAGE-ELABORATE | KernelEmitParity-only | true |

Package band gates fold dualOk pin + bar/scope + unit SurfaceOk (or deepen
FOUNDATION-KIND-SURFACE band) + package dialect E-good + E-bad rejects
(PACKAGE-BAND-INCOMPLETE) while L0 package tokens still accept. See
`HostModuleCheckPackageElab.lean` and package-elaborate-l4-entry research.

| Pin class | Living tip |
|-----------|------------|
| FullHostElaborateRemains | **false** |
| DualResidual free dual-ok (F1) | remains false / free claimed true |
| free / complete / PROVABLY | **true** (unchanged) |
| HostModuleCheck depth honesty | PARTIAL-STRUCTURAL + FOUNDATION-KIND-SURFACE deepen band |
| seed / harness | modules=**206** / harness=**40** |

---

## 3. Checklist vs evidence (measured gate)

| Req | Status | Evidence / gap |
|-----|--------|----------------|
| M1 Multi-band PACKAGE-ELABORATE | **met** | Three dual-ok bands + FirstBandOk / ProofBandOk / KernelBandOk |
| M2 Unit TERM Mult..Extract | **met** | Eight unit dual-ok |
| M3 Unit PROOF Mult/Linear/Types | **met** | Three unit dual-ok |
| M4 Package-scope beyond inventory | **not met** | Only package **inventory dialects** (fixture text unit lists). No seed-wide PACKAGE-ELABORATE dual-ok. No real J5-J12 package environment accept/reject beyond inventory. HostModuleCheck still greps for most seed modules. |
| M5 E-lake on claim flip | **met for present-partial flip** | `lake build` SurfaceMatrix + SurfaceMatrixTheorems (this slice) |
| M6 FullHost / free / product | **met** | FullHost false; free/complete/PROVABLY unchanged |

**Verdict:**

| Claim | Decision | Why |
|-------|----------|-----|
| Surface-matrix **measured** | **No** | M4 missing; would forge measured from inventory dialects alone |
| Surface-matrix **present-partial** | **Yes** | M1-M3 progressive host surface is real dual-ok evidence; intentional PARTIAL vs Lake-grade package typecheck |
| Surface-matrix stay **open** | Rejected as living tip | Open under-states multi-band package L4 + unit L2/L3 dual-ok already dual-pinned |

---

## 4. What is still missing for measured (J-class map)

| Judgment / layer | Living | Gap for measured |
|------------------|--------|------------------|
| J1 Package load | Met without-Lake | Keep |
| J2 Import closure | Seed 206 without-Lake | Keep; *Main/harness carved |
| J3 Decl identity | PARTIAL-STRUCTURAL seed | Floor only |
| J4 Kind surface | Mult..Extract + Kernel/Emit/Parity band | Not full seed kind-surface |
| J5 Definition typing | Unit TERM Mult..Extract only | Not package-wide; not remaining seed |
| J6 Inductive/structure formation | Soft in unit TERM pilots | Not package environment |
| J7 Theorem/proof check | Mult/Linear/Types *Theorems only | Not full *Theorems corpus |
| J8-J12 Universe / binder / app / instance / mutual | Absent or product-local only | Package elaborate path not started |
| L4 seed-wide PACKAGE-ELABORATE dual-ok | Three scoped inventory bands only | Need seed-wide dual-ok redesign + E-bad |
| Same-job Lake compare (checklist item 7) | Cold lake diagnostic only | No same-job parity milestone table for measured |

---

## 5. Residual lockstep intent (this slice)

| Action | Intent |
|--------|--------|
| Surface-matrix Full classic elaborator parity | **present-partial** (SurfaceMatrix.lean dual-pin + prose) |
| Measured claim / dual-ok for full parity | **not** flipped |
| Research note | This file |
| Residual Name Full classic elaborator parity measured | **Done** as present-partial inventory + dual-pin; **not** measured claim closed |
| Next Open | **Package elaborate real judgments path** (M4 toward measured) |
| Pins | FullHost **false**; free/complete/PROVABLY **unchanged**; three PACKAGE-ELABORATE bands + unit TERM/PROOF dual-ok **unchanged** |

---

## 6. Non-claims

- Does **not** claim measured Full classic elaborator parity
- Does **not** claim "Slake typechecks like Lake" or full package typecheck
- Does **not** re-true FullHostElaborateRemains
- Does **not** flip free / complete / PROVABLY / DualResidual free
- Does **not** claim mathlib or full Lean 4 core parity
- Does **not** sell three package inventory bands as seed-wide L4 complete
- Does **not** open Mult foreign link / Phase 3 (that waits on measured)

---

## 7. Pointers

| Artifact | Role |
|----------|------|
| This note | Measured bar vs present evidence SSoT after three package L4 bands |
| `surface-matrix.md` | Full classic elaborator parity **present-partial** |
| `SurfaceMatrix.lean` | `rowFullElaborator` present-partial; `rowFullElaboratorPartialOk` |
| Residual / WATCHER | Next: Package elaborate real judgments path |
| Report | `.agents/reports/impl-full-classic-elaborator-parity-measured-2026-08-09.md` |
