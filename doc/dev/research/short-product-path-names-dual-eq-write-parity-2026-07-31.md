# Short product-path names -- DualEqWriteParity (2026-07-31)

Kind: analysis + implement inventory.

| From | To |
|------|-----|
| `ProductPathFreestandingPerformDualEqualityWriteParity(+Main)` | `DualEqWriteParity(+Main)` |

Claim bools `productPathFreestandingPerformDualEqualityWriteParity*` unchanged.
Lake exe `slake-freestanding-perform-dual-equality-write-parity` unchanged.
