# FullHost bar definition (locked criteria)

Kind: analysis only for the flip bar. **Not** a FullHost pin flip. **Not** free /
complete / PROVABLY re-open. **Not** mathlib day-one. **Not** product Lean
claim-bool edits in this definition slice.

ASCII only. Plain American English. Date: 2026-08-10.
**Bar status: LOCKED as definition** (Wave 1 Track C, 2026-08-10).
Pin `hostResidualShrinkFullHostElaborateRemains` / FullHostElaborateRemains stays
**false** until a **separate** named residual meets this bar and lands lake
elaborator proof.

**SSoT for the bar:** this note.
**Residual Decision:** `RESIDUAL-systems.md` Decisions (FullHost true only when
bar met + lake proof; pin currently false).
**Report:** `.agents/reports/impl-wave1-c-fullhost-bar-def-2026-08-10.md`.

**Program context:** Completeness **C2** progressive Kernel Mult..Emit TERM
delivered (harness=**49**). Free bar **(a)** host residual free **claimed**. Full
classic elaborator parity **measured** under M1-M6 (not mathlib; not seed-wide
J1-J12). Wave 1 Track C defines the bar only.

---

## 0. One-sentence claim (what FullHost would mean)

**FullHost package elaborate done** means: for the **named Systems Lean host
package surface** under `src/systems/` (SystemsLean library + host check tools
that day-to-day develop uses), Slake-owned host check depth has closed the
honest residual gap that free bar (a) intentionally left open -- package-scope
elaborate work that still needs cold / diagnostic Lake as the honest story for
**that** job no longer remains as open product residual.

Short name in residual prose: **FullHost true** (claim language). Living Lean
pin today: `hostResidualShrinkFullHostElaborateRemains` = **false** (A66:
day-to-day cold full Lake retired). A future flip residual must either (a)
introduce a successor claim pin for package-elaborate-done, or (b) redesign
dual-ok folds if it re-uses FullHostElaborateRemains polarity -- free dual-ok
today **requires** `!hostResidualShrinkFullHostElaborateRemains`.

---

## 1. What is already true (do not re-prove as FullHost)

| Already true | Meaning | Why not FullHost |
|--------------|---------|------------------|
| Host elaborator residual free **claimed** (free bar a) | Day-to-day host develop tools do not require Lake on the step; DualResidual remains false / free claimed true | Free intentionally left package-depth residual open |
| FullHostElaborateRemains **false** (A66) | Cold full Lake is bootstrap / emergency / claim-proof only for day-to-day loop | Day-to-day cold-Lake retirement != package elaborate done |
| Full classic elaborator parity **measured** (M1-M6) | Named develop-seed package elaborate bar; four PACKAGE-ELABORATE bands + MultLinearEnv | Measured row != FullHost; not seed-wide J1-J12; not mathlib |
| Package L4 bands (R4) under named bar | MultExtractFoundation + MultLinearTypesProof + KernelEmitParity + MultLinearEnv | Not seed-wide L4 complete |
| Kernel Mult..Emit TERM dual-ok | Progressive C2 TERM-SURFACE; harness **49** | TERM depth alone is progressive deepen, not FullHost |
| Mult..Extract TERM + EmitPlan/Apply/Body TERM | Prior progressive TERM depth | Same |
| Mult / Linear / Types PROOF dual-ok | Named PROOF surfaces under closed bars | Unit PROOF != full package elaborate |
| free / complete / PROVABLY | Product residual free; freestanding self-host complete; CompCert matrix | Product pins orthogonal to host FullHost |

**Non-equivalence (hard):**

- free claimed **is not** FullHost package elaborate done
- A66 FullHostElaborateRemains false **is not** FullHost package elaborate done
- measured M1-M6 **is not** FullHost package elaborate done
- progressive C2 Kernel TERM **is not** FullHost package elaborate done
- proofCompleteClaimed **is not** FullHost package elaborate done

---

## 2. Scope the bar covers (exact surface)

Before any FullHost claim residual may flip, the bar SSoT (this note, or an
explicit residual Done when that **cites this note by path**) must name:

| Scope item | Rule |
|------------|------|
| **Package root** | `src/systems/` SystemsLean host package (`lakefile.lean` roots) |
| **Module set** | Living develop seed modules=**206** (or the measured seed count at flip time, dual-ok greppable) plus named host check harness modules required by the Done when |
| **Jobs** | Package load; import graph; module body check with **real judgments** on the covered set; theorem / proof-term elaborate on the *Theorems develop set the bar names; host develop tools already owned under free bar (a) stay green |
| **Non-claims (always)** | mathlib day-one (**C4**); full Lean 4 language / mathlib-scale elaborator parity; product residual free re-open; "Lake is gone forever"; LLVM full backend / production opt / DominanceClaimed; AffineDrop dual invent; product Rust under `src/`; host UX clear-prompt / resume (**C5**, out of repo) |

Optional expand (only if the flip residual **explicitly** lists them in Done when):

- Seed-wide package L4 complete (today R4 is "done under named bar"; seed-wide L4 is **held**)
- Further TERM/PROOF units (Parity, CompilePath, Freestanding, KernelLinear+ PROOF) only when product residual names a checkable need
- KernelSelfApply or package deepen only when checkable Done when lists them

**Do not** use "more theorems" alone as the green step for FullHost.

---

## 3. Checkable criteria: what MUST be true before FullHost true

A future residual Name (example: **FullHost package bar met** or **C2z FullHost
claim**) may claim FullHost package elaborate done **only when all** of the
following hold. This definition slice does **not** flip anything.

### Must-have (all required)

| # | Checkable outcome (plain English) |
|---|-----------------------------------|
| **H1** | **Written bar authority.** This note (or a residual Done when that cites this path) still lists scope, must-haves, and non-claims. Agents reseed from disk, not chat. |
| **H2** | **Named package surface closed under real judgments.** Host module check depth is no longer only PARTIAL-STRUCTURAL greps for the **named** develop-seed set: real accept/reject judgment paths cover package-scope work the free bar left open (J5-J12 direction / seed-wide package elaborate path as the residual Done when states). Fail-closed E-good / E-bad evidence exists where L0 inventory tokens alone would still accept bad cases. |
| **H3** | **Same-job honesty.** For the named package elaborate job, there is measured evidence that Slake-owned host check is the day-to-day story for that job (without-Lake dual-ok where free bar already requires it; cold Lake remains bootstrap / claim-proof / diagnostic only, consistent with free bar (a)). |
| **H4** | **Free bar (a) still green.** dual-ok / without-Lake host checks stay green; host free claimed remains true (remains false / free claimed true); free claimed and FullHost are both honest without forging free false or product free re-open. |
| **H5** | **Product Lean claim surface + lake elaborator proof.** Product Lean SSoT for the FullHost claim (successor pin and/or dual-ok redesign documented in the flip residual) flips only with **lake** elaborator proof on the claim surface (`lake build` of named modules + claim exe if any) exit 0 while lake is on PATH. Grep-only is **not** enough (`AGENTS.md` claim-bool proof rule). If the flip re-uses `hostResidualShrinkFullHostElaborateRemains`, dual-ok folds that today require the pin **false** must be redesigned in the same residual (free dual-ok must not silently collapse). Prefer a short role name for any new claim pin. |
| **H6** | **Orthogonal pins unchanged unless separate residual.** free / complete / PROVABLY / product StillUsesLake / DependsOnLake / tip fullBackend / band FullBackend / proofCompleteClaimed **unchanged** by the FullHost flip residual unless a separate residual owns those flips with their own bars. |
| **H7** | **Docs lockstep.** Residual Decisions + Open/Done + plan living tip + WATCHER + SESSION-HANDOFF state the claim and the non-claims in plain English. Surface-matrix / self-host honesty rows updated only when the flip residual owns them. |
| **H8** | **Hygiene.** `just hygiene` green; Sub-1-KLOC on novel files touched; short report under `.agents/reports/`. |

### Process order (flip residual only)

1. Open a **separate** residual Name whose Done when cites **H1-H8**.
2. Implement product Lean + dual-ok as designed for that Name.
3. Observe red where required (new dual-ok / pin shape), then green with **lake**.
4. Lock residual Decisions: FullHost package elaborate done claimed under this bar;
   pin values as landed; free still claimed free bar (a).
5. Never treat this criteria note alone as green for a pin flip.

---

## 4. What is explicitly NOT enough (do not forge)

| # | Not enough | Why |
|---|------------|-----|
| **N1** | Kernel Mult..Emit TERM dual-ok alone (C2 progressive) | TERM depth is progressive host deepen; harness 49 is not package elaborate done |
| **N2** | free bar (a) / host free claimed alone | Free deliberately left package-depth residual open |
| **N3** | A66 FullHostElaborateRemains false alone | Day-to-day cold-Lake retirement only |
| **N4** | measured M1-M6 full classic elaborator parity alone | Measured row under named bar; not FullHost; not seed-wide J1-J12; not mathlib |
| **N5** | Four PACKAGE-ELABORATE dual-ok bands alone | R4 under named bar; seed-wide L4 held |
| **N6** | Mult / Linear / Types PROOF dual-ok alone | Unit PROOF surfaces, not full package elaborate |
| **N7** | Mult..Compose foundation dual-ok alone | Foundation harness only; not "Slake does everything Lake does" for package elaborate |
| **N8** | proofCompleteClaimed true alone | SpecProof bar; orthogonal to host FullHost |
| **N9** | Grep / pure Nix presence alone for the claim flip | Claim-bool proof requires lake elaborator while Lake is host elaborator |
| **N10** | More canary theorems without H2 package-scope judgments | Theorem theater is not FullHost green |
| **N11** | mathlib dependency or mathlib-scale parity | **C4** non-goal for freestanding finish line |
| **N12** | tip fullBackend true or Mult..Graph foreign alone | LLVM path; orthogonal |
| **N13** | Empty Open / done-for-now under honest pins | Stop rule is not a FullHost claim |

---

## 5. What remains operator-promote only

| Item | Rule |
|------|------|
| FullHost package elaborate done claim (any pin flip) | Named residual + this bar + lake; this note is definition only |
| Re-true `hostResidualShrinkFullHostElaborateRemains` without dual-ok redesign | Forbidden; free dual-ok requires pin false today |
| mathlib day-one (**C4**) | Non-goal for freestanding finish line |
| AffineDrop dual invent as freestanding ABI | Held; honesty dual is Wave 1 B only |
| A-heavy LLVM (production opt / DominanceClaimed) | Operator promote; not FullHost synonym |
| Host UX clear-prompt / resume (**C5**) | Out of this repo (Grok Build) |
| Kernel PROOF L3 treadmill as FullHost substitute | KernelMult proof-layer is Wave 1 A; not FullHost |

---

## 6. Pin snapshot at bar lock (2026-08-10)

| Pin / surface | Status |
|---------------|--------|
| free / complete / PROVABLY | true |
| FullHostElaborateRemains (`hostResidualShrinkFullHostElaborateRemains`) | **false** (stays false; bar locked, claim not met) |
| host free claimed (free bar a) | true (remains false / free claimed true) |
| Full classic elaborator parity | **measured** (M1-M6; not FullHost) |
| seed / harness | 206 / 49 |
| tip fullBackend | true A-light |
| band FullBackend | false |
| C2 Kernel Mult..Emit TERM | delivered |
| Wave 1 Open A / B | open (disjoint tracks) |
| Wave 1 Open C FullHost bar definition | **done** (this lock) |

---

## 7. Sources

| Path | Role |
|------|------|
| `RESIDUAL-systems.md` | Decisions + Wave 1 Open/Done |
| `src/systems/SystemsLean/HostResidualShrink.lean` | FullHost pin false; free dual-ok requires !pin |
| `src/systems/SystemsLean/DualResidual.lean` | host free claimed; FullHost stays false |
| `.agents/plans/plan-slake-replaces-lake.md` | host finish free bar (a) |
| `.agents/plans/plan-full-readiness-2026-08-09.md` | full readiness living tip |
| `.agents/plans/plan-completeness-remaining-2026-08-10.md` | C2/P5; FullHost until bar + lake |
| `doc/dev/research/full-classic-elaborator-parity-measured-bar-2026-08-09.md` | measured != FullHost |
| `doc/dev/research/host-free-dual-ok-design-2026-08-07.md` | free bar dual-ok design |
| `.agents/joins/a66-full-host-elaborate-remains-false.md` | A66 pin false land |
| `.agents/reports/impl-completeness-p5-honest-stop-2026-08-10.md` | P5 closeout |
| `.agents/reports/impl-wave1-c-fullhost-bar-def-2026-08-10.md` | this definition slice report |
