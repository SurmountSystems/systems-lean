# just/ -- modular task recipes

Root `justfile` is a thin menu: `set shell`, `import` of these modules, `default`, and `check`.
All modules use `import` (same namespace) so recipe names stay flat: `just check`, `just systems-host`.
Full suite `just check` includes fail-closed `product-compcert-matrix` (ccomp) after `systems-cc-probe`.

| Module | Role |
|--------|------|
| `gates.just` | Pure Nix eval strings + hygiene, professional-tone, systems-host/emit-wire/llvm-ir, idris/lean-side, systems-mill-extras-fit, systems-mill-inventory, systems-mill-packed-extras (mill 66 packed 34 extras, mill 68 packed 16 extras, mill 69 packed 0 extras; presence only), systems-mill-file-path (mill 67/68/69 FILE PATH on the helper just line; presence only), systems-mill-recipe-count |
| `progress.just` | Progress meters (`progress`, `progress-scc`) |
| `product-wire.just` | `build`, product path read/compose/write, install/export freestanding |
| `freestanding-capable.just` | freestanding-capable-* gap/read/compose/write/regenerate/install/full-bar |
| `ownership.just` | ownership-regenerate-* |
| `freestanding-perform-1.just` | perform evidence / official path / dual-eq write parity |
| `freestanding-perform-2.just` | dual-eq write close-path / api / capable-gap |
| `freestanding-perform-3.just` | retire-official / perform-claimed / ownership-claimed |
| `freestanding-complete.just` | step-contract-full / freestanding-self-host-complete |
| `llvm.just` | out-llvm-ir, llvm-*-text, llvm-unit-package, llvm-as-smoke |
| `llvm-ssa.just` | llvm-compose-ssa (Compose SSA writer; split so llvm.just stays under 1000) |
| `llvm-link-smoke.just` | `just richer-record-layout` (IrNodeRecord rustc_abi dual-pin) + `just llvm-link-smoke` (out-of-tree clang or rustc+llc links generator SSA `.ll` under TMPDIR; skip-honest if tools missing) + `just llvm-trait-object-vtable` / `just llvm-trait-object-vtable-check` (lean --run regenerates `out/llvm-ir/trait-object-vtable.ll`; slot 4 method1 fifth pin offset 32; slot 5 method2 sixth pin offset 40; slot 6 method3 seventh pin offset 48; slot 7 method4 eighth pin offset 56; slot 8 method5 ninth pin offset 64; method6 slot 9 offset 72; method7 slot 10 offset 80; method8 slot 11 offset 88; method9 slot 12 offset 96; method10 slot 13 offset 104; method11 slot 14 offset 112; method12 slot 15 offset 120; method13 slot 16 offset 128; method14 slot 17 offset 136; method15 slot 18 offset 144; method16 slot 19 offset 152; method17 slot 20 offset 160; method18 slot 21 offset 168; method19 slot 22 offset 176; method20 slot 23 offset 184; method21 slot 24 offset 192; method22 slot 25 offset 200; method23 slot 26 offset 208; Vacant slot 27 offset 216; TraitVPtr slot 28 offset 224; living DualPinOk fixture 29 slots / 232 bytes / 24 Methods; fourth-pin Vacant-at-slot-4 is history only; check fails if dest bytes differ; no mill row) |
| `slake-produced-elf.just` | Thin wrapper: `just slake-produced-elf-presence` evals `nix/slake-produced-elf.nix`; `just slake-produced-elf` / `just slake-produced-elf-check` then `lean --run` on `SystemsLean/SlakeProducedElf.lean`. Dest `out/slake-produced-elf/slake-hello`. Fail closed if clang or lean missing. Not mill `leanc`. Not a pure derivation of host PATH clang. Occupancy 49. |
| `slake-typecheck-closed.just` | Early closed-subset Slake typecheck drivers (`just slake-typecheck-mult` through `just slake-typecheck-packagewrite`). Each compiles the HostFrontLive import chain to oleans under `build/slake-typecheck-*` then `lean --run`. Not mill `leanc`. Not lake on the step. FullHost false. slakeOwnsPackageTypecheck false. Mill 69 of 69. Occupancy 49. |
| `slake-typecheck-closed-more.just` | Later HostFrontLive closed-subset Slake typecheck drivers (`just slake-typecheck-hostterm` through `just slake-typecheck-importwalk`). Same oleans-then-`lean --run` shape. Not mill `leanc`. Not lake on the step. FullHost false. slakeOwnsPackageTypecheck false. Mill 69 of 69. Occupancy 49. Split so the first module stays under 1000 lines. |
| `slake-typecheck-closed-later.just` | Later closed-subset Slake typecheck drivers (`just slake-typecheck-importwalklater` through `just slake-typecheck-kerneltypesterm`). Same oleans-then-`lean --run` shape. Not mill `leanc`. Not lake on the step. FullHost false. slakeOwnsPackageTypecheck false. Mill 69 of 69. Occupancy 49. Split so closed-more stays under 800 lines. |
| `slake-typecheck-closed-parity.just` | Later closed-subset Slake typecheck drivers (`just slake-typecheck-kernelprogramterm` through `just slake-typecheck-irprogramtheorems`). Same oleans-then-`lean --run` shape. Not mill `leanc`. Not lake on the step. FullHost false. slakeOwnsPackageTypecheck false. Mill 69 of 69. Occupancy 49. Split so closed-later stays under 800 lines. |
| `slake-typecheck-closed-kernel.just` | Closed-subset Slake typecheck of live `KernelMult.lean` (`just slake-typecheck-kernelmult`). Same oleans-then-`lean --run` shape. Not mill `leanc`. Not lake on the step. FullHost false. slakeOwnsPackageTypecheck false. Mill 69 of 69. Occupancy 49. Split so closed-parity stays under 1000 lines. |
| `slake-typecheck-closed-compilepath.just` | Closed-subset Slake typecheck of live `CompilePath.lean` (`just slake-typecheck-compilepath`). Same oleans-then-`lean --run` shape. Not mill `leanc`. Not lake on the step. FullHost false. slakeOwnsPackageTypecheck false. Mill 69 of 69. Occupancy 49. Split so closed-kernel stays under 1000 lines. |
| `slake-typecheck-closed-hostkernel.just` | Closed-subset Slake typecheck of live `HostKernel.lean` (`just slake-typecheck-hostkernel`). Same oleans-then-`lean --run` shape. Not mill `leanc`. Not lake on the step. FullHost false. slakeOwnsPackageTypecheck false. Mill 69 of 69. Occupancy 49. Split so closed-compilepath stays under 1000 lines. |
| `slake-typecheck-closed-joinmaptheorems.just` | Closed-subset Slake typecheck of live `JoinMapTheorems.lean` (`just slake-typecheck-joinmaptheorems`). Same oleans-then-`lean --run` shape. Not mill `leanc`. Not lake on the step. FullHost false. slakeOwnsPackageTypecheck false. Mill 69 of 69. Occupancy 49. Split so closed-hostkernel stays under 1000 lines. |
| `slake-typecheck-closed-linearusefaildecide.just` | Closed-subset Slake typecheck of live `LinearUseFail/Decide.lean` (`just slake-typecheck-linearusefaildecide`). Same oleans-then-`lean --run` shape. Not mill `leanc`. Not lake on the step. FullHost false. slakeOwnsPackageTypecheck false. Mill 69 of 69. Occupancy 49. Split so closed-joinmaptheorems stays under 1000 lines. |
| `slake-typecheck-closed-linearusefailkeep.just` | Closed-subset Slake typecheck of live `LinearUseFail/Keep.lean` (`just slake-typecheck-linearusefailkeep`). Same oleans-then-`lean --run` shape. Not mill `leanc`. Not lake on the step. FullHost false. slakeOwnsPackageTypecheck false. Mill 69 of 69. Occupancy 49. Split so closed-linearusefaildecide stays under 1000 lines. |
| `slake-host-tool-elf.just` | Thin wrapper: `just slake-host-tool-elf-presence` evals `nix/slake-host-tool-elf.nix`; `just slake-host-tool-elf` then `lean --run` write (named emit plan) plus dest-missing on `SystemsLean/SlakeHostToolElf.lean`. Dest `out/slake-host-tool-elf/slake-freestanding-self-host-complete` must stay missing. Fail closed. Do not clang. Not mill `leanc`. Not mill 70. Occupancy 49. Name is not GREEN. |
| `subset-emit.just` | first-surface + unit subset emit (+ early rebuilds colocated) |
| `subset-join.just` | subset package rebuild join / without-lake / lake-retire inventory |
| `host.just` | host residual shrink, front, fragment check |
| `host-without-lake.just` | host graph/package write and without-lake host recipes |
| `host-tool-expand.just` | Without-Lake develop-tool recovery and related host recipes |
| `compcert.just` | ccomp resolve/smoke, freestanding/mult under ccomp, product matrix. `product-compcert-matrix` skips unless hostname is `surmount-1` (laptop skip is GREEN). Force: `SYSTEMS_COMPCERT_MATRIX_FORCE=1`. |
| `subset-rebuild.just` | unit subset rebuild recipes (graph..mult) |
| `freestanding-mult.just` | mult freestanding deepen/write + product-wire-freestanding-write |
| `dual-side.just` | idris/lean elaborate, systems-lake, systems-cc-probe, watch. `_lake-if-pin` skips `lake build` unless hostname is `surmount-1` (laptop skip is GREEN). Force: `SYSTEMS_LEAN_LAKE_FORCE=1`. Named red bar `just slake-typecheck-src-systems` (Slake does not own package typecheck; do not skip-GREEN; not in `just check`). Local one-thread recipe `just check-local` forces `LEAN_NUM_THREADS=1`, nice 19 on the process group, and a 96 GiB cap (`systemd-run --user --scope MemoryMax=96G` when that works; else prlimit/ulimit; fail closed if no cap), then that named walk (not OccupancyProbe 192 GiB; not mill; not surmount-1 package typecheck Done when; not Lake-gone). Do not start a second check-local while one walk is live. Remote `just check-remote` SSHs to nixbuilder@surmount-1, nice 19, 256 GiB cap, one fat-walk thread; SSH down is fail closed (not skip-GREEN on horizon). One L2 owns the builder walk (local or remote). GREEN of check-local is not that remote Done when. |
| `bench.just` | Compile-path timing (`just bench`). Smallest same-job recipe is `just bench-same-job` (does not clobber the suite file). Slake cannot run the Lake same-job until it typechecks this package. |
| `bench-runtime.just` | Runtime arm recipes and flag matrix. Timing-link flags only. |
| `bench-runtime-twins.just` | Smoke-only protocol twins. Not product wire. |
| `bench-runtime-report.just` | `just bench-runtime` plus living report `doc/BENCH-RUNTIME-latest.md`. |
| `bench-llvm.just` | Times generator SSA under `out/llvm-ir/`. Not CompCert. Not the C product wire. |
| `first-host-tool.just` | Compile and link inventory host ELFs without Lake (`just first-host-tool`, `just second-host-tool`, `just third-host-tool`, `just fourth-host-tool`, `just fifth-host-tool`, `just sixth-host-tool`, `just seventh-host-tool`, `just eighth-host-tool`, `just ninth-host-tool`, `just tenth-host-tool`, `just eleventh-host-tool`, `just twelfth-host-tool`, `just thirteenth-host-tool`, `just fourteenth-host-tool`, `just fifteenth-host-tool`, `just sixteenth-host-tool`, `just seventeenth-host-tool`, `just eighteenth-host-tool`, `just nineteenth-host-tool`, `just twentieth-host-tool`, `just twenty-first-host-tool`, `just twenty-second-host-tool`; lean + leanc). Helper `_compile-inventory-exe` compiles extra modules in concurrent topological waves (cap min(wave width, floor(free GiB/2), 8)), then body, then Main, then leanc. |
| `later-host-tools.just` | Compile and link later inventory host ELFs without Lake (`just fifty-fifth-host-tool` through `just sixty-ninth-host-tool`; mill 67 HostModuleCheck ELF GREEN, extras-file, FILE PATH on the same just line as the helper; mill 68 MultFsWrite GREEN; mill 69 SelfHostComplete ELF GREEN, 0 extras, FILE on the same just line as the helper; living mill metric is 69 of 69; do not mill mill 69 again; lean + leanc; helper in first-host-tool.just). |

**Bar:** every file here and the root justfile must stay **under 1000 lines**. Growing past that is residual (split or port gate logic to pure Nix). See `AGENTS.md` (sub-1-KLOC / modular just).
