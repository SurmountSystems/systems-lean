# Implement report: residual closeout after Package-env for FirstSurface

**Date:** 2026-08-12
**Repo:** `/home/hunter/Projects/ai/iso`
**Kind:** residual / WATCHER / handoff / RESIDUAL.md lockstep only. No product Lean/C/Nix. No claim-bool flips.

ASCII only. Plain American English.

**Status:** **GREEN** (docs lockstep). `just hygiene` run after this report.

---

## Goal

Update living residual so the queue matches sibling evidence. Package-env for
FirstSurface closed GREEN with reviews at 0 open. Mark that Name **done**.
Full host package elaborate meet stays **done partial**. Do not flip
`FullHostElaborateRemains`. Do not re-queue FirstSurface typed term-surface
(TERM) or package-env. Do not open cold compose init unroll. Do not invent
seed-wide 206 or a FullHost flip.

---

## Evidence trusted (not re-researched)

| Report | Outcome |
|--------|---------|
| `/tmp/grok-1000/grok-impl-summary-pe-firstsurface.md` | GREEN. PE56 residual-free reject twin plus E-good S10 plus dialect S10. Harness 104. TERM dual-ok 50. |
| `.agents/reports/impl-package-env-firstsurface-2026-08-12.md` | GREEN. Same product slice. Pin stays false. |
| Review/mop (trashed; verdict on impl row) | GREEN / APPROVE 0. See `impl-package-env-firstsurface-2026-08-12.md`. |

Spot-checked (no product Lean edit):

- `hostResidualShrinkFullHostElaborateRemains := false` in
  `src/systems/SystemsLean/HostResidualShrink.lean`.
- Seed list after FirstSurface is `SystemsLean.LinearSubsetEmit` in
  `HostModuleCheckSeeds.lean`. No `HostModuleCheckLinearSubsetEmitTerm` exists.
- All 50 TERM units now have package-env twins (PE56 closed the FirstSurface
  gap). Next checkable meet-path work is TERM for LinearSubsetEmit.

---

## Paths touched

| Path | Change |
|------|--------|
| `RESIDUAL-systems.md` | Open queue 15 **done**; 16 Term-surface for LinearSubsetEmit **open**; Full host meet stays **done partial**; living tip + highest-value next + Done archive + next-prompt |
| `RESIDUAL.md` | Join Open table + highest-value next + living tip same story |
| `WATCHER.md` | **IN PROGRESS** Term-surface for LinearSubsetEmit. `/implement --effort 2 Term-surface for LinearSubsetEmit`. Did not re-queue FirstSurface TERM or package-env. Did not open cold compose init unroll. |
| `doc/SESSION-HANDOFF.md` | Living tip / Active product residual / Next; table rows |
| `.agents/reports/impl-residual-closeout-package-env-firstsurface-2026-08-12.md` | This report |

No product Lean. No emit C. No just modules. No claim-bool flips.

`src/systems/host-partial-inventory.md` still lists land-time harness=**42**
on the DualResidual row (Phase P 2026-08-08). That is a dated inventory
snapshot, not the living tip. Living counts stay in residual / WATCHER /
handoff. Did not rewrite the historical inventory.

---

## Queue after lockstep

| Name | Status | Role |
|------|--------|------|
| Compose SSA lower partial | **done** | `slake_compose_ssa.ll` 4542 bytes; gates green |
| SSA llvm-as expected set | **done** | Mult through Graph SSA and compose SSA assemble |
| Richer-than-i32 layout | **done** | IrNodeRecord 16/4; no product Rust; FullBackend false |
| IR consumer link smoke | **done** | `just llvm-link-smoke`; not FullBackend |
| LLVM IR compile and run timing | **done** | `just bench-llvm`; not CompCert; not FullBackend |
| Full host package elaborate meet | **done partial** | Inventory + PE43/PE44 + PE45..PE56; pin still false |
| Package-env for CompilePath unit TERM | **done** | PE45..PE52 twins; pin still false |
| Term-surface for CompilePath Plan Apply Body | **done** | TERM dual-ok 49 at land; pin still false |
| Package-env for CompilePath Plan Apply Body | **done** | PE53..PE55 twins; pin still false |
| Term-surface for FirstSurface | **done** | TERM dual-ok 50; harness 103 at land; pin still false |
| Package-env for FirstSurface | **done** | PE56 twin; harness 104; pin still false |
| Term-surface for LinearSubsetEmit | **open** | Highest-value next; next real host seed module; pin stays false |
| Cold compose init unroll | **not open** | Leftover mixed; operator notes stay not open |

Highest-value next: **Term-surface for LinearSubsetEmit**.

Why this Name: FirstSurface now has TERM dual-ok and a package-env twin.
Every existing TERM unit has a package-env twin. The next real host seed
module after FirstSurface is LinearSubsetEmit (ideal ladder Linear subset
emit). It is a real seed module with residual-free / ready / complete pins
and no TERM companion yet. This is not seed-wide 206. This is not a FullHost
flip.

---

## Pin proof (FullHost stays false)

`src/systems/SystemsLean/HostResidualShrink.lean`:

`def hostResidualShrinkFullHostElaborateRemains : Bool := false`

This lockstep did not edit that file. No FULLHOST_FLIP. No free / complete /
PROVABLY / DominanceClaimed / band FullBackend edits.

---

## Pins unchanged (must not forge)

| Pin | Status |
|-----|--------|
| free / complete / PROVABLY | **true** (do not re-open) |
| FullHostElaborateRemains | **false** |
| DominanceClaimed / band FullBackend | **false** |
| seed / harness / TERM dual-ok | 206 / 104 / 50 |
| package-env | Mult..ParityEmit + U2 + S5 plus PE43/PE44 plus PE45..PE56 (scope string still SurfaceMatrixEnv-only; not seed-wide) |
| PackageEnv line counts | FixturesS5 607; PackageEnv 805; Evidence 921 (all under 1000) |
| systems-emit-wire | green |
| FULLHOST_FLIP | no |
| Speed | empirical (`just bench-runtime`); never rename as PROVABLY |

No Lean claim-bool edits.

---

## Out of scope (honored)

- FullHostElaborateRemains / FULLHOST_FLIP
- Seed-wide 206 / mathlib / full Lean 4 elaborator parity
- DominanceClaimed / band FullBackend
- Product C; product Rust under src/
- Invented C fail-closed residual
- Cold compose init unroll
- Presenting a new plan
- New product Lean

---

## Next implement prompt (same as WATCHER fence)

/implement --effort 2 Term-surface for LinearSubsetEmit

Name: Term-surface for LinearSubsetEmit
Goal: Add typed term-surface (TERM) dual-ok for LinearSubsetEmit, the next real host seed module after FirstSurface now has TERM dual-ok and a package-env twin.
Done when: checkable TERM accept + reject twins for LinearSubsetEmit; focused lake green; FullHostElaborateRemains stays false; harness count honest if a new module is added.
Out of scope: FullHost flip; seed-wide 206; mathlib; DominanceClaimed; band FullBackend; product C; product Rust; package-env twins in the same slice unless they fit Sub-1-KLOC; cold compose init unroll; inventing a C fail-closed residual.
Paths: HostModuleCheckLinearSubsetEmitTerm.lean (new companion) and LoadOk / host presence as needed. Do not grow past Sub-1-KLOC; split if needed.
Gates: just hygiene; focused lake build of touched modules. FULLHOST_FLIP=no.
