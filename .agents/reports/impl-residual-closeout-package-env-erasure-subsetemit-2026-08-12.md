# Implement report: residual closeout after Package-env for ErasureSubsetEmit

**Date:** 2026-08-12
**Repo:** `/home/hunter/Projects/ai/iso`
**Kind:** residual / WATCHER / handoff / RESIDUAL.md lockstep only. No product Lean/C/Nix. No claim-bool flips.

ASCII only. Plain American English.

**Status:** **GREEN** (docs lockstep). `just hygiene` run after this report.

---

## Goal

Update living residual so the queue matches sibling evidence. Package-env for
ErasureSubsetEmit closed GREEN with reviews at 0 open. Mark that Name **done**.
Open next Name **Term-surface for ExtractSubsetEmit**. Full host package
elaborate meet stays **done partial** (inventory plus PE43..PE62; pin still
false). Do not flip `FullHostElaborateRemains`. Do not re-queue PE62 or
ErasureSubsetEmit TERM. Do not open cold compose init unroll. Do not invent
seed-wide 206, a FullHost flip, or a C product Name.

---

## Evidence trusted (not re-researched)

| Report | Outcome |
|--------|---------|
| `.agents/reports/impl-package-env-erasure-subsetemit-2026-08-12.md` | GREEN. PE62 residual-free reject twin plus E-good S16 plus dialect S16. Harness 110. TERM dual-ok 56. Files FixturesS5 989 / PackageEnv 907 / Evidence 964. Soft J2 CompilePathErasure-only (`packageBodyImportsCompilePathErasure`; no EmitErasure farm). Soft J5 `checkErasureSubsetEmitTermDialect` at package scope. Later-fold helper `packageEnvDialectRejectsIllTyped` kept PE45..PE62 dialect rejects in the same file. Did not split Evidence. |
| Review/mop (trashed; verdict on impl row) | GREEN / APPROVE 0. See `impl-package-env-erasure-subsetemit-2026-08-12.md`. |

Spot-checked (no product Lean logic edit):

- Seed list after `SystemsLean.ErasureSubsetEmit` is `SystemsLean.ExtractSubsetEmit`.
- There is no `HostModuleCheckExtractSubsetEmitTerm.lean` yet (this Open).
- ExtractSubsetEmit already has the four-claim TERM twin shape:
  `extractSubsetEmitResidualFreeClaimed := false`;
  `extractSubsetEmitExtractDialectOk := emitExtractReady`;
  multi-line `extractSubsetEmitReady`;
  `extractSubsetEmitProductSelfHostCompleteClaimed := false`.
- Seed 206 unchanged. Harness **110**. TERM dual-ok **56**. Package-env now
  through PE62 / S16.
- Evidence **964** is under Sub-1-KLOC (later-fold helper, no split).
- FixturesS5 **989** is near the bar for a later Extract PE twin. That warning
  lives on the later PE, not this TERM. A split Name is **not** open now.

Honesty notes only (not product misses; recorded on PE62 Status):

- `packageEnvTermSurfaceOk` header still says "Living Mult..EmitBody...".
- S14 comment still says "then S15" for the immediate hop.

---

## Paths touched

| Path | Change |
|------|--------|
| `RESIDUAL-systems.md` | Package-env for ErasureSubsetEmit **done**; Term-surface for ExtractSubsetEmit **open**; Full host meet stays **done partial** (now includes PE62); two-target table row 27 done / row 28 open; Done archive lockstep line; living tip + next-prompt |
| `RESIDUAL.md` | Join Open table + highest-value next + living tip same story |
| `WATCHER.md` | **IN PROGRESS** Term-surface for ExtractSubsetEmit. Fence `/implement --effort 2 Term-surface for ExtractSubsetEmit`. Did not re-queue PE62 or ErasureSubsetEmit TERM. Did not open cold compose init unroll. |
| `doc/SESSION-HANDOFF.md` | Living tip / Active product residual / Next / Remaining; table rows; WATCHER tip |
| `.agents/reports/impl-residual-closeout-package-env-erasure-subsetemit-2026-08-12.md` | This report |
| `/tmp/grok-1000/grok-impl-summary-residual-closeout-pe-ese.md` | Short summary for the parent |

No product Lean. No emit C. No just modules. No claim-bool flips.

`src/systems/host-partial-inventory.md` DualResidual harness snapshot was not
rewritten. Living counts stay in residual / WATCHER / handoff.

---

## Queue after lockstep

| Name | Status | Role |
|------|--------|------|
| Compose SSA / llvm-as / richer layout / link smoke / LLVM timing | **done** | already done |
| Full host package elaborate meet | **done partial** | Inventory + PE43..PE62; pin still false; next named meet-path is Extract TERM |
| Package-env for CompilePath unit TERM | **done** | PE45..PE52 |
| Term-surface for CompilePath Plan Apply Body | **done** | |
| Package-env for CompilePath Plan Apply Body | **done** | PE53..PE55 |
| Term-surface for FirstSurface | **done** | |
| Package-env for FirstSurface | **done** | PE56 / S10 |
| Term-surface for LinearSubsetEmit | **done** | harness 105 at land; TERM 51 at land; pin false |
| Package-env for LinearSubsetEmit | **done** | PE57 / S11 |
| Term-surface for TypesSubsetEmit | **done** | harness 106 at land; TERM 52 at land |
| Package-env for TypesSubsetEmit | **done** | PE58 / S12 |
| Term-surface for ProgramSubsetEmit | **done** | harness 107 at land; TERM 53 at land |
| Package-env for ProgramSubsetEmit | **done** | PE59 / S13 |
| Term-surface for GraphSubsetEmit | **done** | harness 108; TERM 54 |
| Package-env for GraphSubsetEmit | **done** | PE60 / S14 |
| Term-surface for ComposeSubsetEmit | **done** | harness 109 at land; TERM 55 at land |
| Package-env for ComposeSubsetEmit | **done** | PE61 / S15 |
| Term-surface for ErasureSubsetEmit | **done** | harness 110; TERM 56; pin false |
| Package-env for ErasureSubsetEmit | **done** | PE62 / S16; FixturesS5 989 / PackageEnv 907 / Evidence 964 |
| Term-surface for ExtractSubsetEmit | **open** | Highest-value next; clone ErasureSubsetEmit TERM; pin stays false |
| Package-env for ExtractSubsetEmit | **not this Open** | PE twin comes after this TERM |
| Cold compose init unroll | **not open** | operator notes stay not open |

Highest-value next: **Term-surface for ExtractSubsetEmit**.

Why this Name: ErasureSubsetEmit now has both TERM dual-ok and PE62 / S16.
Established meet path is TERM first, then the PE twin. Clone
`HostModuleCheckErasureSubsetEmitTerm`. This is not seed-wide 206. This is not
a FullHost flip. This is not a re-queue of PE62 or ErasureSubsetEmit TERM.

Expected TERM land: harness **110 -> 111**; TERM dual-ok **56 -> 57**; LoadOk
later-fold stays **914** / under 1000; do **not** edit PackageEnv; residual-free
and complete claimed stay false; ready multi-line via
`extractSubsetEmitReadyTermBody` (product fold is `extractSubsetEmitReady`).

---

## Next Name contract (living)

Name: Term-surface for ExtractSubsetEmit

Goal: Add accept plus reject term-surface twins for ExtractSubsetEmit now that
ErasureSubsetEmit has both TERM and package-env twins.

Done when (checkable):

- New `HostModuleCheckExtractSubsetEmitTerm` companion cloned from
  `HostModuleCheckErasureSubsetEmitTerm`.
- LoadOk later-fold folds ExtractSubsetEmit SurfaceOk. LoadOk stays **914** /
  under 1000.
- RealModule `refineExtractSubsetEmitWithTermSurface`.
- Accepts uses TERM text not L0 FixtureTextsLater stub.
- Four ILL twins: residual-free claimed true
  (`extractSubsetEmitResidualFreeClaimed`); emit body true
  (`extractSubsetEmitExtractDialectOk` / living `emitExtractReady`); ready body
  true (`extractSubsetEmitReady`); complete claimed true
  (`extractSubsetEmitProductSelfHostCompleteClaimed`).
- L0 accept + dialect reject (`ILL-TYPED-TERM`).
- E-good emit twins living `emitExtractReady`.
- Residual-free and complete claimed stay false.
- Ready multi-line via `extractSubsetEmitReadyTermBody` (same collapse as
  Erasure; product fold is `extractSubsetEmitReady`).
- Soft J2 ExtractSubsetEmit-only.
- Harness **110 -> 111**. TERM dual-ok **56 -> 57**.
- FullHostElaborateRemains stays **false**.
- Do **not** edit PackageEnv in this slice.
- `just hygiene` plus focused lake family plus `just systems-host` green.

Out of scope: FullHost flip; seed-wide 206 / mathlib; DominanceClaimed / band
FullBackend; product C; product Rust; Package-env for ExtractSubsetEmit (PE
twin comes after this TERM); rewrite of ErasureSubsetEmit TERM; re-queue PE62;
cold compose init unroll; inventing a C fail-closed residual.

Paths: new `HostModuleCheckExtractSubsetEmitTerm.lean`; LoadOk later-fold;
RealModule; Accepts; HostModuleCheck; SystemsLean; HostResidualShrink family.
Do not grow past Sub-1-KLOC.

---

## FixturesS5 near-bar note (do not open a split now)

`HostModuleCheckPackageEnvFixturesS5.lean` is **989** lines. That is near the
Sub-1-KLOC bar for a later Extract PE twin. Do **not** open a split Name now.
Do **not** edit PackageEnv in the Extract TERM slice. The later PE twin owns
any FixturesS5 split if that land would exceed 1000.

Evidence is **964** after the later-fold helper. No Evidence split was needed
for PE62 and none is open now.

---

## Pin proof (FullHost stays false)

This lockstep did not edit `hostResidualShrinkFullHostElaborateRemains`.
No FULLHOST_FLIP. No free / complete / PROVABLY / DominanceClaimed / band
FullBackend edits.

---

## Pins unchanged (must not forge)

| Pin | Status |
|-----|--------|
| free / complete / PROVABLY | **true** (do not re-open) |
| FullHostElaborateRemains | **false** |
| DominanceClaimed / band FullBackend | **false** |
| seed / harness / TERM dual-ok | 206 / 110 / 56 |
| package-env | Mult..ParityEmit + U2 + S5 plus PE43/PE44 plus PE45..PE62 (PE62 / S16 landed; scope string still SurfaceMatrixEnv-only; not seed-wide) |
| PackageEnv line counts | FixturesS5 989; PackageEnv 907; Evidence 964 (all under 1000) |
| systems-emit-wire | green |
| FULLHOST_FLIP | no |
| Speed | empirical (`just bench-runtime`); never rename as PROVABLY |

No Lean claim-bool edits.

---

## Out of scope (honored)

- FullHostElaborateRemains / FULLHOST_FLIP
- Seed-wide 206 / mathlib / full Lean 4 elaborator parity
- DominanceClaimed / band FullBackend
- Product C; product Rust under src/
- Re-queue PE62 or ErasureSubsetEmit TERM
- Opening Package-env for ExtractSubsetEmit now
- Opening an Evidence or FixturesS5 long-file split Name now
- Invented C fail-closed residual
- Cold compose init unroll
- Presenting a new plan
- New product Lean

---

## Hygiene

`just hygiene`: **exit 0** (1428 files; 755 markdown).

---

## Next implement

WATCHER fence:

```
/implement --effort 2 Term-surface for ExtractSubsetEmit
```

Do not implement that product Lean in this lockstep.
