# Report: Phase B2 FullHostElaborateRemains evidence criteria (2026-08-07)

ASCII only. Explore-only. No product code. No claim flips.

**Sources:** `.agents/plans/plan-slake-replaces-lake.md` (Phase B / section 5);
`RESIDUAL-systems.md` Open queue; `HostResidualShrink.lean`; `DualResidual.lean`;
`HostModuleCheck.lean` depth pins; `.agents/joins/a60-develop-tool-inventory-closed.md`;
`WATCHER.md`; `src/systems/self-host.md` host finish line; prior
`.agents/plans/plan-slake-replaces-lake.md`.

---

## 1. Exact Done when (quote briefly)

### Plan Phase B2 (section 4 table + section 5 Immediate next)

| Field | Plan text |
|-------|-----------|
| **Name** | FullHostElaborateRemains false |
| **Goal** | Only when library day-to-day no longer needs cold full Lake; set pin false with evidence |
| **Done when (table)** | Pin false + evidence + residual honesty |
| **Done when (section 5)** | Written cold-elaborate day-to-day retirement evidence + pin false dual-ok in HostResidualShrink + residual honesty; DualResidual free stays false; free/complete/PROVABLY and product Lake pins unchanged; `just hygiene` green |
| **Blocked until** | Evidence that day-to-day host develop no longer requires cold full `lake build` of the library, **or** the operator names a checkable evidence slice |
| **Out of scope** | DualResidual free flip; delete Lake binary theater; forge pin false; invent *Main seed |

B2 is **not** DualResidual free. Free is Phase F / A-last after the full layer-1 checklist.

### Residual Open (`RESIDUAL-systems.md`)

| Field | Content |
|-------|---------|
| **Name** | FullHostElaborateRemains false |
| **Goal** | Plan Phase B2: only when library day-to-day develop no longer needs cold full Lake, set FullHostElaborateRemains false with evidence and residual honesty |
| **Done when** | Real evidence that cold full `lake build` under `src/systems/` is bootstrap/emergency/claim-proof only for day-to-day host develop; HostResidualShrink pin false with dual-ok; DualResidual free still false unless full free checklist also met; free/complete/PROVABLY and product Lake pins unchanged unless separately evidenced; `just hygiene` green |
| **Out of scope** | Flip pin without cold-elaborate retirement evidence; DualResidual free forge; delete Lake binary theater; host free claim alone |
| **Status** | open (held for evidence; do not forge) |

### Living pins (product Lean today)

| Pin | Value | File |
|-----|-------|------|
| `hostResidualShrinkFullHostElaborateRemains` | **true** | `src/systems/SystemsLean/HostResidualShrink.lean` |
| `hostElaboratorResidualRemains` | **true** | `src/systems/SystemsLean/DualResidual.lean` |
| `hostElaboratorResidualFreeClaimed` | **false** | same |
| `hostResidualShrinkHostFreeClaimed` | **false** | HostResidualShrink |
| `hostResidualShrinkBootstrapPrebuildRemains` | **false** (A8) | HostResidualShrink |
| `hostResidualShrinkDiagnosticLakeRecipesRemain` | **true** | HostResidualShrink |
| A60 `hostDevelopToolInventoryClosedDualOk` | closed; day-to-day candidates **empty** | HostResidualShrink + join a60 |
| Check depth | `PARTIAL-STRUCTURAL` + Mult..Extract `FOUNDATION-KIND-SURFACE` | HostModuleCheck |
| Seed / L-band | modules=206 seed; *Main 59 + harness 5 out-of-scope | A55/A59 |

Comment on the full-elaborate pin: full SystemsLean elaborate still requires
Lake (not peer fragment alone). A8 only retired peer bin prebuild via leanc
when IR exists; total wipe still needs classic Lake library elaborate once.

---

## 2. What evidence is missing today (concrete)

B2 Done when needs **retirement evidence**, not another seed-count or tool-list
row. Missing pieces:

1. **No written cold-elaborate day-to-day retirement evidence.** A60 join and
   WATCHER state explicitly: no evidence that cold full `lake build` under
   `src/systems/` is bootstrap/emergency/claim-proof only for day-to-day host
   develop. Pin stays true on purpose.

2. **Slake path is not full elaborator typecheck.** `HostModuleCheck` honesty is
   PARTIAL-STRUCTURAL (package-local imports + key decls). Deepen bar is
   FOUNDATION-KIND-SURFACE for Mult..Extract only (structures/axioms + theorem
   corpus counts). That is **not** full Lean 4 elaborate/typecheck of module
   bodies for the 206-module seed, let alone for *Main or full package cold
   build.

3. **Day-to-day develop still depends on Lake for library elaborate.** Operator
   free-checklist choice (a) is full host develop: retire cold full elaborate
   from day-to-day. Inventory surfaces that still need Lake for that job:
   cold `just systems-lake` / `lake build` under `src/systems/`; claim-proof
   lake on PATH for claim flips; diagnostic Lake recipes remain true; *Main
   (59) lake exe entrypoints remain Lake-built (L-band out-of-scope honesty,
   not "owned without Lake elaborate").

4. **No dual-ok for FullHostElaborateRemains false.** There is no greppable
   measured false dual-ok today; residual-shrink and presence specs expect the
   pin **true**. Forging false would fail honesty gates and residual law.

5. **Layer-1 checklist item 5 unmet.** Plan section 2: cold full `lake build` is
   bootstrap/emergency/claim-proof only. Items 1-2 (roots, import graph on seed)
   and tool ownership (B1) are done at their honesty bars; item 3 (check/elaborate
   including theorems) is partial structural deepen only; item 6 (DualResidual free)
   is after B2 + full free checklist.

6. **WATCHER is BLOCKED.** Fence has no `/implement` token. Auto-run must not
   spin a pin flip.

What A1-A60 **did** buy (not B2 evidence): seed 206 PARTIAL-STRUCTURAL;
FOUNDATION-KIND-SURFACE Mult..Extract; L-band leftover empty; day-to-day tool
candidates empty + claim/diagnostic Lake-only explicit; bootstrap prebuild false.
Plan: seed / L-band / B1 closed as treadmill; do not re-open as seed theater.

---

## 3. Checkable intermediate without pin flip?

### Verdict on living residual: **BLOCKED for agents without a new operator-named slice**

| Question | Answer |
|----------|--------|
| Is B2 implementable now? | **No.** Evidence-held; forge forbidden. |
| Is DualResidual free flipable? | **No.** A-last only after full free checklist. |
| Is there already an Open intermediate Name? | **No.** Open queue has only FullHostElaborateRemains false (held). |
| Does plan name a default intermediate? | **No.** Section 5: blocked until evidence **or** operator names a checkable evidence slice. Optional further check-depth / prose scrub only as a **named** Open residual, not auto next. |
| May agents invent a slice to keep the treadmill warm? | **No** as substitute for B2 evidence. Plan and residual ban inventing seed/*Main theater and ban forging free/complete/PROVABLY. |

**Conclusion:** With only living law as written, the honest posture is
**BLOCKED**. Only the human can (a) supply cold-elaborate retirement evidence
that satisfies Done when, or (b) **name** a checkable evidence or capacity slice
that does not flip FullHostElaborateRemains or DualResidual free.

### Capacity work that does **not** satisfy B2 alone

Even if the operator later names them, these **advance technical path** but do
**not** by themselves set FullHostElaborateRemains false:

- Further FOUNDATION-KIND-SURFACE (or deeper) bands past Mult..Extract
- Stronger HostModuleCheck toward real body elaborate on a named seed subset
- Pure inventory refresh of "which just recipes still invoke lake for develop"

None of those rewrite the cold full package elaborate story until measured
day-to-day develop no longer needs that cold Lake step.

### If the operator wants a safe intermediate (template only; not opened)

Suggested residual shape **only if the human opens it**. Agents must not open
this without operator OK.

| Field | Content |
|-------|---------|
| **Name** | Cold elaborate evidence protocol |
| **Goal** | Define and dual-pin a written, checkable protocol for what counts as day-to-day host develop vs bootstrap/emergency/claim-proof Lake, without flipping FullHostElaborateRemains or DualResidual free |
| **Done when** | Research note under `doc/dev/research/` lists: (1) every measured without-Lake host develop recipe already green (roots, import-graph, module-check, foundation+develop tools); (2) every remaining Lake-using step with class bootstrap / claim-proof / diagnostic / day-to-day; (3) explicit accept criteria for future B2 (what evidence would reclassify cold full `lake build` as non-day-to-day); (4) HostResidualShrink still has FullHostElaborateRemains **true** dual-ok; DualResidual free **false**; residual Open still names B2 held; `just hygiene` green; short join under `.agents/joins/` |
| **Out of scope** | Flip FullHostElaborateRemains; DualResidual free; host free; delete Lake; invent full elaborator typecheck slogans; re-open seed expand theater |
| **Primary paths** | `doc/dev/research/cold-elaborate-evidence-protocol-YYYY-MM-DD.md`; optional greppable honesty strings in HostResidualShrink **without** pin false; residual / WATCHER / handoff honesty only |
| **Gates** | `just hygiene`; optional `just host-residual-shrink` still GREEN with pin true; no lake claim-flip required |

That intermediate **documents** evidence criteria; it does **not** close B2.

A stronger intermediate (also operator-named only) would be **named check-depth
or real-elaborate capacity** on a bounded module band: green HostModuleCheck
deeper bar, modules seed unchanged or honestly expanded, pins unchanged. Still
not B2 Done when until cold day-to-day retirement evidence exists.

---

## 4. Highest-value next work (unblock / parallelize)

Ranked for human + agent coordination (not auto-run B2 forge):

1. **Operator decision on evidence posture (highest leverage).** Either:
   - supply / approve written cold-elaborate day-to-day retirement evidence that
     meets residual Done when (then B2 implement is unblocked), **or**
   - name a checkable intermediate (protocol inventory and/or deepen band) so
     agents have non-forge work without spinning WATCHER.

2. **If capacity path: operator-named deepen toward real elaborate on a bounded
   band.** HostModuleCheck still PARTIAL-STRUCTURAL ceiling for most modules.
   A named T-style residual (deeper bar or next band) is the only technical path
   that could eventually support honest "cold Lake is bootstrap-only" claims.
   Keep FullHostElaborateRemains true and DualResidual free false.

3. **Do not parallelize seed expand, L-band re-open, tool inventory re-open, or
   DualResidual free.** Those treadmills are closed or held. Parallel only
   disjoint non-claim work the operator names (e.g. optional H1 living prose
   scrub, non-host tracks). Product free / complete / PROVABLY / product Lake
   pins: preserve only.

---

## 5. Bottom line

| Item | Status |
|------|--------|
| B2 Done when | Pin false **plus** written cold-elaborate day-to-day retirement evidence + dual-ok + residual honesty; DualResidual free stays false |
| Evidence today | **Missing.** Pin true. Check path structural, not full elaborate. Tools B1 closed does not retire cold library elaborate |
| Intermediate already open | **None** |
| Agent default | **BLOCKED.** Human supplies evidence or names a checkable evidence/capacity slice |
| Do not | Flip FullHostElaborateRemains; flip DualResidual free; re-open A55-A60 theater; forge free/complete/PROVABLY |

**WATCHER remains BLOCKED** until one of those human gates clears.
