# Root Lake workspace (path monorepo)

## Root package name

**`Iso`**

- Product host package stays **`SystemsLean`** at `src/systems` (operator preference: that name is the real product).
- Root cannot also be named `SystemsLean` while `require`ing the systems path package as a dependency (root would collide with its own dep name).
- Operator rejected `SystemsLeanWorkspace`.
- **`Iso`** is a short plain monorepo umbrella name matching the checkout path nickname; it does not steal the product package name.

## Paths created / updated

| Path | Action |
|------|--------|
| `/home/hunter/Projects/ai/iso/lakefile.lean` | **Created** -- thin umbrella; `require` path deps only |
| `/home/hunter/Projects/ai/iso/lean-toolchain` | **Created** -- `leanprover/lean4:v4.32.0` (same as nested) |
| `/home/hunter/Projects/ai/iso/lake-manifest.json` | **Regenerated by `lake update`** (not hand-aligned) |

Not touched: `src/systems/**` Host* seed work, residual/WATCHER, just recipes, nested package names or offline empty manifests.

### Root `lakefile.lean` (summary)

- Package: `Iso`
- `require SystemsLean from "src" / "systems"`
- `require SystemsLeanLean4 from "src" / "lean4"`
- No product libs/exes at root; offline only (no mathlib / remote deps)

### Root `lake-manifest.json` (post-update)

- `"name": "Iso"`, `"version": "1.2.0"` (Lake's lock format, not the earlier hand 1.0.0)
- Path packages: `SystemsLean` -> `src/systems` (`lakefile.lean`), `SystemsLeanLean4` -> `src/lean4` (`lakefile.toml`)
- Nested `src/systems/lake-manifest.json` and `src/lean4/lake-manifest.json` left as empty offline manifests (`packages: []`)

## Verification

| Command | Exit | Notes |
|---------|------|--------|
| `lake update` (repo root) | **0** | Offline; toolchain already up-to-date |
| `lake env` | **0** | `LEAN_PATH` / `LEAN_SRC_PATH` include `src/systems` and `src/lean4` |
| `lake script list` | **0** | Empty scripts list (umbrella has none) |
| `lake query @SystemsLean` | **0** | Resolves product path package |
| `lake query @SystemsLeanLean4` | **0** | Resolves lean4 dual path package |
| `lake check-build --allow-empty` | **1** | Expected: root has no default targets (thin umbrella) |

Did **not** run full systems 50+ exe build or host-without-lake seed gates.

## Manifest source

**`lake update`** rewrote root `lake-manifest.json`. Prefer Lake output over prior hand pretty-JSON / version 1.0.0.

## Follow-up (not this slice)

- CI / `just` product path still uses nested `src/systems` Lake package day to day; root umbrella is optional monorepo entry, not a claim that CI switched.
- Root has no default build target; use `@SystemsLean/...` or nested `cd src/systems && lake ...` for real product builds.
- Optional README one-liner that root Lake loads path deps was skipped (min useful; discovery is the lakefile).
