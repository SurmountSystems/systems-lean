# Wave 3 Name 9 Single fail-closed walk -- implementer report

**Status:** GREEN

**Name:** Single fail-closed walk

**Date:** 2026-08-12

## What walk was collapsed

Before, a compose `check_fail_closed` nested three well-typed walks:

1. `slake_host_compose_check_fail_closed` called `slake_ir_graph_is_well_typed`
   (program well-typed + edge soundness).
2. Then Mult 1 / Mult 0 pre-scan.
3. Then `slake_ir_graph_check_fail_closed`, which called
   `slake_ir_graph_is_well_typed` again, then `slake_ir_program_check_fail_closed`.
4. Program check called `slake_ir_program_is_well_typed` (node well-typed walk),
   then `slake_ir_node_check_fail_closed` per live node (each node check still
   requires the node to be well-typed).

After (cheaper honest shape: one live walk that types and fail-closes at
program; graph check does not re-call graph well-typed; compose check does
not re-call graph well-typed):

| Surface | Collapsed | Kept |
|---------|-----------|------|
| `slake_ir_program_check_fail_closed` | No call to `slake_ir_program_is_well_typed` | Guards (null / invalid / empty / over-cap -> FAIL_CLOSED) + one live-node loop via `slake_ir_node_check_fail_closed` |
| `slake_ir_graph_check_fail_closed` | No call to `slake_ir_graph_is_well_typed` | Edge soundness, then `slake_ir_program_check_fail_closed`. Empty graph still OK |
| `slake_host_compose_check_fail_closed` | No call to `slake_ir_graph_is_well_typed` | Mult 1 / Mult 0 pre-scan, then `slake_ir_graph_check_fail_closed` |

Greppable: `SINGLE_FAIL_CLOSED_WALK` (program + graph), `HOST_COMPOSE_SINGLE_WALK`
(compose).

Owners: `host_emit_{program,graph,compose}.ssot.txt` + matching Lean dual
strings in `Emit{Program,Graph,Compose}Scaffold.lean`. Ownership prose in
`src/systems/emit/host-owned-emit.md`. Product wire regenerated by `just build`
into `src/systems/emit/slake_freestanding.{c,h}` and `out/freestanding-c/`.
No hand product C.

## Public extract / check semantics unchanged

How we know:

- Public `slake_ir_{node,program,graph}_is_well_typed` and
  `slake_host_compose_is_well_typed` still exist and still walk well-typed.
- Public `*_check_fail_closed` still return `SLAKE_EXTRACT_OK` or
  `SLAKE_EXTRACT_FAIL_CLOSED` with the same fail-closed cases: null, invalid,
  empty program, over-cap, ill-typed node, spent Mult 1, missing Mult 0 mark.
- Empty graph remains OK at graph check. Ill-typed still FAIL_CLOSED (node
  check still requires well-typed).
- Extract still runs on the primary cold arm (living report: "full re-init
  each op; extract not skipped"). E2 fuse still one check then write
  `RUNTIME_FS`.
- `just systems-cc-probe` (behavioral probe) still calls public
  `slake_ir_program_is_well_typed`, `slake_host_compose_check_fail_closed`,
  and `slake_extract_with_checks` with the same OK / FAIL_CLOSED expects.
  Exit 0.

## HostCost Nat changed?

**No.** HostCost models Lean `HostCompose` (`checkFailClosed` = well-typed
bar + `multPreScan`; `extractOkFs` = freestanding-goal + check). It does not
count C nested walks. Defs and theorems were not edited this slice.

- `costCheckFailClosed n = costHostIsWellTyped + costMultPreScan n`
- `costExtractOkFs n = costFreestandingGoal + costCheckFailClosed n`
- Fuse theorems from Wave 2 stay: `costMultPreScan_eq_live_nodes`,
  `costCheckFailClosed_eq_live_nodes`, `hostCost_spent_mult1_rejects_check`,
  `hostCost_spent_mult1_rejects_extractFs`,
  `hostCost_omega_share_vs_mult1_exact_once`,
  `hostCost_thin_extract_mult1_disagrees_compose`.

Did not run `lake build SystemsLean.HostCost` (Nat model unchanged).

## Gates (commands + exit)

| Command | Exit |
|---------|------|
| `lake build slake-freestanding-capable-regenerate` | 0 (required: prebuilt writer had stale fragments; first `just build` failed DUAL-SSOT-EQUALITY on PROGRAM HEADER) |
| `just build` | 0 (after regenerate rebuild) |
| `just systems-cc-probe` | 0 |
| `just systems-emit-wire` | 0 |
| `just hygiene` | 0 (pre-doc lockstep; re-run after this report) |
| `just bench-runtime` | 0 (full suite; default `-O3 -flto`) |

## Bench stamp + ratios

Living report: `doc/BENCH-RUNTIME-latest.md`

- **Generated:** 2026-08-12 19:29:51 UTC
- **Stamp:** `20260812T192951Z`
- Timing-link flags: **`-O3 -flto`**

### Compose (two ratios; do not claim compose beat heap)

| Comparison | free-compose | baseline | Phrase | Ratio (baseline/free) |
|------------|-------------:|---------:|--------|----------------------:|
| vs heap_churn (allocator) | 17.5277 | 10.6737 | **~1.6x slower** | 0.609 |
| vs fail_closed_shape (protocol; not product wire) | 17.5277 | 18.2351 | **~1.0x faster** | 1.040 |

Prior Wave 2 stamp `20260812T190625Z`: compose ~2.5x slower vs heap, ~1.5x
slower vs fail_closed_shape. Compose **improved**. Compose did **not** beat
heap.

### Mint vs heap / stack twin

| Comparison | free | baseline | Phrase | Ratio |
|------------|-----:|---------:|--------|------:|
| vs heap_churn (allocator) | 0.6645 | 0.6748 | **~1.0x faster** | 1.016 |
| vs stack live-flag twin (protocol; not product wire) | 0.6645 | 0.6336 | **~1.0x slower** | 0.953 |
| vs Boehm | 0.6645 | 18.7710 | **~28.2x faster** | 28.248 |
| vs Lean managed | 0.6645 | 7.4700 | **~11.2x faster** | 11.242 |

User-facing: root `README.md` item 8, `doc/SESSION-HANDOFF.md` living tip,
`out/freestanding-c/README.md` runtime digest. PROVABLY is CompCert, not speed.

## Honest leftover (Name 10 verdict)

Stage timers (separate pass; not the same ns as whole-op 17.5) still show
mixed cost: init ~22, push_nodes ~25, edges ~24, mint ~23, mark ~23,
well_typed ~25, extract ~26, consume ~24, post_check ~24. `check_fail_closed`
stage reads 0.0000 on this pass (attribution only; extract still runs).

Remaining compose vs heap ~1.6x is **not clearly init-only**. It is mixed:
cold init + bench explicit `is_well_typed` + Mult pre-scan + per-node check
+ extract + post_check.

**Name 10 Init unroll stays not open.**

Bootstrap subset packages `emit/slake_{program,graph,compose}_subset.c` still
nest well-typed walks. Those are M1 subset packages, not product Out / not
the cold compose arm. `just build` regenerates `slake_freestanding.{c,h}` +
`out/freestanding-c/`. Not Name 10.

## Pins untouched

- free / complete / PROVABLY: **true**
- FullHostElaborateRemains: **false**
- DominanceClaimed / band FullBackend: **false**
- seed 206 / harness 99 / TERM dual-ok 46
- package-env: Mult..ParityEmit + U2 + S5 (not seed-wide)
- systems-emit-wire: green
- FULLHOST_FLIP=no
- No fourth Mult / SLAKE_MULT_3
- No seed-wide H2 / mathlib / FullHost true

## Residual lockstep

- `RESIDUAL-systems.md`: Name 9 **done**; living Open **empty**; Name 10
  **not open**.
- `RESIDUAL.md` Systems Open: Name 9 **done**; living Open empty; Wave 4 next.
- `WATCHER.md`: Wave 3 done; `/implement` Wave 4 mop + review + closeout.
- `doc/SESSION-HANDOFF.md`: Active/Next + stamp `20260812T192951Z`.

## Open remaining

Empty product Open. Wave 4 process mop + review + closeout is next (not an
Open Name). Name 10 not open.
