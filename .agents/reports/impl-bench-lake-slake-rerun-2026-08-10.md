# Impl report: Lake vs Slake bench re-run (2026-08-10)

Kind: measurement re-run + user-facing tip refresh. Not a residual claim flip.
Not free / complete / PROVABLY / FullHostElaborateRemains forge.

Explore inventory: `/tmp/grok-1000/explore-chi-rust-and-benches-2026-08-10.md`.

---

## Commands and exits

| Step | Command | Wall (approx) | Exit |
|------|---------|---------------|------|
| Warm Lake (untimed bootstrap; not suite arm) | `(cd src/systems && lake build)` | 314 s | 0 |
| Compile-path suite | `just bench` | 113 s | 0 |
| Runtime suite | `just bench-runtime` | 77 s | 0 |
| Cold full Lake | `just bench-lake-host-cold` | **skipped** | n/a |

### Skip: `just bench-lake-host-cold`

Destructive trash of `src/systems/.lake` then full rebuild. Historical cold class was multi-minute to ~800 s (research `doc/dev/research/lake-vs-slake-proper-bench-2026-08-01.md`). This session already spent ~314 s on a non-cold warm rebuild dominated by `SystemsLean.SelfHostBodyTheorems` (~312 s). Default suite intentionally omits cold wipe; operator can re-run explicitly.

Logs:

- Compile: `/tmp/systems-lean-bench-20260810T223544Z.log` (tee `/tmp/grok-1000/bench-compile-run-2026-08-10.log`)
- Runtime: `/tmp/systems-lean-bench-runtime-20260810T223743Z.log` (tee `/tmp/grok-1000/bench-runtime-run-2026-08-10.log`)

Living reports rewritten by suites:

- `doc/BENCH-latest.md` (Generated: 2026-08-10 22:35:44 UTC)
- `doc/BENCH-RUNTIME-latest.md` (Generated: 2026-08-10 22:37:43 UTC)

---

## Compile path headlines (`just bench` / `doc/BENCH-latest.md`)

Different jobs: Arm A freestanding product wire emit vs Arm B classic Lake host package elaborate. Not freestanding peer elaborator.

| Scenario | real (s) |
|----------|----------|
| A-hot freestanding | **0.142** |
| A-without-Lake host | **2.107** |
| A-cold freestanding | **0.295** |
| B-hot first pass (Lake) | **1.202** |
| B-hot second pass (Lake idle hot) | **0.412** |
| CompCert matrix (seal path) | **109.050** |

Plain English:

- Idle-hot Lake second pass is about **2.9x** the wall of hot freestanding emit (0.412 / 0.142); jobs differ.
- Day-to-day without-Lake host tools total about **2.1 s** (not full elaborator parity).
- Cold freestanding product wire rebuild about **0.30 s** after trash of generated wire.

---

## Runtime headlines (`just bench-runtime` / `doc/BENCH-RUNTIME-latest.md`)

Primary operator question: linear freestanding runtime vs GC / Lean managed / heap_churn. **Not** Lake-vs-Slake compile.

| Baseline | Freestanding vs baseline (this run) |
|----------|-------------------------------------|
| Boehm GC | **~6.7x faster** (4.0455 vs 27.1691 ns/op) |
| Lean managed runtime | **~2.8x faster** (4.0455 vs 11.4740 ns/op; different N / not same C IR) |
| heap_churn malloc/free | **~4.5x slower** (4.0455 vs 0.9040 ns/op) |
| compose vs compose heap_churn | **~2.8x slower** (93.7815 vs 33.3964 ns/op) |

Honesty vs prior living tip (2026-08-09 digest ~9.3x Boehm / ~3.4x Lean / ~3x slower heap):

- Wins vs **Boehm** and **Lean managed** still hold; absolute multipliers **moved** (machine noise, load, driver variance). User-facing tips updated to **this run**, not the older higher multipliers.
- Freestanding remains **slower** than pure malloc/free microchurn; docs still say slower for that baseline.
- PROVABLY is wire correctness only, not speed.

---

## User-facing doc updates (AGENTS Benchmarks rule)

| Surface | Change |
|---------|--------|
| `doc/BENCH-latest.md` | Regenerated by `just bench` |
| `doc/BENCH-RUNTIME-latest.md` | Regenerated by `just bench-runtime` |
| Root `README.md` living tip | Runtime line: 6.7x Boehm / 2.8x Lean / 4.5x slower heap; link kept |
| `doc/SESSION-HANDOFF.md` | Measurement block date + numbers + compile secondary one-liner |
| `out/freestanding-c/README.md` | Runtime evidence digest line with same headlines |
| `just/README.md` | No recipe change; optional row left alone |

No residual Open invent. No free/complete/PROVABLY flip.

---

## Hygiene

After novel markdown edits: `just hygiene` (ASCII + trailing WS + professional-tone).
