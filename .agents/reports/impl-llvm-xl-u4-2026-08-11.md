# Implement report: DominanceClaimed or band FullBackend XL deepen (U4)

**Date:** 2026-08-11
**Status:** GREEN (honest hold path B)
**Name:** DominanceClaimed or band FullBackend XL deepen (U4)

## Goal

Best-effort XL product dual-pin for DominanceClaimed and/or band Mult..Graph FullBackend,
or deepest honest hold with no missing next-step docs. multiUnit Mult..Graph DF deepen true
is **not** DominanceClaimed. tip fullBackend A-light true is **not** band FullBackend.
Do **not** forge.

## Design (honest hold -- path B)

Path **(A)** requires written XL bar + product dual-pin + pure Nix forge-ban invert + lake.
No checkable A-heavy product dual-pin remains: multiUnit Mult..Graph Cytron DF join is already
true; remaining gaps are a proved dominator-tree constructor, arbitrary-graph DF export, and
operator redesign of pure Nix forge-bans. Path **(B)** delivered: honest hold reseed;
pins stay measured false; bar + inventory + consumer README + residual lockstep.

### Why hold (R3/W4/T4 reasons re-confirmed)

| Reason | Detail |
|--------|--------|
| Structural idom only | Per-band production idom tables are hand dual-pins, not a proved dominator-tree constructor |
| Fixed unit diamonds | Mult..Graph DF covers unit SSA diamonds only, not arbitrary-graph DF export or multi-join production pipeline |
| Path B bar (2) | `dominancePipelineRequiredForFullBackend` is **false**; progressive bar already met without DominanceClaimed |
| Forge ban | Pure Nix forbids `def llvmCfgFixtureDominanceClaimed : Bool := true` and band `*FullBackendClaimed := true` until operator XL redesign |
| Band FullBackend A-heavy | Fixture tip `llvmCfgFixtureFullBackendClaimed` **true** (A-light) is a separate pin; band Mult..Graph `*FullBackendClaimed` stay **false** by design |

### What XL path (A) would still need (not this slice)

| Requirement | Status after U4 |
|-------------|-----------------|
| Written XL bar for DominanceClaimed true and/or band FullBackend true | Held optional; documented; not redesign + invert this slice |
| Proved dominator-tree constructor | **missing** |
| Arbitrary-graph DF export beyond Mult..Graph unit diamonds | **missing** |
| Pure Nix forge-ban invert | **not inverted** (bans still require false) |
| Lake elaborator proof after pin flip | **n/a** (no flip) |

## Paths touched

| Path | Role |
|------|------|
| `doc/dev/research/full-llvm-production-backend-bar-2026-08-10.md` | U4 reseed + hold pin table; XL path A checklist |
| `doc/dev/research/llvm-backend-deepen-inventory-2026-08-08.md` | U4 candidate row done; honesty bullets; harness tip 64 |
| `out/llvm-ir/README.md` | Consumer lockstep U4 hold note |
| `RESIDUAL-systems.md` | U4 Open -> done; living Open U7/U1/U2/U3/U5/U6 |
| `RESIDUAL.md` | Systems Open join U4 done; evidence + Done archive |
| `WATCHER.md` | U4 closed; remaining multi-track Open |
| `doc/SESSION-HANDOFF.md` | Active open lockstep (U4 done) |
| `.agents/reports/impl-llvm-xl-u4-2026-08-11.md` | This report |

**No product Lean claim-bool edits.** Pins left exactly as measured.

## Pin honesty (mandatory table)

| Pin | After U4 |
|-----|----------|
| `multiUnitDominanceFrontierDeepen` | **true** (unchanged; Mult..Graph) |
| `llvmCfgFixtureDominanceClaimed` | **false** |
| Mult..Graph band `*FullBackendClaimed` | **false** each |
| `llvmCfgFixtureFullBackendClaimed` | **true** (A-light tip; unchanged) |
| `dominancePipelineRequiredForFullBackend` | **false** |
| free / complete / PROVABLY | **true** unchanged |
| FullHostElaborateRemains | **false** unchanged |

Measured defs (product Lean, no edit this slice):

- `LlvmCfgHonesty.lean` / `LlvmCfgFixture.lean`: `llvmCfgFixtureDominanceClaimed := false`; tip FullBackend `:= true`; `dominancePipelineRequiredForFullBackend := false`
- Band SSA modules Mult/Linear/Types/Program/Graph: each `*FullBackendClaimed := false`
- `LlvmCfgUnitDf.lean`: `multiUnitDominanceFrontierDeepen` join Mult..Graph true with `!llvmCfgFixtureDominanceClaimed`

## Commands + exits

| Command | Exit |
|---------|------|
| `just systems-llvm-ir` | **0** (186 required paths; multi-unit Mult..Graph DF + DominanceClaimed false in banner; tip fullBackend true A-light; band FullBackend false) |
| `just hygiene` | **0** (source-hygiene + professional-tone; 1185 files / 588 md; re-run after md touch; scrubbed banned long-file-split slang from U7 living prose) |

No lake claim flip (no Lean claim-bool change). Lake not required for inventory-only honesty.

## Out of scope (honored)

- DominanceClaimed true from multiUnit DF alone
- Band Mult..Graph FullBackend true from tip A-light alone
- free/complete/PROVABLY/FullHost flips
- Product Rust under `src/`
- HostModuleCheck package-env / TERM / Theorems (U7/U1/U2)
- FullHost meet (U3); emit-wire (U5); elaborator (U6)
- git commit

## Why hold (summary)

Multi-unit Mult..Graph DF Cytron dual-pin is already **true**. That is progressive unit-diamond
evidence, not a full dominance pipeline claim. Path B closed bar (2) without DominanceClaimed.
Band FullBackend would need A-heavy honesty redesign. Prefer documented hold over forge.
Same conclusion as R3/W4/T4/N4; U4 is completeness-wave XL reseed confirmation with an
explicit path-A checklist for any later operator-promoted flip residual.

## Next

Remaining Open (disjoint; leave to peers):

1. U7 Host module check Theorems long-file headroom (first priority)
2. U1 Host unit TERM expand (L0 real units)
3. U2 Package env seed expand (after U1 product)
4. U3 FullHost meet flip-or-partial (after U1/U2)
5. U5 Emit-wire unit-walk honesty
6. U6 Host elaborator measured deepen

Optional later A-heavy / XL path A: proved idom + arbitrary-graph DF + pure Nix invert + lake
for DominanceClaimed true, or band FullBackend honesty redesign (operator C1b; not auto Open).

## Status

**GREEN** -- honest inventory hold path B; DominanceClaimed confirmed **false**; band
FullBackend confirmed **false**; free/complete/PROVABLY/FullHost unchanged;
`just systems-llvm-ir` + `just hygiene` green; residual / WATCHER / bar / inventory /
consumer README lockstep.
