# Implement report: residual closeout after Package-env for ExtractSubsetEmit

**Date:** 2026-08-13
**Repo:** `/home/hunter/Projects/ai/iso`
**Kind:** residual / WATCHER / handoff / RESIDUAL.md lockstep only. No product Lean/C/Nix. No claim-bool flips.

ASCII only. Plain American English.

**Status:** **GREEN** (docs lockstep). `just hygiene` run after this report.

---

## Goal

Update living residual so the queue matches sibling evidence. Package-env for
ExtractSubsetEmit closed GREEN with reviews at 0 open. Mark that Name **done**.
Open next Name **Term-surface for MultSubsetEmit**. Full host package
elaborate meet stays **done partial** (inventory plus PE43..PE63; pin still
false). Do not flip `FullHostElaborateRemains`. Do not re-queue PE63 or
ExtractSubsetEmit TERM. Do not open cold compose init unroll. Do not invent
seed-wide 206, a FullHost flip, or a C product Name.

---

## Evidence trusted (not re-researched)

| Report | Outcome |
|--------|---------|
| `.agents/reports/impl-package-env-extract-subsetemit-2026-08-12.md` | GREEN. PE63 residual-free reject twin plus E-good S17 plus dialect S17. New `HostModuleCheckPackageEnvFixturesLater.lean` (93) because FixturesS5 was 989. FixturesS5 990. PackageEnv 926. Evidence 976 later-fold PE45..PE63. Soft J2 CompilePathExtract-only. Soft J5 Extract TERM dialect at package scope. Harness **111**. TERM **57**. Seed **206**. FullHostElaborateRemains false. |
| Review/mop (trashed; verdict on impl row) | GREEN / APPROVE 0. See `impl-package-env-extract-subsetemit-2026-08-12.md`. |

Spot-checked (no product Lean logic edit):

- Seed list after `SystemsLean.ExtractSubsetEmit` is `SystemsLean.MultSubsetEmit`
  (`HostModuleCheckSeeds.lean` and `HostImportGraphSeeds.lean`).
- There is no `HostModuleCheckMultSubsetEmitTerm.lean` (this Open).
- MultSubsetEmit already has the four-claim TERM twin shape:
  `multSubsetEmitResidualFreeClaimed := false`;
  `multSubsetEmitMultDialectOk := emitMultReady`;
  multi-line `multSubsetEmitReady`;
  `multSubsetEmitProductSelfHostCompleteClaimed := false`.
- Seed 206 unchanged. Harness **111**. TERM dual-ok **57**. Package-env now
  through PE63 / S17.
- Evidence **976** is under Sub-1-KLOC (later-fold helper PE45..PE63, no split).
- FixturesS5 **990**. Later-fixtures role module **93**. PackageEnv **926**.

---

## Why Term-surface for MultSubsetEmit is the honest next

The established FullHost meet path is TERM first, then the package-env twin,
walking the real host seed in order.

| Check | Result |
|-------|--------|
| Next seed after ExtractSubsetEmit | `SystemsLean.MultSubsetEmit` |
| Existing `HostModuleCheck*Term.lean` for that unit | **none** |
| Earlier SubsetEmit units (Linear through Extract) | TERM + package-env both landed |
| Seed-wide 206 / mathlib / FullHost flip | not this Open |
| Production LLVM SSA / layout / link deepen | in-scope (Q7) but not the next checkable L0 TERM unit |
| Cold compose init unroll | stays not open |
| C product-meaning Name | not invented |

This is not a re-queue of PE63 or ExtractSubsetEmit TERM.

---

## Paths touched

| Path | Change |
|------|--------|
| `RESIDUAL-systems.md` | Package-env for ExtractSubsetEmit **done**; Term-surface for MultSubsetEmit **open**; Full host meet stays **done partial** (now includes PE63); two-target table row 29 done / row 30 open; Done archive lockstep line; living tip + next-prompt |
| `RESIDUAL.md` | Join Open table + highest-value next + living tip same story |
| `WATCHER.md` | **IN PROGRESS** Term-surface for MultSubsetEmit. Fence `/implement --effort 2 Term-surface for MultSubsetEmit`. Did not re-queue PE63 or ExtractSubsetEmit TERM. Did not open cold compose init unroll. |
| `doc/SESSION-HANDOFF.md` | Living tip / Active product residual / Next / Remaining; table rows; WATCHER tip |
| `.agents/reports/impl-residual-closeout-package-env-extract-subsetemit-2026-08-12.md` | This report |
| `/tmp/grok-1000/grok-impl-summary-residual-closeout-pe-xse.md` | Short summary for the parent |

No product Lean. No emit C. No just modules. No claim-bool flips.

`src/systems/host-partial-inventory.md` DualResidual harness snapshot was not
rewritten. Living counts stay in residual / WATCHER / handoff.

---

## Queue after lockstep

| Name | Status | Role |
|------|--------|------|
| Compose SSA / llvm-as / richer layout / link smoke / LLVM timing | **done** | already done |
| Full host package elaborate meet | **done partial** | Inventory + PE43..PE63; pin still false; next named meet-path is MultSubsetEmit TERM |
| Package-env for CompilePath unit TERM | **done** | PE45..PE52 |
| Term-surface for CompilePath Plan Apply Body | **done** | |
| Package-env for CompilePath Plan Apply Body | **done** | PE53..PE55 |
| Term-surface for FirstSurface | **done** | |
| Package-env for FirstSurface | **done** | PE56 / S10 |
| Term-surface for LinearSubsetEmit | **done** | harness 105 at land; TERM 51 at land; pin false |
| Package-env for LinearSubsetEmit | **done** | PE57 / S11 |
| Term-surface for TypesSubsetEmit | **done** | harness 106 at land; TERM 52 at land |
| Package-env for TypesSubsetEmit | **done** | PE58 / S12 |
| Term-surface for ProgramSubsetEmit | **done** | harness 107 at land; TERM 53 at land |
| Package-env for ProgramSubsetEmit | **done** | PE59 / S13 |
| Term-surface for GraphSubsetEmit | **done** | harness 108; TERM 54 |
| Package-env for GraphSubsetEmit | **done** | PE60 / S14 |
| Term-surface for ComposeSubsetEmit | **done** | harness 109 at land; TERM 55 at land |
| Package-env for ComposeSubsetEmit | **done** | PE61 / S15 |
| Term-surface for ErasureSubsetEmit | **done** | harness 110; TERM 56; pin false |
| Package-env for ErasureSubsetEmit | **done** | PE62 / S16 |
| Term-surface for ExtractSubsetEmit | **done** | harness 111; TERM 57; pin false |
| Package-env for ExtractSubsetEmit | **done** | PE63 / S17; FixturesS5 990 / FixturesLater 93 / PackageEnv 926 / Evidence 976 |
| Term-surface for MultSubsetEmit | **open** | Highest-value next; clone ExtractSubsetEmit TERM; pin stays false |
| Package-env for MultSubsetEmit | **not this Open** | PE twin comes after this TERM |
| Cold compose init unroll | **not open** | operator notes stay not open |

Highest-value next: **Term-surface for MultSubsetEmit**.

Expected TERM land: harness **111 -> 112**; TERM dual-ok **57 -> 58**; LoadOk
later-fold stays **914** / under 1000; do **not** edit PackageEnv; residual-free
and complete claimed stay false; ready multi-line via
`multSubsetEmitReadyTermBody` (product fold is `multSubsetEmitReady`).

---

## Next Name contract (living)

Name: Term-surface for MultSubsetEmit

Goal: Add accept plus reject term-surface twins for MultSubsetEmit now that
ExtractSubsetEmit has both TERM and package-env twins.

Done when (checkable):

- New `HostModuleCheckMultSubsetEmitTerm` companion cloned from
  `HostModuleCheckExtractSubsetEmitTerm`.
- LoadOk later-fold folds MultSubsetEmit SurfaceOk. LoadOk stays **914** /
  under 1000.
- RealModule `refineMultSubsetEmitWithTermSurface`.
- Accepts uses TERM text not L0 FixtureTextsLater stub.
- Four ILL twins: residual-free claimed true
  (`multSubsetEmitResidualFreeClaimed`); emit body true
  (`multSubsetEmitMultDialectOk` / living `emitMultReady`); ready body
  true (`multSubsetEmitReady`); complete claimed true
  (`multSubsetEmitProductSelfHostCompleteClaimed`).
- L0 accept + dialect reject (`ILL-TYPED-TERM`).
- E-good emit twins living `emitMultReady`.
- Residual-free and complete claimed stay false.
- Ready multi-line via `multSubsetEmitReadyTermBody` (same collapse as
  Extract; product fold is `multSubsetEmitReady`).
- Soft J2 MultSubsetEmit-only.
- Harness **111 -> 112**. TERM dual-ok **57 -> 58**.
- FullHostElaborateRemains stays **false**.
- Do **not** edit PackageEnv in this slice.
- `just hygiene` plus focused lake family plus `just systems-host` green.

Out of scope: FullHost flip; seed-wide 206 / mathlib; DominanceClaimed / band
FullBackend; product C; product Rust; Package-env for MultSubsetEmit (PE
twin comes after this TERM); rewrite of ExtractSubsetEmit TERM; re-queue PE63;
cold compose init unroll; inventing a C fail-closed residual.

Paths: new `HostModuleCheckMultSubsetEmitTerm.lean`; LoadOk later-fold;
RealModule; Accepts; HostModuleCheck; SystemsLean; HostResidualShrink family.
Do not grow past Sub-1-KLOC.

---

## Pin proof (FullHost stays false)

This lockstep did not edit `hostResidualShrinkFullHostElaborateRemains`.
No FULLHOST_FLIP. No free / complete / PROVABLY / DominanceClaimed / band
FullBackend edits. ExtractSubsetEmit residual-free and complete claimed stay
false.

---

## Pins unchanged (must not forge)

| Pin | Status |
|-----|--------|
| free / complete / PROVABLY | **true** (do not re-open) |
| FullHostElaborateRemains | **false** |
| DominanceClaimed / band FullBackend | **false** |
| irNodeLayoutFullBackendClaimed / rustIrInteropFullBackendClaimed | **false** (untouched) |
| seed / harness / TERM dual-ok | 206 / 111 / 57 |
| package-env | Mult..ParityEmit + U2 + S5 plus PE43/PE44 plus PE45..PE63 (PE63 / S17 landed; scope string still SurfaceMatrixEnv-only; not seed-wide) |
| PackageEnv line counts | FixturesS5 990; FixturesLater 93; PackageEnv 926; Evidence 976 (all under 1000) |
| ExtractSubsetEmit residual-free / complete claimed | **false** |
| systems-emit-wire | green |
| FULLHOST_FLIP | no |
| Speed | empirical (`just bench-runtime`); never rename as PROVABLY |

No Lean claim-bool edits.

---

## Out of scope (honored)

- FullHostElaborateRemains / FULLHOST_FLIP
- Seed-wide 206 / mathlib / full Lean 4 elaborator parity
- DominanceClaimed / band FullBackend
- Product C; product Rust under src/
- Re-queue PE63 or ExtractSubsetEmit TERM
- Opening Package-env for MultSubsetEmit now
- Opening an Evidence or Fixtures long-file split Name now
- Invented C fail-closed residual
- Cold compose init unroll
- Presenting a new plan
- New product Lean

---

## Hygiene

`just hygiene`: **exit 0** (1440 files; 765 markdown). source-hygiene OK;
professional-tone OK. Prior Package-env for ExtractSubsetEmit mop hygiene was
1436 files / 761 markdown. Extra novel markdown is this closeout plus
residual/handoff already in tree. `/tmp` summary is outside the tree.

---

## Next implement

WATCHER fence:

```
/implement --effort 2 Term-surface for MultSubsetEmit
```

Do not implement that product Lean in this lockstep.
