# Plan: remaining residual, smallest first (2026-08-11)

Kind: read-only inventory for agent-doable work. **Not residual open.**
**Not** a FullHost / DominanceClaimed / band FullBackend flip.
ASCII only. Plain American English.

Authority (living tip wins):
- `RESIDUAL-systems.md` Open: **empty done-for-now** (U1-U7 closed)
- `WATCHER.md`: **DONE-FOR-NOW**
- `RESIDUAL.md` Systems Open join + Host finish residual
- `doc/SESSION-HANDOFF.md` living tip
- U1 report: `.agents/reports/impl-unit-term-u1-2026-08-11.md`
- U2 report: `.agents/reports/impl-package-env-u2-2026-08-11.md`
- U3 FullHost: `.agents/reports/impl-fullhost-meet-u3-2026-08-11.md`
- U4 LLVM XL: `.agents/reports/impl-llvm-xl-u4-2026-08-11.md`
- Meet inventory: `doc/dev/research/fullhost-meet-inventory-2026-08-11.md`
- Final closeout: `.agents/reports/impl-final-u1u7-closeout-2026-08-11.md`

**Pin floor (must not forge without bar + lake):**

| Pin | Living tip |
|-----|------------|
| free / complete / PROVABLY | **true** |
| FullHostElaborateRemains | **false** |
| DominanceClaimed | **false** |
| band Mult..Graph `*FullBackendClaimed` | **false** |
| tip fullBackend (A-light) | **true** |
| multiUnit Mult..Graph DF deepen | **true** (not DominanceClaimed) |
| seed / harness | **206** / **78** |
| TERM dual-ok | **32** |
| package-env | Mult..ParityEmit + U2 Emit*Scaffold x9 + KernelSelfApply + ProductOutKernel |

**Policy honesty:** product Open is empty on purpose. Residual and WATCHER ban
inventing seed-wide H2 or FullHost true Open Names. Items below are **optional
promote-later checkable slices** (or standing hygiene), not a license to auto-open
FullHost flip or forge pins. Prefer operator promote before reopening product Open.

---

## 1. L0 host modules without TERM dual-ok

### Counts (U1 inventory, post-U1 living tip)

| Band | Count (approx) | Notes |
|------|----------------|-------|
| Develop seed modules | **206** | Host library product seed |
| Non-Theorems L0 (approx) | **142** | U1 inventory |
| TERM dual-ok | **32** | Mult..ParityEmit (21) + U1 eleven (11) |
| L0 still without TERM | **~110** | optional later; not invent tool-surface theater |

### TERM dual-ok set (32, living)

Mult, Linear, Types, IrProgram, IrGraph, HostCompose, Erasure, Extract,
EmitPlan, EmitApply, EmitBody, KernelMult, KernelLinear, KernelTypes,
KernelProgram, KernelEmit, ParityMult, ParityLinear, ParityTypes, ParityProgram,
ParityEmit, EmitMultScaffold, EmitLinearScaffold, EmitTypesScaffold,
EmitProgramScaffold, EmitGraphScaffold, EmitComposeScaffold, EmitErasureScaffold,
EmitExtractScaffold, EmitBannerScaffold, KernelSelfApply, ProductOutKernel.

All 32 also sit in multi-module package-env (U2 PE28..PE38 for the U1 eleven).

### Concrete real L0 candidates (product shape; not invent)

Best next TERM targets if Open is reopened for optional completeness (U1 class):

| # | Module | Why real / checkable | Difficulty |
|---|--------|----------------------|------------|
| 1 | `CompilePath` | Host compile-path readiness SSoT (`SLAKE_COMPILE_PATH_V1`); has theorems companion; clear dialect body | M |
| 2 | `JoinMap` | Dual-to-Slake join map; JOIN-ALG use pins; theorems companion | M |
| 3 | `SelfHost` | Self-host **direction** readiness (not claim B flip); unit/program bars | M |
| 4 | `SurfaceMatrix` | Superset surface inventory rows; measured full-elaborator row honesty | M |
| 5 | `SelfApply` | Host structural self-apply (kernel rebuilds kernel); distinct from KernelSelfApply TERM | M |
| 6 | `CompilePathMult` (then Linear/Types/Program/Graph/Compose/Erasure/Extract peels) | Real unit compile-path fixtures; product shape; series after core CompilePath | M each |
| 7 | `SpecProof` | Formal feedback bar surface (proofCompleteClaimed already true elsewhere; TERM is dialect check only) | S-M |
| 8 | `DualResidual` | Dual residual honesty module; free claimed dual-ok lives nearby | S-M |

**Skip / low value unless Named by operator (U1 skip table spirit):**

| Class | Examples | Why skip by default |
|-------|----------|---------------------|
| Product-path / capable claim cluster | ProductPath*, Capable*, Official*, DualEq*, Perform*, Ownership* | Claim-path ladder, not first-class unit dialect |
| Emit product shells | EmitMult, EmitLinear, ... | Dialect authority already on *Scaffold TERM |
| Plan/Apply/Body scaffolds | EmitPlanScaffold, ... | Product EmitPlan/Apply/Body already TERM |
| Subset emit/rebuild / front tools | *SubsetEmit, HostFront, HostCheck, Llvm*Text | Tool surface; low TERM value |
| Freestanding load glue | FreestandingEmitLoadScaffold | Load glue |

**Estimate:** of ~110 L0-without-TERM, about **4 core join/path + ~10 CompilePath peels + a few honesty modules** are U1-class "real dialect" candidates (~15-20). The rest are product-path, tools, or shells.

---

## 2. Sub-1-KLOC near / at bar (SystemsLean)

No HostModuleCheck / HostImportGraph file is **over** 1000 after R5 + U7 + U2.
Near-limit files need **headroom before growth** (standing residual, not open oversize debt).

### Measured tip (disk end-of-file / recent reports)

| File | ~LOC | Headroom |
|------|------|----------|
| `HostImportGraphWalk.lean` | **1000** | at bar |
| `HostFront.lean` | **1000** | at bar |
| `SelfApplyFs.lean` | **1000** | at bar (prior long-file campaign tip) |
| `HostModuleCheckFixtureTexts.lean` | **993** | thin |
| `HostImportGraphLoadOk.lean` | **996** | thin |
| `HostModuleCheckAcceptsGoods.lean` | **991** | thin |
| `HostResidualShrink.lean` | **982** | thin (grew with dualOk/harness strings) |
| `HostModuleCheckDriver.lean` | **973** | modest |
| `HostGraph.lean` | **939** | ok |
| `HostModuleCheckTheorems.lean` | **668** | good (U7 -> Smoke 383) |
| `HostModuleCheckPackageEnv.lean` | **611** | good (U2 split) |
| `HostModuleCheckPackageEnvEvidence.lean` | **651** | good |
| `HostModuleCheckPackageEnvFixtures.lean` | **802** | ok; watch growth |
| `HostModuleCheckPackageEnvFixturesU2.lean` | **391** | good |
| `HostPackageWrite.lean` | **784** | good |

**Rule:** before adding more dualOk greps, PE twins, or TERM smoke folds into a
near-limit file, extract a role companion first (same pattern as U7 / R5 / U2).

---

## 3. FullHost H1-H8 (U3 tip; FULLHOST_FLIP=no)

Bar SSoT: `doc/dev/research/c2-fullhost-bar-criteria-2026-08-10.md`.
Inventory: `doc/dev/research/fullhost-meet-inventory-2026-08-11.md`.

| # | Criterion | Status | Smallest honest next (no flip) |
|---|-----------|--------|--------------------------------|
| **H1** | Written bar authority | **met** | Keep SSoT; cite on any future flip residual |
| **H2** | Named package surface under **real** accept/reject (not PARTIAL-STRUCTURAL greps alone); seed-wide or bar-named flip surface | **partial** | Optional further pilot TERM+env on real L0 (CompilePath..) only grows pilots; **does not** meet H2 for flip. Seed-wide 206 real judgments is **L-XL** and promote-later only |
| **H3** | Same-job honesty: Slake owns package elaborate day-to-day for the named flip surface | **partial** | Design note only until a bar-named surface lands; free-bar without-Lake tools green is not H3 met |
| **H4** | Free bar (a) green; free claimed; FullHost false honest | **met** | Keep free dual-ok intact |
| **H5** | Product claim surface + **lake** proof for FullHost done | **unmet** | Blocked on H2/H3; **do not open flip residual** |
| **H6** | Orthogonal pins unchanged | **met** | free/complete/PROVABLY true; Dominance/band FullBackend false |
| **H7** | Docs lockstep residual / plan / WATCHER / handoff | **met** (U3) | Re-lockstep only if product work reopens |
| **H8** | Hygiene + Sub-1-KLOC + short report | **met** (U3) | Standing on every slice |

**Verdict:** FullHostElaborateRemains stays **false**. More pilot bands alone are
not expected to flip. Smallest FullHost-related agent work without flip: optional
real L0 TERM (H2 pilot growth) **or** inventory reseed after such growth with
explicit **FULLHOST_FLIP=no**.

---

## 4. LLVM / Dominance (smallest deepen without pin flip)

Living:

- `multiUnitDominanceFrontierDeepen` **true** (Mult..Graph Cytron dual-pin)
- `llvmCfgFixtureDominanceClaimed` **false**
- band `*FullBackendClaimed` **false**
- tip `llvmCfgFixtureFullBackendClaimed` **true** (A-light)
- `dominancePipelineRequiredForFullBackend` **false** (path B)
- U4 XL path B **done** (honest hold)

### What XL path A still needs (not agent default)

| Requirement | Status |
|-------------|--------|
| Written XL bar for DominanceClaimed and/or band FullBackend | held optional |
| Proved dominator-tree constructor | **missing** |
| Arbitrary-graph DF export beyond unit diamonds | **missing** |
| Pure Nix forge-ban invert | **not inverted** |
| Lake elaborator proof after flip | n/a until flip |

### Smallest agent-doable LLVM work **without** DominanceClaimed / band FullBackend true

| Option | Size | Notes |
|--------|------|-------|
| Honesty reseed only (bar + inventory + consumer README lockstep) | S | Already done U4/R3/W4/T4; re-do only if tips drift |
| Structural fixture deepen that keeps pins false | M | New partial CFG/SSA evidence with forge-bans still requiring false; low product value after progressive path B done |
| Path A algorithm (proved idom + arbitrary DF + ban invert + lake) | XL | Operator promote C1b only; not auto Open |

**Honest default:** no auto Open for Dominance / band FullBackend. Progressive LLVM
residual is **closed** under path B.

---

## 5. Dual / CHI / package-env / emit-wire / hygiene

| Surface | Status | Green-to-improve? |
|---------|--------|-------------------|
| `just hygiene` | standing green | On-touch scrub only; no open mill |
| `just systems-host` | green | Presence grows only when new modules land |
| `just systems-emit-wire` | green (unit-surface **307**) | U5 done; no open emit-wire residual |
| `just systems-llvm-ir` | green | Mult..Graph DF + Dominance false in banner |
| `just host-residual-shrink` | green | FullHost false; free dual-ok |
| `just host-module-check-without-lake` | green | seed 206; package-env Mult..ParityEmit+U2 |
| CHI Idris-Lean + Lean-Rust | **done** (T0; report chi-readiness) | Formal dual map still **present-partial** (two edges); further dual pairs held (operator promote; not freestanding affine ABI) |
| package-env Mult..ParityEmit+U2 | **done** | Further env only after new TERM; Sub-1-KLOC on PackageEnv* |
| Dual JOIN-ALG six pairs | **done** | AffineDrop + AffineOwn duals documented; not invent more duals as residual mill |
| Runtime / compile benches | measurement done | Re-run when speed story changes; not pin work |

No mandatory green-to-improve product residual while Open is empty. Standing gates
are re-run on touch.

---

## 6. Smallest-first implementable Names (optional; promote-later)

**Context:** Living Open is empty. These Names are **checkable** if reopened, ordered
smallest / highest honesty first. Difficulty: **S** small, **M** medium, **L** large.
None may flip free/complete/PROVABLY true, or FullHost / DominanceClaimed / band
FullBackend true, unless Done when explicitly includes bar + lake (and residual
policy allows that Name).

| Rank | Name | Diff | Done when (checkable) | Out of scope |
|-----:|------|------|------------------------|--------------|
| 1 | Host near-bar long-file headroom | S | Extract role companion(s) so at least one of Walk / Front / SelfApplyFs / FixtureTexts / LoadOk / AcceptsGoods / ResidualShrink sits under ~900; lakefile + systems-host lockstep; hygiene green; pins unchanged | Claim flips; invent TERM |
| 2 | Host unit TERM CompilePath | M | `HostModuleCheckCompilePathTerm` (E-good + ILL twins + dualOk + RealModule + LoadOk + harness); TERM dual-ok **33**; systems-host + host-module-check-without-lake + hygiene green; free/complete/PROVABLY true; FullHost false | Invent units; FullHost flip |
| 3 | Host unit TERM JoinMap | M | Same Term pattern for `JoinMap`; dual-ok +1; gates green; pins unchanged | FullHost flip |
| 4 | Host unit TERM SelfHost | M | Same for `SelfHost` direction module; pins unchanged | Claim B re-open; freestanding complete flip |
| 5 | Host unit TERM SurfaceMatrix | M | Same for `SurfaceMatrix`; pins unchanged | Mathlib / full elaborator parity claim |
| 6 | Package env after new TERM batch | M | PE twins for each new Term unit; PackageEnv* under 1000 (split if needed); harness partition honest; packageEnv gate green; FullHost false | Seed-wide H2 claim; FullHost flip |
| 7 | Host unit TERM CompilePath unit modules | M-L | TERM for CompilePathMult..Extract (series); dual-ok climb; Sub-1-KLOC care on Accepts/Fixtures | Invent unit modules that do not exist |
| 8 | FullHost meet reseed (partial) | S | Inventory H1-H8 reseed after any pilot growth; **FULLHOST_FLIP=no** unless bar met; residual lockstep; no pin edit | FullHostElaborateRemains true without H1-H8 + lake |
| 9 | Host unit TERM SelfApply | M | Term companion for structural SelfApply; dual-ok +1; pins unchanged | Confuse with KernelSelfApply (already TERM) |
| 10 | Dual formal map edge deepen | L | One more **real** formal dual edge with dual-side sources + gates; formal map remains honest partial; free/complete/PROVABLY unchanged | Invent dual algorithms; freestanding affine ABI |
| 11 | Dominance / band FullBackend XL path A | XL | Written XL bar + proved idom + arbitrary-graph DF + pure Nix invert + lake; only then pin true | Forge from multiUnit DF alone |
| 12 | FullHost true flip residual | XL | Bar H1-H8 all met for a **named** flip surface (seed-wide or explicit) + short claim pin + **lake** elaborator proof + free dual-ok redesign if polarity changes | Grep-only flip; pilot package-env alone |

### Not on the list (held / non-goal)

- Package 3 / **C4** mathlib day-one (**non-goal**)
- **C5** host UX (out of this repo)
- Seed-wide H2 theater without real judgments
- Restoring shell/Python mills or hand-written product C
- Auto-open after DONE-FOR-NOW without operator promote

---

## 7. Recommendation

1. **Default posture:** keep product Open **empty done-for-now**. Standing:
   Sub-1-KLOC on touch; `just hygiene` / systems-host / emit-wire / llvm-ir when
   editing those surfaces.
2. **If operator wants more completeness without pin forge:** rank **1** then
   **2-5** (core join/path TERM), then **6** (env), then **8** (FullHost reseed
   FULLHOST_FLIP=no). That is the smallest honest completeness loop.
3. **Do not** start rank **11** or **12** without explicit operator promote and
   written bar + lake in Done when.
4. **Pins stay:** free/complete/PROVABLY **true**; FullHostElaborateRemains **false**;
   DominanceClaimed **false**; band FullBackend **false**.

---

## Sources (evidence)

| Path | Used for |
|------|----------|
| `RESIDUAL-systems.md`, `RESIDUAL.md`, `WATCHER.md`, `doc/SESSION-HANDOFF.md` | Living Open empty; pin floor |
| `.agents/reports/impl-unit-term-u1-2026-08-11.md` | TERM 32; ~110 L0; candidate classes |
| `.agents/reports/impl-package-env-u2-2026-08-11.md` | PE28..PE38; harness 78; PackageEnv split |
| `doc/dev/research/fullhost-meet-inventory-2026-08-11.md` | H1-H8 U3 tip |
| `.agents/reports/impl-fullhost-meet-u3-2026-08-11.md` | FULLHOST_FLIP=no |
| `.agents/reports/impl-llvm-xl-u4-2026-08-11.md` | Dominance / band FullBackend hold |
| `doc/dev/research/llvm-backend-deepen-inventory-2026-08-08.md` | LLVM progressive closed; XL held |
| `.agents/reports/impl-long-file-split-r5-2026-08-11.md` | Near-bar LOC table pre-U1 |
| `.agents/reports/impl-theorems-headroom-u7-2026-08-11.md` | Theorems 668 / Smoke 383 |
| `.agents/reports/impl-final-u1u7-closeout-2026-08-11.md` | Promote-later list |
| Disk EOF samples under `src/systems/SystemsLean/` | Living LOC tips (Walk/Front/SelfApplyFs/ResidualShrink/PackageEnv*) |

No product files edited. Report only.
