# Implement report: residual closeout after Term-surface for ComposeSubsetEmit

**Date:** 2026-08-12
**Repo:** `/home/hunter/Projects/ai/iso`
**Kind:** residual / WATCHER / handoff / RESIDUAL.md lockstep only. No product
Lean, theorems, or product C. No claim-bool flips.

ASCII only. Plain American English.

**Status:** **GREEN** (docs lockstep). This lockstep writer has no shell tool,
so `just hygiene` was not run here. Parent or process mop should run it.

---

## Goal

Update living residual so the queue matches sibling evidence. Term-surface for
ComposeSubsetEmit closed GREEN with reviews at 0 open. Mark that Name **done**.
Open next Name **Package-env for ComposeSubsetEmit**. Full host package
elaborate meet stays **done partial**. Do not flip `FullHostElaborateRemains`.
Do not re-queue ComposeSubsetEmit typed term-surface. Do not open cold compose
init unroll. Do not invent seed-wide 206 or a FullHost flip.

---

## Evidence trusted (not re-researched)

| Report | Outcome |
|--------|---------|
| `.agents/reports/impl-composesubsetemit-term-2026-08-12.md` | GREEN. New `HostModuleCheckComposeSubsetEmitTerm.lean` 271. Harness 108 -> 109. TERM dual-ok 54 -> 55. LoadOk 914. PackageEnv not edited. |
| `.agents/reports/process-mop-composesubsetemit-term-2026-08-12.md` | GREEN. No product edits. Hygiene 1408 files / 736 markdown. Lake 97 jobs. systems-host 326 paths. |
| `.agents/reports/review-general-composesubsetemit-term-2026-08-12.md` | APPROVE 0 open |
| `.agents/reports/review-tests-composesubsetemit-term-2026-08-12.md` | APPROVE 0 open |

Spot-checked (no product Lean logic edit):

- Seed string after ComposeSubsetEmit is `SystemsLean.ErasureSubsetEmit` in
  `HostModuleCheckSeeds.lean` (ideal ladder Erasure subset emit).
- After a TERM land, the next checkable slice is still the package-env twin
  (every TERM unit gets a PE twin before the next TERM).
- Did **not** open Term-surface for ErasureSubsetEmit.
- Seed string stays 206 (living partition seed=206+Main=59+harness=109).
- Living counts: harness **109**, TERM dual-ok **55**, LoadOk **914**.
- Pin `hostResidualShrinkFullHostElaborateRemains` stays **false**.
- Evidence is **981** (from PE60). This PE twin (expected PE61 / S15) may
  need an Evidence long-file split if adding the twin would exceed 1000.
  That warning lives on the open PE Name Done when / out of scope. A split
  Name is **not** open now. Prefer the PE Name as highest-value next.

---

## Paths touched

| Path | Change |
|------|--------|
| `RESIDUAL-systems.md` | Term-surface for ComposeSubsetEmit **done**; Package-env for ComposeSubsetEmit **open**; Full host meet stays **done partial**; living tip + highest-value next + Done archive + next-prompt |
| `RESIDUAL.md` | Join Open table + highest-value next + living tip same story |
| `WATCHER.md` | **IN PROGRESS** Package-env for ComposeSubsetEmit. Fence `/implement --effort 2 Package-env for ComposeSubsetEmit`. Did not re-queue ComposeSubsetEmit TERM. Did not open cold compose init unroll. Did not open Term-surface for ErasureSubsetEmit. |
| `doc/SESSION-HANDOFF.md` | Living tip / Active product residual / Next; table rows |
| `.agents/reports/impl-residual-closeout-composesubsetemit-term-2026-08-12.md` | This report |
| `/tmp/grok-1000/grok-impl-summary-residual-closeout-cse-term.md` | Short summary |

No emit C. No just modules. No Nix. No claim-bool flips. No product Lean
comment edits.

`src/systems/host-partial-inventory.md` DualResidual harness=42 is a dated
snapshot. Did not rewrite it. Living counts stay in residual / WATCHER /
handoff.

---

## Queue after lockstep

| Name | Status | Role |
|------|--------|------|
| Compose SSA / llvm-as / richer layout / link smoke / LLVM timing | **done** | already done |
| Full host package elaborate meet | **done partial** | Inventory + PE43..PE60; ComposeSubsetEmit TERM landed; pin still false |
| Package-env for CompilePath unit TERM | **done** | PE45..PE52 |
| Term-surface for CompilePath Plan Apply Body | **done** | |
| Package-env for CompilePath Plan Apply Body | **done** | PE53..PE55 |
| Term-surface for FirstSurface | **done** | |
| Package-env for FirstSurface | **done** | PE56 / S10 |
| Term-surface for LinearSubsetEmit | **done** | harness 105 at land; TERM 51 at land; pin false |
| Package-env for LinearSubsetEmit | **done** | PE57 / S11; harness 105 at land; TERM 51 at land |
| Term-surface for TypesSubsetEmit | **done** | harness 106 at land; TERM 52 at land; pin false |
| Package-env for TypesSubsetEmit | **done** | PE58 / S12; harness 106 at land; TERM 52 at land |
| Term-surface for ProgramSubsetEmit | **done** | harness 107 at land; TERM 53 at land; pin false |
| Package-env for ProgramSubsetEmit | **done** | PE59 / S13; harness 107 at land; TERM 53 at land |
| Term-surface for GraphSubsetEmit | **done** | harness 108; TERM 54; pin false |
| Package-env for GraphSubsetEmit | **done** | PE60 / S14; harness 108; TERM 54; pin false |
| Term-surface for ComposeSubsetEmit | **done** | harness 109; TERM 55; pin false |
| Package-env for ComposeSubsetEmit | **open** | Highest-value next; PE twin; PE61 / S15 not landed; pin stays false |
| Cold compose init unroll | **not open** | operator notes stay not open |

Highest-value next: **Package-env for ComposeSubsetEmit**.

Why this Name: ComposeSubsetEmit now has TERM dual-ok. The PE60 GraphSubsetEmit
pattern is the next checkable meet-path twin (expected PE61 / S15). The next
real seed after ComposeSubsetEmit is ErasureSubsetEmit, which still lacks TERM.
That TERM waits until this PE twin lands. This is not seed-wide 206. This is
not a FullHost flip. This is not a re-queue of ComposeSubsetEmit TERM.

Expected PE land: harness stays 109; TERM stays 55; FixturesS5 / PackageEnv /
Evidence surgical; E-good S15 = S14 ++ ComposeSubsetEmit multi-line ready;
dialect S15 after S14; Soft J2 CompilePathCompose-only (no EmitCompose farm);
Soft J5 ComposeSubsetEmit TERM dialect at package scope; residual-free claimed
true ILL; L0 accepts. If Evidence would exceed 1000, split Evidence first in
the same PE slice (or a one-Name split immediately before PE) so the PE land
stays under Sub-1-KLOC.

---

## Pin proof (FullHost stays false)

`src/systems/SystemsLean/HostResidualShrink.lean`:

`def hostResidualShrinkFullHostElaborateRemains : Bool := false`

This lockstep did not edit that pin. No FULLHOST_FLIP. No free / complete /
PROVABLY / DominanceClaimed / band FullBackend edits.

---

## Pins unchanged (must not forge)

| Pin | Status |
|-----|--------|
| free / complete / PROVABLY | **true** (do not re-open) |
| FullHostElaborateRemains | **false** |
| DominanceClaimed / band FullBackend | **false** |
| seed / harness / TERM dual-ok | 206 / 109 / 55 |
| package-env | Mult..ParityEmit + U2 + S5 plus PE43/PE44 plus PE45..PE60 (PE61 not landed; scope string still SurfaceMatrixEnv-only; not seed-wide) |
| PackageEnv line counts | FixturesS5 862; PackageEnv 871; Evidence 981 (all under 1000; Evidence near the bar) |
| systems-emit-wire | green |
| FULLHOST_FLIP | no |
| Speed | empirical (`just bench-runtime`); never rename as PROVABLY |

No Lean claim-bool edits.

---

## Out of scope (honored)

- FullHostElaborateRemains / FULLHOST_FLIP
- Seed-wide 206 / mathlib / full Lean 4 elaborator parity
- DominanceClaimed / band FullBackend
- Product C; product Rust under src/
- Rewrite of HostModuleCheckComposeSubsetEmitTerm
- Invented C fail-closed residual
- Cold compose init unroll
- Re-queue ComposeSubsetEmit TERM
- Opening Term-surface for ErasureSubsetEmit before the PE twin
- Opening an Evidence long-file split Name now (warn only)
- Presenting a new plan

---

## Hygiene

`just hygiene`: this lockstep writer has no shell tool, so the live Nix
eval was not run here. Spot-check of touched markdown (residual, WATCHER,
handoff, this report): ASCII only; no trailing whitespace; no professional-tone
banned tokens. Parent or process mop should run `just hygiene` and record
the file count. Expected: exit 0 (prior ComposeSubsetEmit TERM mop was 1408
files / 736 markdown; this slice adds this closeout report).

---

## Next implement prompt (same as WATCHER fence)

/implement --effort 2 Package-env for ComposeSubsetEmit

Name: Package-env for ComposeSubsetEmit
Goal: Add package-env fixture twins for ComposeSubsetEmit now that the unit has TERM dual-ok.
Done when: PE twin for ComposeSubsetEmit residual-free claimed true is ILL-TYPED-TERM; L0 still accepts. E-good package-env text grows S14 good ++ ComposeSubsetEmit using the real multi-line composeSubsetEmitReady body (hostModuleCheckGoodPackageEnvS15Text). Dialect checkPackageEnvDialectS15 (or next unused S-number after S14) after S14 GraphSubsetEmit succeeds. Soft J2 CompilePathCompose-only (no EmitCompose farm). Soft J5 ComposeSubsetEmit TERM dialect at package scope. No new harness module (count stays 109). TERM dual-ok stays 55. FullHostElaborateRemains stays false. just hygiene plus focused lake on PackageEnv family plus just systems-host green. Evidence is 981; if adding the twin would exceed 1000, split Evidence first in the same PE slice (or a one-Name split immediately before PE) so the PE land stays under Sub-1-KLOC.
Out of scope: FullHost flip / FULLHOST_FLIP; seed-wide 206 / mathlib; DominanceClaimed / band FullBackend; product C; product Rust under src/; rewrite of HostModuleCheckComposeSubsetEmitTerm; re-queue ComposeSubsetEmit TERM; next TERM unit (ErasureSubsetEmit); cold compose init unroll; inventing a C fail-closed residual; opening Term-surface for ErasureSubsetEmit before this PE twin.
Paths: src/systems/SystemsLean/HostModuleCheckPackageEnvFixturesS5.lean; HostModuleCheckPackageEnv.lean; HostModuleCheckPackageEnvEvidence.lean. Do not grow past Sub-1-KLOC; split Evidence if needed.
Gates: just hygiene; focused lake build of PackageEnv family; just systems-host. FULLHOST_FLIP=no.
