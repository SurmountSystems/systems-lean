# Plan: Close the six open claims (honest product path)

ASCII only. **Historical product-program snapshot** for claims A-F. Living tip
and living Open are **not** governed by present-tense sentences below that still
say complete false or Open Name freestanding product self-host.

**Living authority (2026-07-31):** freestanding product self-host **complete true**;
product residual free **false**; Lake host still used; living Open = **Residual
free bar inventory**. Detail: section 0 below, `WATCHER.md`,
`RESIDUAL-systems.md`, `.agents/plans/plan-residual-free-freestanding.md` (living
product program Steps 0-3), claim-bool SSoT in `SelfApplyFs.lean` /
`SelfHostComplete.lean`. When this intro fights section 0 or residual, **living
tip wins**.

Durable history of Systems Lean / Slake after the host theorem mill and
**closed** host-owned emit Mult..Out (wire stage Body / `EMIT_BODY_V0`) plus
**closed** Track 2 unit e2e Mult..Out. Snapshot originally refreshed
**2026-07-30** (B40 OWNERSHIP-CLAIMED; later Full + claim B complete closed).
Living authority when this file lags: `WATCHER.md`, `RESIDUAL-systems.md`,
`SelfApplyFs.lean` claim-bool SSoT, `self-host.md`,
`plan-residual-free-freestanding.md`.

**Audience:** coordinator + Systems / Slake fork (primary); Idris and Lean dual
forks for dual track only.

**Related plans (do not discard):**

| Plan | Role |
|------|------|
| `plan-residual-free-freestanding.md` | **Living product program** (residual free + linear freestanding) |
| `plan-close-six-claims.md` (this file) | **Historical** six-claims program; section 0 living tip wins |
| `plan-unambiguous-residual-work.md` | Residual schema (plain Names, short loops) -- still valid process |
| `plan-remaining-work.md` | Older snapshot / historical; **not** living tip |
| `plan-paydown-shell-c-surfaces.md` | Shell/C paydown (largely met) |

**North star (unchanged):** Systems Lean compiles itself to **runtimeless
freestanding C** under `out/freestanding-c` with no Lean managed runtime and no
product garbage collection (GC) on the product wire. Slake is that compiler,
written in Systems Lean, consummating an Idris/Lean meet-in-the-middle map.

**What this plan is not:** another readiness-canary or theorem-mill residual.
Host algebraic leftovers under the current partial surface are **done-for-now**.
Track 1 host-owned emit Mult..Out and Track 2 unit e2e Mult..Out are **closed**.
Dual SSOT equality gate is **done**. Track 4 claim **A** residual free
**measurement** is **closed** (free claim still false). Claim B ladder through
B40 + Full is **history** (living tip: complete **true**; do not re-open).
Living Open product residual is **Residual free bar inventory** (not freestanding
product self-host). Parallel hygiene long-file / short-name work is **parked**,
not the product goal.

**Naming (prose only; no ABI churn):** residual short name for the last Mult..*
emit stage is **Out** (maps to frozen product wire `EMIT_BODY_V0` / host
`EmitBody` / HOST-EMIT-BODY). **SelfHostBody** in prose is the **defined
freestanding compile step** / **self-host step readiness** pin -- not claim B
complete. Detail: `emit/host-owned-emit.md`, `doc/vocabulary.md`.

**Agent tooling (project skill):** host Lean work on this path uses the project
**lean4** skill pack (`skills/lean4-skills`, discovered at `.agents/skills/lean4`).
Skill workflows accelerate Lean draft/prove/review/doctor; they do **not**
replace residual Names, freestanding emit ownership, pure Nix gates, or honesty
bars. Policy SSoT: root `AGENTS.md` section **Project agent skills**.

---

## 0. Living tip and honesty (2026-07-30 reseed)

**Living tip wins (2026-07-31 pointer):** freestanding product self-host
**complete is true**; product residual free is **false**; Lake host still used;
long-file campaign is parked hygiene. This section still has historical snapshot
rows below that say complete false / Full false -- treat those as land-time
history, not living tip. Living product program:
`.agents/plans/plan-residual-free-freestanding.md`. Authority:
`SelfApplyFs.lean` / `SelfHostComplete.lean` claim-bool SSoT, `WATCHER.md`,
`RESIDUAL-systems.md`. Older product modules may keep **land-time** local false
pins; do not misread them as living tip.

### Living claim-bool tip (SelfApplyFs)

| Bool | Value | Note |
|------|-------|------|
| `productPathFreestandingPerformClaimed` | **true** | B39; official dual-eq WRITE evidence |
| `productPathOfficialPathStillUsesFreestandingEmit` | **false** | B38 retire official path |
| `productPathFreestandingOwnershipClaimed` | **true** | B40; perform-claimed + official dual-eq WRITE evidence |
| Full step-contract satisfied (`stepContractFull`) | **true** | living tip; Full bar met with ownership + perform + dual-eq WRITE |
| `freestandingProductSelfHostComplete` | **true** | claim B complete closed (2026-07-30+) |
| `productPathPerformDependsOnLake` / StillUsesLake | **true** | Lake host still elaborates Systems Lean |
| residual free / proof / llvm / PROVABLY | **false** / held | free claim still false; measure separate |

### Finished claim-B ladder (partials; complete false throughout)

| Step | Plain name | Living tip highlight |
|------|------------|----------------------|
| B..B5 | evidence + writer substrates | early self-host Open Name |
| B6..B10 | writer plan / exec / ownership inputs / perform gap / Lake-free install | substrate |
| B11..B15 | READ/COMPOSE/WRITE-HC / capable gap / step contract | freestanding-capable path |
| B16..B21 | capable READ/COMPOSE/WRITE-HC/regenerate/InstallOut/FullBar measure | capable pipeline |
| B22..B29 | ownership regenerate + Lake-free ordered join | ownership path substrate |
| **B30** | WithoutLake / product path authority | WithoutLake **true** |
| **B31** | freestanding perform evidence | evidence claimed |
| **B32** | official-path gap | gap measured |
| **B33** | official-path alternate | alternate present |
| **B34** | dual-eq WRITE parity | parity measured |
| **B35** | dual-eq WRITE close path | steps named |
| **B36** | dual-eq WRITE API | API present; not official alone |
| **B37** | dual-eq WRITE CAPABLE-GAP | **gap closed**; dual-eq live |
| **B38** | RETIRE-OFFICIAL | StillUsesFreestandingEmit **false**; short `OfficialRetire` |
| **B39** | PERFORM-CLAIMED | perform claimed **true**; short `PerformClaimed`; FULL-BAR-REQ closed |
| **B40** | OWNERSHIP-CLAIMED | ownership claimed **true**; short `OwnershipClaimed`; FULL-BAR-REQ closed |

**Ban:** B6..B40 theater. Harness stale prompts that name older Bn must **reseed**
to WATCHER tip, not re-implement.

### Ordered cliffs toward claim B complete (not a fixed calendar)

```
B40 OWNERSHIP-CLAIMED (landed)
    -> Full step-contract true    [WATCHER tip; only with real evidence]
        -> freestandingProductSelfHostComplete true
           [only if full bar in self-host.md]
```

Optional extra substrates may appear if ownership/Full/complete policy needs them.
Prefer one greppable delta per slice. Prefer short role modules. Prefer proved
non-claim over forged true.

### Unknown finish horizon (do not declare finished early)

Neither operator nor agents know the calendar or exact Bn count for claim B
complete. Treat every "going well" report as **substrate progress**, not a finish
forecast.

| Rule | Meaning |
|------|---------|
| **Bar-driven, not stage-driven** | Finished when `freestandingProductSelfHostComplete` is true under `self-host.md` flip policy -- not when "we shipped Bn." |
| **Partial != complete** | B40 ownership claimed true is not Full, not complete. |
| **Green gates != free** | Measure green, dual-pin green, Lake green do not flip residual free / PROVABLY / llvm. |
| **Living tip bools are SSoT** | `SelfApplyFs.lean`. Land-time modules may keep local false. |
| **No empty-Open theater** | While Full / complete bars remain checkable, keep Open Name and WATCHER tip. |
| **B complete != six claims done** | After B: free flip (held), C continuous, D PROVABLY, E llvm, F duals remain separate programs. |

### Product Lean module names (process law)

Documented in root `AGENTS.md` **Product Lean module names**.

| Do | Do not |
|----|--------|
| Short role names for **new** modules: `OwnershipClaimed`, `PerformClaimed`, `OfficialRetire`, `CapableWrite`, ... | Mint `ProductPathFreestandingPerform*` or `ProductPathFreestandingCapable*` |
| Stage labels in residual / WATCHER / greppable **string** tokens | Stage labels in **filenames** |
| Deliberate rename residual later (rg find + surgical hand edits via subagents) | Bulk find-and-replace / mass sed across the tree mid Full |
| Leave existing 19 long basenames until rename residual | Drive-by rename mid Full / complete |

### Closed tracks (do not re-open)

| Item | Status | Still false |
|------|--------|-------------|
| Track 1 Mult..Out host-owned emit + banner | **CLOSED** | free / complete |
| Track 2 CompilePath unit e2e Mult..Out | **CLOSED** | free / complete |
| Dual SSOT equality gate | **done** | free / complete |
| Track 4 A residual free **measure** | **closed** | `residualFreeClaimed` **false** |

---

## 1. The six claims (unpack once)

| # | Claim | What it actually means | What does **not** count |
|---|--------|------------------------|-------------------------|
| A | **Residual free** | Measured product residual (and host residual if claimed free) actually gone; greppable gates agree | Green Lake; probe green; inventory close readiness; dual residual honesty alone |
| B | **Freestanding product self-host complete** | A freestanding product path rebuilds the stated self-host kernel (and grows toward compiler self-application) without classic Lean on the **product wire** | HOST-SELF-HOST canaries; SelfApply host-structural `kernelRebuildsKernel`; SelfHostBody **compile-step readiness** fold alone (Mult..Out emit owned + stage cite) |
| C | **Proof complete** | Formal specs for a stated surface have real proofs that close that surface; `SpecProof.proofCompleteClaimed` only flips when evidence exists | Partial module theorems; readiness `_true` lemmas; "lots of theorems" |
| D | **PROVABLY / CompCert** | Real resolved `ccomp` + defined product matrix seals freestanding C | Dogfood compile; "CompCert-oriented" prose; host holds |
| E | **llvm-ir unlock** | Project permission to open `out/llvm-ir` residual after **true freestanding product self-host** evidence | SelfApply / ProductPath / InventoryClose readiness |
| F | **Meet-in-the-middle duals finished** | Stated correspondence between **real** Idris-side and **real** Lean-side sources is complete enough for a progressive surface matrix (imperfect edges documented) | Host Lean models of duals only; JoinMap surface canaries; three algorithm examples alone as "finished" |

**Dual residual honesty (always):** host elaborator residual (classic Lake) is
**not** the same as product freestanding residual. Closing B/A on the product
wire may leave host residual forever true -- that is allowed and already modeled
(`DualResidual`).

---

## 2. Dependency graph (do not reorder casually)

```
                    F duals (progressive, parallel early)
                         |
                         v
  Track 1 host-owned emit Mult..Out     [CLOSED]
  (Mult Linear Erasure Extract Types IR
   program graph compose plan apply Out)
                         |
                         v
  Track 2 real compile path unit e2e    [CLOSED]
  Mult..Out fixtures in CompilePath
                         |
                         v
  Track 3 B freestanding kernel rebuild [EVIDENCE PARTIAL]
  evidence (complete flag still false; stronger B when human Open)
                    /                    \
                   v                      v
            E llvm-ir unlock        A residual free (product)
            (planning + implement)   (stricter; may lag B)
                   |
                   v
            D PROVABLY (real ccomp + matrix)

  C proof complete: progressive ladder on each closed surface;
    never gates B alone; never retires tests/smokes.

  lean4 skill: default workbench on every Lean-heavy arrow above
  (Tracks 1-3, F Lean side, C); not on pure Nix / Idris / residual measurement alone.
```

| Order | Claim | Depends on |
|-------|--------|------------|
| 1 (parallel) | F duals deepen | Named map gaps; dual forks; not Systems alone |
| 1 (serial product) | Host-owned emit Mult..Out + banner | **CLOSED** (banner HOST-EMIT-BANNER; Dual SSOT equality is post-close hygiene then Done) |
| 1b (serial product) | Real compile path Mult..Out unit e2e | **CLOSED** |
| 2 | B self-host complete | Kernel coverage + freestanding rebuild **evidence partial**; complete flag still false |
| 3a | E llvm unlock | **B only** (project rule) |
| 3b | A residual free (product) | B + residual measurement gates; host may still remain |
| 4 | D PROVABLY | Real `ccomp` + product matrix (after freestanding C stable enough) |
| continuous | C proof complete | Specs + proofs per closed property; `proofCompleteClaimed` late |

---

## 3. Where we are today (evidence, not vibes)

Snapshot: **2026-07-29** after Track 1 + Track 2 Mult..Out closed, Track 3 B
kernel rebuild evidence partial, Track 3 B2 product kernel self-application
partial, Track 3 B3 product Out kernel evidence partial, Track 3 B4 product path
Lake bootstrap honesty partial, Track 3 B5 product writer surface substrate
partial, Track 3 B6 product writer path plan substrate partial, Track 3 B7
product writer path execution evidence partial, Track 3 B8 product path
ownership inputs partial, Track 3 B9 product path perform gap partial, Track 3 B10 product path perform step partial, Track 3 B11 product path perform READ dual SSOT substrate partial, Track 3 B12 product path perform COMPOSE plan/apply/body substrate partial, Track 3 B13 product path perform WRITE-HC freestanding .h/.c substrate partial, Track 3 B14 product path freestanding-capable gap measure + ordered pipeline join partial, Track 3 B15 product path freestanding-capable step contract / interface partial, Track 3 B16 product path freestanding-capable READ without FreestandingEmit partial, Track 3 B17 product path freestanding-capable COMPOSE without FreestandingEmit partial, Track 3 B18 product path freestanding-capable WRITE-HC without FreestandingEmit partial, Track 4 A
residual free measurement closed, Dual SSOT equality gate done, and Out-first
product language reseeded.

| Area | Status |
|------|--------|
| Systems host modules Mult..LlvmHold + algebraic/path/content theorems | **partial done**; theorem mill **exhausted** for this surface |
| Host-owned freestanding C (Track 1) | **CLOSED Mult..Out + banner:** Mult, Linear, Erasure, Extract/FAIL_CLOSED, Types/TYPED_IR, IR program, IR graph, host compose, emit plan, emit apply, emit Out (wire `EMIT_BODY_V0` / host EmitBody), HOST-EMIT-BANNER. Ownership SSoT: `emit/host-owned-emit.md` |
| Template-owned still | **none** (thin SPDX + placeholders only) |
| Dual SSOT | **done** (emit-time `DUAL-SSOT-EQUALITY` in FreestandingEmit; pure Nix proves live). Free/complete stay false |
| Real compile path unit e2e (Track 2) | **CLOSED Mult..Out:** fixtures Mult, Linear, Types, Program, Extract, Graph, Compose, Erasure, Plan, Apply, Body/Out in `CompilePath.lean` |
| Defined freestanding compile step | `SelfHostBody` / `selfHostBodyReady` = Mult..Out emit readiness fold + freestanding stage cite; complete **false**; residual free **false** |
| Duals (claim F) | Three algorithm ids both sides; JoinMap host use; **not** formal iso; further duals deferred (**partial**) |
| Open Systems residual queue | **Freestanding product self-host** (B complete path; Dual SSOT done) -- not empty done-for-now (`RESIDUAL-systems.md`) |
| Track 3 B kernel rebuild evidence | **partial** (`self-host.md` SELF-HOST-KERNEL-REBUILD; measured Mult..Out regenerate; freestanding `cc -c` green; complete stayed false) |
| Track 3 B2 product kernel self-application | **partial** (`self-host.md` SELF-HOST-KERNEL-SELF-APPLY; SelfApplyFs `productKernelSelfApplyPartialReady`; pure Nix emit/out match; complete stayed false) |
| Track 3 B3 product Out kernel evidence | **partial** (`self-host.md` SELF-HOST-PRODUCT-OUT; SelfApplyFs `productOutKernelEvidencePartialReady`; pure Nix Out B-bar ownership+APIs; complete stayed false) |
| Track 3 B4 product path Lake bootstrap | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-BOOTSTRAP; SelfApplyFs `productPathHostLakeBootstrapPartialReady` / bootstrap remains true; pure Nix writer honesty; complete stayed false) |
| Track 3 B5 product writer surface | **partial** (`self-host.md` SELF-HOST-PRODUCT-WRITER-SURFACE; SelfApplyFs `productWriterSurfacePartialReady`; pure Nix plan/apply/body + ownership on Out; B4 bootstrap remains; complete stayed false) |
| Track 3 B6 product writer path plan | **partial** (`self-host.md` SELF-HOST-PRODUCT-WRITER-PATH-PLAN; SelfApplyFs `productWriterPathPlanPartialReady`; ordered WRITER-PATH-STEP-* plan; pure Nix plan + B4 bootstrap + B5 when release; complete stayed false) |
| Track 3 B7 product writer path execution | **partial** (`self-host.md` SELF-HOST-PRODUCT-WRITER-PATH-EXEC; SelfApplyFs `productWriterPathExecutionPartialReady`; FreestandingEmit load/render/write + just install cites; pure Nix exec + B6 when release; complete stayed false) |
| Track 3 B8 product path ownership inputs | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-OWNERSHIP; SelfApplyFs `productPathOwnershipInputsPartialReady`; Mult..Out SSOT + emit + Out inputs substrate; living tip after B40 `productPathFreestandingOwnershipClaimed` true; pure Nix SSOT present + B7 when release; complete stayed false; B8 is not Full) |
| Track 3 B9 product path perform gap | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-PERFORM; SelfApplyFs `productPathPerformPartialReady`; `productPathFreestandingPerformClaimed` false; `productPathPerformDependsOnLake` true; install step mechanism; FREESTANDING-PERFORM-GAP-*; pure Nix Lake writer + install cp + B8 when release; complete stayed false) |
| Track 3 B10 product path perform step | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-PERFORM-STEP; SelfApplyFs `productPathPerformStepPartialReady`; `productPathPerformInstallLakeFreeEntrypointClaimed` true; recipe `install-freestanding-c-out`; perform claimed false; DependsOnLake true; pure Nix productPathPerformStepGreen; complete stayed false) |
| Track 3 B11 product path perform READ | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-PERFORM-READ; SelfApplyFs `productPathPerformReadPartialReady`; `productPathPerformReadEntrypointClaimed` true; recipe `read-product-ssot`; ProductPathReadSsot `readDualSsotAtRoot`; ReadDependsOnLake true; perform claimed false; pure Nix productPathPerformReadGreen; complete stayed false; not freestanding close of FREESTANDING-PERFORM-GAP-READ-SSOT) |
| Track 3 B12 product path perform COMPOSE | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-PERFORM-COMPOSE; SelfApplyFs `productPathPerformComposePartialReady`; `productPathPerformComposeEntrypointClaimed` true; recipe `compose-product-plan`; ProductPathComposePlan `composePlanApplyBodyAtRoot`; ComposeDependsOnLake true; perform claimed false; pure Nix productPathPerformComposeGreen; complete stayed false; not freestanding close of FREESTANDING-PERFORM-GAP-COMPOSE) |
| Track 3 B13 product path perform WRITE-HC | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-PERFORM-WRITE-HC; SelfApplyFs `productPathPerformWriteHcPartialReady`; `productPathPerformWriteHcEntrypointClaimed` true; recipe `write-freestanding-hc`; ProductPathWriteHc `writeFreestandingHcAtRoot` -> FreestandingEmit.emitAtRoot; WriteHcDependsOnLake true; perform claimed false; pure Nix productPathPerformWriteHcGreen; complete stayed false; not freestanding close of FREESTANDING-PERFORM-GAP-WRITE-HC) |
| Track 3 B14 freestanding-capable gap | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-GAP; SelfApplyFs `productPathFreestandingCapablePartialReady`; install Lake-free true; CapableRead true (B26); CapableCompose true (B27); CapableWriteHc true (B28); full perform false; recipes `regenerate-product-path` + `freestanding-capable-gap` + three Lake-free Capable* recipes; ProductPathFreestandingCapable; pure Nix productPathFreestandingCapableGreen; complete stayed false; not freestanding close of FREESTANDING-PERFORM-GAP-*) |
| Track 3 B15 freestanding-capable step contract | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-STEP-CONTRACT; SelfApplyFs `productPathFreestandingCapableStepContractPartialReady`; authority-not-emit true; READ satisfied true after B16; Compose satisfied true after B17; WriteHc satisfied true after B18; Full false; freestandingCapable* APIs + BLOCKER-*; recipe `freestanding-capable-step-contract`; ProductPathFreestandingCapableStepContract; pure Nix productPathFreestandingCapableStepContractGreen; complete stayed false; not Full / ownership regenerate close) |
| Track 3 B16 freestanding-capable READ | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-READ; SelfApplyFs `productPathFreestandingCapableReadPartialReady`; freestandingCapableReadDualSsot no FreestandingEmit; ReadSatisfied true; B14 CapableRead true after B26; DependsOnLake true Lake diagnostic; recipes `freestanding-capable-read` + `freestanding-capable-read-lake-free`; ProductPathFreestandingCapableRead; pure Nix productPathFreestandingCapableReadGreen; complete stayed false; not freestanding WRITE close) |
| Track 3 B17 freestanding-capable COMPOSE | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-COMPOSE; SelfApplyFs `productPathFreestandingCapableComposePartialReady`; freestandingCapableComposePlanApplyBody no FreestandingEmit; ComposeSatisfied true; B14 CapableCompose true after B27; DependsOnLake true Lake diagnostic; recipes `freestanding-capable-compose` + `freestanding-capable-compose-lake-free`; ProductPathFreestandingCapableCompose; pure Nix productPathFreestandingCapableComposeGreen; complete stayed false; not freestanding WRITE close) |
| Track 3 B18 freestanding-capable WRITE-HC | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-WRITE-HC; SelfApplyFs `productPathFreestandingCapableWriteHcPartialReady`; freestandingCapableWriteFreestandingHc no FreestandingEmit; dual SSOT HEADER/BODY + template embed + emit .h/.c write; WriteHcSatisfied true; B14 CapableWriteHc true after B28; DependsOnLake true Lake diagnostic; recipes `freestanding-capable-write-hc` + `freestanding-capable-write-hc-lake-free`; ProductPathFreestandingCapableWriteHc; pure Nix productPathFreestandingCapableWriteHcGreen; complete stayed false; Full unsatisfied; not ownership regenerate WithoutLake) |
| Track 3 B19 freestanding-capable ordered regenerate | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-REGENERATE; SelfApplyFs `productPathFreestandingCapableRegeneratePartialReady`; freestandingCapableOrderedRegenerate READ+COMPOSE+WRITE-HC+INSTALL-OUT no FreestandingEmit; Full false; InstallOutOpen false after B20; DependsOnLake true; recipe `freestanding-capable-regenerate`; ProductPathFreestandingCapableRegenerate; pure Nix productPathFreestandingCapableRegenerateGreen; complete stayed false) |
| Track 3 B20 freestanding-capable Install Out | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-INSTALL-OUT; SelfApplyFs `productPathFreestandingCapableInstallOutPartialReady`; freestandingCapableInstallFreestandingOut emit->Out no FreestandingEmit; Full false; InstallOutOpen false; DependsOnLake true; recipe `freestanding-capable-install-out`; ProductPathFreestandingCapableInstallOut; pure Nix productPathFreestandingCapableInstallOutGreen; complete stayed false; not Full; not perform claimed) |
| Track 3 B21 freestanding-capable Full bar measure | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-FULL-BAR; SelfApplyFs `productPathFreestandingCapableFullBarPartialReady`; closed step contracts+InstallOut+ordered regenerate true; ownership regenerate without Lake false; FullBar CapableLakeFree true after B26+B27+B28 (Read true; Compose true; WriteHc true); Full false; DependsOnLake true; recipe `freestanding-capable-full-bar`; ProductPathFreestandingCapableFullBar; pure Nix productPathFreestandingCapableFullBarGreen; complete stayed false; not Full; not ownership regenerate WithoutLake) |
| Track 3 B22 ownership regenerate substrate | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-OWNERSHIP-REGENERATE; SelfApplyFs `productPathOwnershipRegeneratePartialReady`; install Lake-free true via ownership-regenerate-install-only; evolved with B23 READ + B24 COMPOSE + B25 WRITE-HC Lake-free; WithoutLake false; DependsOnLake true; ownership claimed false; recipes `ownership-regenerate` + `ownership-regenerate-install-only`; ProductPathOwnershipRegenerate; pure Nix productPathOwnershipRegenerateGreen; complete stayed false; not ownership claimed; not full WithoutLake) |
| Track 3 B23 ownership regenerate READ Lake-free | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-OWNERSHIP-REGENERATE; SelfApplyFs `productPathOwnershipRegenerateReadLakeFree` true + `productPathOwnershipRegenerateReadPathClaimed` true; Lake-free dual SSOT presence+token via `ownership-regenerate-read-only`; WithoutLake false; DependsOnLake true; B14 CapableRead was false at B23 land (true after B26 freestanding-capable-read-lake-free); ownership claimed false; complete stayed false; not full WithoutLake; B23 path piece is not freestanding-capable CapableRead) |
| Track 3 B24 ownership regenerate COMPOSE Lake-free | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-OWNERSHIP-REGENERATE; SelfApplyFs `productPathOwnershipRegenerateComposeLakeFree` true + `productPathOwnershipRegenerateComposePathClaimed` true; Lake-free plan/apply/body presence+token via `ownership-regenerate-compose-only`; WithoutLake false; DependsOnLake true; B14 CapableCompose was false at B24 land (true after B27 freestanding-capable-compose-lake-free); ownership claimed false; complete stayed false; not full WithoutLake; B24 path piece is not freestanding-capable CapableCompose) |
| Track 3 B25 ownership regenerate WRITE-HC Lake-free | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-OWNERSHIP-REGENERATE; SelfApplyFs `productPathOwnershipRegenerateWriteHcLakeFree` true + `productPathOwnershipRegenerateWriteHcPathClaimed` true; Lake-free freestanding .h/.c presence+token via `ownership-regenerate-write-hc-only`; WithoutLake false -- path pieces != freestanding Capable* / Full dual-pin; DependsOnLake true; B14 CapableWriteHc was false at B25 land (true after B28 freestanding-capable-write-hc-lake-free); ownership claimed false; complete stayed false; not full WithoutLake; B25 path piece is not freestanding-capable CapableWriteHc) |
| Track 3 B26 freestanding-capable READ Lake-free / CapableRead | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-GAP / READ; SelfApplyFs `productPathFreestandingCapableRead` true + `productPathFreestandingCapableReadLakeFreeRecipe`; Lake-free Mult..Out token+HEADER/BODY via `freestanding-capable-read-lake-free`; freestanding-capable contract depth matching B16; distinct from ownership-regenerate-read-only; WithoutLake false; Full false; CapableWriteHc true after B28; ownership claimed false; perform claimed false; complete stayed false; not Full; not WithoutLake) |
| Track 3 B27 freestanding-capable COMPOSE Lake-free / CapableCompose | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-GAP / COMPOSE; SelfApplyFs `productPathFreestandingCapableCompose` true + `productPathFreestandingCapableComposeLakeFreeRecipe`; Lake-free plan/apply/body token+HEADER/BODY via `freestanding-capable-compose-lake-free`; freestanding-capable contract depth matching B17; distinct from ownership-regenerate-compose-only; WithoutLake false; Full false; CapableWriteHc true after B28; CapableRead true remains; ownership claimed false; perform claimed false; complete stayed false; not Full; not WithoutLake) |
| Track 3 B28 freestanding-capable WRITE-HC Lake-free / CapableWriteHc | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-GAP / WRITE-HC; SelfApplyFs `productPathFreestandingCapableWriteHc` true + `productPathFreestandingCapableWriteHcLakeFreeRecipe`; Lake-free Mult..Out dual SSOT token+HEADER/BODY + write substrate via `freestanding-capable-write-hc-lake-free`; freestanding-capable contract depth matching B18; distinct from ownership-regenerate-write-hc-only; FullBar CapableLakeFree true; WithoutLake false; Full false; CapableRead+Compose true remain; ownership claimed false; perform claimed false; complete stayed false; not Full; not WithoutLake) |
| Track 3 B29 ownership regenerate Lake-free ordered join | **partial** (`self-host.md` SELF-HOST-PRODUCT-PATH-OWNERSHIP-REGENERATE; SelfApplyFs `productPathOwnershipRegenerateLakeFreeJoinClaimed` true + `productPathOwnershipRegenerateLakeFreeJoinRecipe`; ordered `ownership-regenerate-lake-free` joins freestanding-capable-*-lake-free + ownership path pieces + install (no lake); WithoutLake false -- join measure != freestanding product path authority; Full false; DependsOnLake true; ownership claimed false; perform claimed false; complete stayed false; not Full; not WithoutLake) |
| Track 4 A residual free measurement | **measurement closed** (`self-host.md` SELF-HOST-RESIDUAL-FREE-MEASURE; pure Nix PRODUCT-RESIDUAL-FREE-MEASURE; DualResidual free stayed false) |
| Claims A / B / C / D / E | **false** / held -- A free still false (measure != free flip); B ladder **B2..B40 partial** (perform claimed true B39; ownership claimed true B40; StillUsesFreestandingEmit false B38; WithoutLake true B30; FullBar CapableLakeFree true; Full false; complete false) != B complete; do not forge. Living tip SSoT: section 0 + `SelfApplyFs.lean` |
| llvm / PROVABLY | **held** (`LlvmHold.lean`) |
| Project lean4 skill | **landed** (submodule `skills/lean4-skills` + `.agents/skills/lean4`); use on host Lean residual |
| **Out-first product** | Product consumers = **`out/freestanding-c/`**; `src/systems/emit/` = **host freestanding workspace** (not peer product). Three languages only; freestanding C is **generated product Out only** |
| **Next product residual** | **Freestanding product self-host** (B complete path; living tip B40 ownership claimed true; next Full then complete). Free flip only when deliberately opened. Section 0 is living tip SSoT |

**Gap in one sentence (living tip B40; detail section 0):** Mult..Out host-owned
emit, unit e2e, Dual SSOT, claim A free **measurement**, and claim-B ladder through
**B40 OWNERSHIP-CLAIMED** are landed (perform claimed **true**; ownership claimed
**true**; StillUsesFreestandingEmit **false**; WithoutLake **true**; FullBar
CapableLakeFree **true**). Remaining cliff is **Full** step-contract then complete
only on full bar. B8 Mult..Out ownership **inputs substrate** remains (living tip
ownership true after B40 is claim-bool tip cascade, not B8 as non-claim of the bool).
Historical B9..B29 land-time rows below keep land-time wording; do not reseed living
bools from them. **Not** freestanding product self-host complete (Full false;
complete flag false; Lake host remains for capable regenerate), residual free
claim still false (DualResidual), duals not finished.

**Slake ready?** No. Host model + Mult..Out owned wire + unit fixtures + partial
kernel rebuild + B2..B14 partial != freestanding product self-host complete.

---

## 4. Project lean4 skill (how we work Lean on this plan)

### Discovery and policy

| Item | Path / rule |
|------|-------------|
| Skill body | `.agents/skills/lean4` -> `skills/lean4-skills/plugins/lean4/skills/lean4` |
| Upstream pack | `skills/lean4-skills` (read-only submodule; bump pin, do not edit as product) |
| Policy SSoT | `AGENTS.md` **Project agent skills** + **When to reach for lean4 tools** |
| Inventory | `skills/README.md`, `.agents/skills/README.md` |
| Entry pointer | `doc/lean-entry.md` |
| Optional PATH | `export PATH="$PWD/skills/lean4-skills/plugins/lean4/bin:$PATH"` (sorry analyzer, smart-search wrappers) |
| Host-agnostic invoke | Load `SKILL.md`; ask for named workflow (`draft`, `prove`, `autoprove`, `review`, `doctor`, ...). Slash `/lean4:*` only if the host registers them |

**Project overrides skill defaults:** agents never `git commit` (checkpoint =
status only); freestanding honesty; no invent Open Names to exercise the skill;
product wire ownership stays `emit/host-owned-emit.md` + FreestandingEmit.

### Default residual slice loop (host Lean)

```
1. Open residual Name with checkable Done when (product-first).
2. If primary paths are .lean / Lake: load lean4 skill at slice start.
3. Red gate first: just systems-host (or focused lake) before deep prove loops.
4. lean4 workflows for declaration/proof work (see map below).
5. Project gates decide Done: systems-host, systems-emit-wire, hygiene,
   ./src/systems/check.sh (and just check when full suite is warranted).
6. Update residual / ownership map / WATCHER / handoff as usual.
```

### Workflow menu (this repo)

| Situation | Workflow | Notes |
|-----------|----------|--------|
| New host module / emit SSOT companion Lean | **draft** then **prove** (or **formalize** if claim+proof together) | e.g. EmitTypes + readiness theorems |
| Long unattended sorry fill | **autoprove** with cycle/wall budget | Stop when stuck; replan; no header rewrites without ask |
| Refute a bad claim | **disprove** | Append counterexample; do not rewrite original header |
| Lake / elaborator / instance soup | **doctor** + skill error refs | Then project gates |
| Quality before residual Done | **review** (read-only) | Does not replace `just check` / systems-host |
| After green, simplify proofs | **refactor** / **golf** | Optional; still no claim forge |
| Progress report | **checkpoint** | Status only; human GPG-signed commit |
| Stuck mid-proof | **prove** cycle (plan/work/replan) | LSP when available; scripts via bin PATH |

### Claim / track -> skill role

| Track / claim | lean4 skill | Project tools still own |
|---------------|-------------|-------------------------|
| **1** Host-owned emit | **Primary:** draft/prove/review new `Emit*.lean`, fragment honesty theorems, Lake fix | SSOT `.txt`, FreestandingEmit embed, pure Nix presence, residual Name |
| **2** Real compile path | **Primary:** draft/prove path theorems; doctor on build breaks | Compile-path stamp shell; gates |
| **3** / **B** Self-host complete | **Partial:** host pins and path theorems for rebuild evidence | Freestanding rebuild measurement, complete flag flip policy, residual free still false |
| **4** / **A** Residual free | **Little:** only if residual rules are Lean theorems | Measurement gate (Nix/nm), DualResidual honesty |
| **5** / **C** Proof complete | **Yes when real proofs:** prove/autoprove/review/axiom scan on *new* product properties | Global `proofCompleteClaimed` human+late; tests never retired |
| **6** / **E** llvm unlock | **After B only:** later draft/prove llvm host modules | Unlock flags only after B evidence |
| **7** / **D** PROVABLY | **Almost none** | Real `ccomp` + matrix |
| **8** / **F** Duals | **Lean side primary:** formalize/prove correspondence, review duals | Idris fork tools; coordinator divergence; JoinMap consume only |

### Anti-patterns (skill misuse)

- Invent residual to exercise draft/prove
- Theorem canary mill sold as Track 1-3 progress
- Hand-author freestanding C "with the lean4 skill"
- Treat skill quality gate (lake + no sorry) as residual free / self-host complete / PROVABLY
- Use lean4 skill as main tool for Idris side, pure Nix, or shell glue

---

## 5. Work tracks (concrete Names for residual when unblocked)

Use plain Open Names in `RESIDUAL-systems.md` when implementing (schema already
there). Track labels below are planning structure only -- not residual item titles.

### Track 1 -- Host-owned freestanding emit (**CLOSED** Mult..Out)

**Status:** **CLOSED** (2026-07-29). Do not re-open Mult..Out ownership as Open
Names. Goal met (partial honesty): template bulk for Mult through Out moved to
Lean SSOT + FreestandingEmit embed. Ownership map SSoT:
`src/systems/emit/host-owned-emit.md`.

| Slice (closed) | Host stage | Wire id (kept) | Primary modules / SSOT |
|----------------|------------|----------------|------------------------|
| Mult | HOST-EMIT-MULT | product Mult APIs | `EmitMult.lean`, `host_emit_mult.ssot.txt` |
| Linear + ConsumeToken | HOST-EMIT-LINEAR | -- | `EmitLinear.lean`, `host_emit_linear.ssot.txt` |
| Erasure (mult-0 absence) | HOST-EMIT-ERASURE | -- | `EmitErasure.lean`, `host_emit_erasure.ssot.txt` |
| Extract + FAIL_CLOSED | HOST-EMIT-EXTRACT | FAIL_CLOSED_CHECKER_V1 | `EmitExtract.lean`, `host_emit_extract.ssot.txt` |
| Types / TYPED_IR | HOST-EMIT-TYPES | TYPED_IR_V0 | `EmitTypes.lean`, `host_emit_types.ssot.txt` |
| IR program | HOST-EMIT-PROGRAM | IR_PROGRAM_V0 | `EmitProgram.lean`, `host_emit_program.ssot.txt` |
| IR graph | HOST-EMIT-GRAPH | IR_GRAPH_EDGES_V0 | `EmitGraph.lean`, `host_emit_graph.ssot.txt` |
| Host compose | HOST-EMIT-COMPOSE | HOST_COMPOSE_V0 | `EmitCompose.lean`, `host_emit_compose.ssot.txt` |
| Emit plan | HOST-EMIT-PLAN | EMIT_PLAN_V0 | `EmitPlan.lean`, `host_emit_plan.ssot.txt` |
| Emit apply | HOST-EMIT-APPLY | EMIT_APPLY_V0 | `EmitApply.lean`, `host_emit_apply.ssot.txt` |
| Emit **Out** (residual name; wire Body) | HOST-EMIT-BODY + HOST-EMIT-SSOT | **EMIT_BODY_V0** (frozen ABI) | `EmitBody.lean`, `host_emit_body_fragment.ssot.txt` |

**Dual SSOT hygiene:** emit-time equality gate (Banner + Mult..Out) is the
post-Track-1 Name; not a Track 1 reopen. Banner / version / unit_translation
**closed** as HOST-EMIT-BANNER.

**Rules still:** no new bash `EMIT_*` residual stages; no grow probe C as product
progress; probe stays smoke debt. Product wire ABI for Out keeps `EMIT_BODY_V0`
and `slake_emit_body_*` (map-only frozen).

**Addresses:** product side of **A** (reduces product residual debt); substrate
for **B**. Track closed; next product is Track 3 B.

---

### Track 2 -- Real compile path (**CLOSED** Mult..Out unit e2e)

**Status:** **CLOSED** (2026-07-29). Do not re-open Mult..Out unit fixtures as
Open Names. Goal met (partial honesty): named fixtures in `CompilePath.lean`
lower unit surface through ordered IR -> host compose -> host-owned emit
readiness, with Lake smoke + theorems and local free/complete/proof/llvm claims
false.

| Fixture readiness | Chain (short) | Host emit fold |
|-------------------|---------------|----------------|
| `multFixtureCompilePathReady` | Mult grades -> IR -> mark/mint -> unitCompileReady | emitMultReady |
| `linearFixtureCompilePathReady` | MULT-1 linear -> IR -> mint/consume exact-once | emitLinearReady |
| `typesFixtureCompilePathReady` | TYPED_IR kind/mult -> IR -> compose | emitTypesReady |
| `programFixtureCompilePathReady` | multi-node IR + fold -> compose | emitProgramReady |
| `extractRuntimeFsGateReady` | Mult+Program: RUNTIME-FS accept; classic/edge reject | extract path honesty |
| `graphFixtureCompilePathReady` | multi-node IR + edges 0->1,1->2 -> compose | emitGraphReady |
| `composeFixtureCompilePathReady` | multi-node + edges + mark/mint/consume | emitComposeReady |
| `erasureFixtureCompilePathReady` | MULT-0 mark-only (no mint) | emitErasureReady |
| `planFixtureCompilePathReady` | planFromCompose inventory | emitPlanReady |
| `applyFixtureCompilePathReady` | applyFromCompose tag buffer | emitApplyReady |
| `bodyFixtureCompilePathReady` (Out residual name) | bodyFromCompose buffer + HOST-EMIT-SSOT fragment | emitBodyReady |

**Explicit non-claims for Track 2 close:** not residual free; not product
self-host complete; not full product module emit; not CFG/SSA; not elaborator
MULT-1 residual free; Out fragment scaffolding != full product module emit.

**Truth location:** Lean host (`CompilePath`, kernels, compose, emit). Shell
stamp remains thin process glue.

**Addresses:** **B** prerequisites closed for Mult..Out unit path. Track 3 B
kernel rebuild evidence is **partial** (complete flag still false).

---

### Track 3 -- Freestanding product self-host complete (**B**) -- **B2 PARTIAL**

**Status:** freestanding kernel rebuild **evidence partial** + product kernel
self-application **partial B2** (2026-07-29). Track 1 Mult..Out host-owned emit
and Track 2 Mult..Out unit e2e are closed. Names **Freestanding kernel rebuild
evidence** and **Product kernel self-application** are **done (partial)** in
`RESIDUAL-systems.md`. Claim **B complete** still **false**. Full freestanding
product self-application of Slake sources is **deferred** (not a small residual).

**Acceptance SSoT:** `src/systems/self-host.md` sections **Freestanding kernel
rebuild evidence** (SELF-HOST-KERNEL-REBUILD) + **Product kernel self-application**
(SELF-HOST-KERNEL-SELF-APPLY) + defined freestanding compile step (SELF-HOST-BODY).
`SelfHostBody` / `selfHostBodyReady` is **compile-step readiness only** -- not B
complete.

| Milestone | Evidence bar | Status |
|-----------|--------------|--------|
| Kernel freestanding closed loop | Host + product C + parity for Mult (then Linear..) | prior partial |
| Freestanding kernel rebuilds kernel (measured) | Host FreestandingEmit + `just build` regenerates Mult..Out HOST-EMIT-* greppably; freestanding `cc -c` green; emit/out sha256 match | **partial landed** -- complete flag **false** |
| Product kernel self-application (B2) | Acceptance names B-bar inputs; SelfApplyFs pin; pure Nix emit/out match when release present; product wire exercise | **partial landed** -- complete flag **false** |
| Compiler subset self-application (complete) | Stated Slake check+emit sources processable by freestanding product path without classic Lean as product path | **deferred**; unlocks **E** planning only after true complete |
| Full Mult..Out self-application | Progressive after kernel | optional growth after B complete |

**B bar (default, still the flip policy):**

1. Host-owned emit covers Mult + Linear + Erasure + Types + IR program at minimum
   (**owned**; measured regenerate expanded to Mult..Out with greppable evidence).
2. Freestanding product path rebuilds that kernel C (measured partial via host emit
   writer + automated emit/out match; full product self-application still open).
3. `SelfApplyFs.freestandingProductSelfHostComplete` (or successor) flips **only**
   with full freestanding product evidence; residual free still false unless A gates
   pass. **Stayed false** after evidence + B2 partial.

**lean4 skill:** host modules that *state* complete/rebuild contracts and
non-claims -- **draft**/**prove**/**review**. Rebuild measurement, install path,
and complete-flag policy remain project residual work (not skill theater).

**Addresses:** **B** evidence + B2 partial substrate; does **not** unlock **E**
until complete.

---

### Track 4 -- Residual free (**A**)

**Two residuals always:**

| Side | Free means | Typical path |
|------|------------|--------------|
| **Product** | No managed Lean runtime / GC / forbidden residual on freestanding nm/IR/link | After B + residual measurement script/gate (not probe alone) |
| **Host elaborator** | Classic Lake residual gone | **May never be claimed free**; DualResidual allows host remains true forever |

**Plan for product residual free:**

1. Define residual measurement (what symbols/libs are forbidden on product).
2. Gate under pure Nix / `just` (not only prose).
3. Flip product free claim only when gate green on release `out/freestanding-c`.
4. Keep `hostElaboratorResidualRemains` honest unless a separate host-free program exists.

**lean4 skill:** not primary. Use only if residual criteria are formalized as
Lean theorems that need **prove**/**review**.

**Addresses:** **A**. Do not sell inventory close or dual residual honesty as A.

---

### Track 5 -- Proof complete (**C**)

**Principle (AGENTS.md):** specs then proofs; proofs do not retire tests.

| Layer | Work | When `proofCompleteClaimed` |
|-------|------|------------------------------|
| Property proofs (partial) | Continue only for **new** product properties tied to Tracks 1-3 | stays **false** |
| Spec surface per closed unit | Readable statements for Mult/Linear/Types/IR contracts on freestanding path | false |
| Closed surface proof pack | Named "Mult freestanding contracts proof-complete" sibling flags if useful | module-local, not global forge |
| Global `SpecProof.proofCompleteClaimed` | Entire stated formal program closed | **late**; after substantial product + dual formal map; human explicit |

**lean4 skill (primary engine for this track):**

- **draft** readable specs, then **prove** / **autoprove** with budgets
- **review** + optional axiom scan (`lean4-skills-check-axioms-inline` when PATH set)
- **disprove** if a candidate complete claim is wrong
- **golf**/**refactor** after green -- optional polish, not residual mill

**Do not:** run theorem mill as residual when product tracks are open.
**Do:** attach proofs when a compile/emit slice lands a new invariant.

**Addresses:** **C** progressively; global flip is endgame, not near-term.

---

### Track 6 -- llvm-ir unlock (**E**) then implement

| Step | Condition |
|------|-----------|
| Keep held | Until **B** true freestanding product self-host evidence |
| Unlock planning | Flip `LlvmHold` unlock flags only with greppable B evidence + residual honesty |
| Implement `out/llvm-ir` | Separate residual Names after unlock; Rust-native link design bar later |
| Never | Unlock from SelfApply / ProductPath / theorem depth alone |

**lean4 skill:** after unlock only -- draft/prove host LLVM modules; doctor on
Lake. No residual-open llvm mill before B.

**Addresses:** **E**.

---

### Track 7 -- PROVABLY / CompCert (**D**)

| Step | Condition |
|------|-----------|
| Held | Until real `ccomp` in environment + product matrix defined |
| Matrix | Which freestanding units must seal; fail-closed if ccomp missing |
| PROVABLY claim | Full matrix green under real CompCert; never dogfood forge |
| Order | Prefer after freestanding C stable (post large Track 1 / toward B); can parallel measurement once ccomp exists |

**lean4 skill:** almost never primary. Optional later: formalize matrix *specs*
in Lean with **draft**/**prove**.

**Addresses:** **D**.

---

### Track 8 -- Meet-in-the-middle duals finished (**F**)

**Owners:** Idris fork (`src/idris2/`, `RESIDUAL-idris.md`) + Lean fork
(`src/lean4/`, `RESIDUAL-lean.md`) + coordinator join (`doc/divergence.md`,
surface matrix). Systems JoinMap **consumes** duals; does not invent dual trees.

| Phase | Work | Done when |
|-------|------|-----------|
| F0 (today) | Three dual algorithms + maps + JOIN | **done** partial |
| F1 | Named imperfect edges closed or documented in divergence | edge table stable; no silent invent |
| F2 | Further duals **only** when product map requires new algorithm ids | dual id + both sides + join map update |
| F3 | Formal correspondence (Lean theorems of the map, not host freestanding) | Lean-side `L-FORMAL-ISO` when prioritized |
| F4 | Progressive surface matrix rows close | `SurfaceMatrix` open rows shrink with evidence; day-one full parity never required |
| F5 "finished" | Stated core map complete for freestanding Slake needs (0/1/omega, erasure, extract honesty, join algorithms used by Slake) | human accepts matrix + divergence; not infinite dual farm |

**lean4 skill:** **Lean side** -- formalize/prove/review/golf on
`src/lean4/**/*.lean` for F3+. **Idris side** -- not a lean4-skill target.
Systems host may **prove** JoinMap consumption theorems only when product
residual Names them.

**Addresses:** **F**. Parallel to Tracks 1-3; does not unlock llvm alone.

---

## 6. Multi-chat ownership

| Role | Owns | Does not own |
|------|------|--------------|
| **Systems / Slake** | Tracks 1-3, 4 (product), 5 (host proofs), residual/WATCHER for systems; **lean4 skill on `src/systems/**/*.lean`** | Dual source farms; llvm before B; git unless asked |
| **Idris side** | Real Idris duals, maps, check.sh | `src/systems/`, Lean dual writes; lean4 skill as main tool |
| **Lean side** | Real Lean duals, optional formal map; **lean4 skill on `src/lean4/**/*.lean`** | freestanding emit treadmill |
| **Coordinator** | Join board, divergence merge, fork guidance, progress meters | racing both duals + systems alone |
| **Watcher** | Auto-continue only when Open Name has checkable Done when | Invent residual when queue empty |

**Implement orchestrator note:** when `/implement` Done when is mainly Lean,
implementer prompts should **require loading** `.agents/skills/lean4/SKILL.md`
and name workflows (draft/prove/review). Parent still coordinates; skill does
not replace residual honesty or project gates.

---

## 7. Recommended program order (comprehensive but phased)

### Phase 0 -- Compaction-durable reseed (**done** 2026-07-29 hygiene + Out-first)

1. Human accepted six-claims program; Track 1+2 Mult..Out closed on disk.
2. This file section 3 / tracks / defaults refreshed (Out-first product language).
3. Naming honesty: residual emit stage **Out** <-> wire `EMIT_BODY_V0`; product
   **Out (release)** = `out/freestanding-c/`; SelfHost* = defined freestanding
   compile step (prose only; no ABI rename).
4. Open queue: Dual SSOT then Freestanding product self-host -- **not** empty
   done-for-now while plan defaults remain checkable.
5. free / complete / proof / PROVABLY / llvm stay false.

### Phase 1 -- Product wire ownership (Track 1) -- **CLOSED** Mult..Out + banner

All Mult..Out ownership slices landed (Types through Out). Banner / version /
unit_translation host ownership **closed** (HOST-EMIT-BANNER). Do not re-open
as Open Names.

### Phase 1b -- Dual SSOT equality gate (hygiene)

Fail-closed emit-time equality: durable `host_emit_*.ssot.txt` HEADER/BODY vs
Lean `*HeaderFragment` / `*BodyFragment` (Banner + Mult..Out). Pure Nix proves
the check is live. Not free flip; not B complete.

### Phase 2 -- Real lower path (Track 2) -- **CLOSED** Mult..Out unit e2e

All Mult..Out CompilePath fixtures landed. Do not re-open as Open Names.

### Phase 3 -- Self-host complete bar (Track 3 / **B**) -- **B / B2 PARTIAL**

1. **Done (partial):** Freestanding kernel rebuild evidence Name -- acceptance in
   `self-host.md`, measured Mult..Out regenerate, complete stayed false.
2. **Done (partial B2):** Product kernel self-application Name -- acceptance
   SELF-HOST-KERNEL-SELF-APPLY, SelfApplyFs pin, pure Nix emit/out match, product
   wire exercise; complete stayed false.
3. **Next product residual after Dual SSOT:** **Freestanding product self-host**
   (B complete path) -- open Name with checkable Done when from `self-host.md`
   flip policy; do not empty-Open park.
4. Host Lean pins via skill **prove**/**review** only with evidence; keep residual
   free false unless A ready; do not unlock E until complete true.

### Phase 4 -- Residual free product (Track 4 / **A**) + llvm planning (**E**)

1. **Done (measurement):** Residual measurement gate (Nix/just) Name **Product
   residual free gate** -- PRODUCT-RESIDUAL-FREE-MEASURE on release when present;
   residualFreeClaimed **stayed false** (measure green != free claimed).
2. Product free flip only per `self-host.md` flip policy (deliberate residual;
   not auto-queued from measure alone).
3. Unlock llvm **planning** only after B **complete** (not evidence-only / B2
   partial); implement llvm as separate residual (then skill on llvm host Lean).

### Phase 5 -- PROVABLY (Track 7 / **D**)

When `ccomp` real + matrix defined; independent of llvm implement if desired,
but needs stable freestanding C.

### Phase C -- Conditional RC / TLS / atomic / parallel (not default residual)

Open **only** when freestanding design forces them (see north-star explore).
Default remains **no product RC**, **no product GC**, no pthread residual on
product Out. Short table (conditional Names, not auto-Open):

| Conditional Name | When |
|------------------|------|
| Reference counting necessity proof | Named freestanding case cannot meet bar without RC |
| Thread-local reference count model | After necessity; single-thread / thread-affine only |
| Atomic reference count model | After necessity; shared concurrent ownership only |
| Ownership transfer concurrency bar | Cross-thread exclusive transfer without data races |
| Parallel safety gates vs Rust | After a concurrency model exists; greppable, not marketing |

Do **not** invent RC/concurrency residual as progress. Prefer linear/affine/arena.

### Continuous -- Duals (**F**) + proofs (**C**)

- Dual forks: only named gaps; Lean side uses lean4 skill for formal map.
- Proofs: attach to new product properties with prove/review; global proof complete late.

---

## 8. Defaults if open questions unanswered

| Question | Default |
|----------|---------|
| Next residual Name | **Freestanding product self-host** (B complete path; Dual SSOT done). Not empty done-for-now while plan defaults remain checkable |
| Full B complete residual | Open as **Freestanding product self-host**; flip only per `self-host.md`; not auto-reopen closed evidence/B2/A-measure Names as substitutes |
| B bar | Kernel = Mult+Linear+Erasure+Types+IR program host-owned at minimum (now owned); freestanding rebuild of that kernel; may expand if measured |
| Lake bootstrap | Allowed indefinitely for **host** development; product Out freestanding is the bar |
| Dual SSOT equality | **Done:** emit-time fail-closed + pure Nix live tokens |
| Out-first product | Product = `out/freestanding-c/`; `emit/` = host freestanding workspace; freestanding C generated only |
| Three languages only | Idris 2 / Lean 4 / pure Nix; no handwritten product C residual; no Python; no new shell mills |
| Banner / version host ownership | **Closed** as HOST-EMIT-BANNER (EmitBanner + host_emit_banner.ssot.txt); not substitute for B |
| proofCompleteClaimed | Stays false until human-approved global close |
| llvm | Planning only after B **complete**; no residual-open llvm mill before |
| Phase C RC/TLS/atomic/parallel | Conditional only (section 7 Phase C); default no product RC |
| Emit stage residual name | **Out** (maps to wire `EMIT_BODY_V0`; ABI frozen) |
| SelfHostBody prose | **Defined freestanding compile step** / self-host step readiness -- not B complete |
| lean4 skill on B / A slices | **Load at start** when paths are `.lean` / Lake; project gates for Done |
| Checkpoint / commit | Skill checkpoint = status only; human signed commit |

---

## 9. Hard bans (carry forward)

- No forge residual free / product complete / proof complete / PROVABLY / llvm unlock
- No bash EMIT_* residual stage treadmill
- No grow check.sh / probe C as product progress
- No dual race (systems inventing Idris/Lean duals)
- No ProductPath alias theater
- No theorem canary residual when Open is empty
- No invent Open Names to exercise lean4 skill
- No agent git commit from skill checkpoint
- Hands-off git unless asked
- Isolation: work in this repo; `ref/` and `skills/*` read-only (submodule pin bump only)

---

## 10. Success scorecard (how we know each claim is addressed)

| Claim | Current | Minimum green evidence |
|-------|---------|------------------------|
| A residual free (product) | **false** -- **measurement closed** (PRODUCT-RESIDUAL-FREE-MEASURE green on release when present; residualFreeClaimed false) | DualResidual free flip only per flip policy after measure green; measure alone does not flip |
| B self-host complete | **false** -- **B2..B40 partial** (through B40 OWNERSHIP-CLAIMED: ownership claimed **true** with perform-claimed + dual-eq WRITE; perform claimed **true**; StillUsesFreestandingEmit **false**; FULL-BAR-REQ-OWNERSHIP + PERFORM closed; WithoutLake true after B30; FullBar CapableLakeFree true; Full **false**; DependsOnLake **true**; complete flag **false**) | Greppable complete flag true + freestanding product path rebuild of stated kernel without classic Lean as product path + honesty non-claims for residual free / PROVABLY / llvm; partial ladder does not flip complete |
| C proof complete | **false** | `proofCompleteClaimed` true only with stated closed formal surface; tests still required; skill review/axiom scan support partial ladders only |
| D PROVABLY | **false / held** | Real `ccomp` + full defined matrix green |
| E llvm unlock | **false / held** | `llvmUnlocked` true after B **complete** (not evidence-only partial); then separate implement residual for `out/llvm-ir` |
| F duals finished | **partial** (F0) | Surface matrix + divergence accept stated core map; both sides real sources; formal iso optional later (lean4 skill on Lean side) |

**Track substrate (not claims):** Track 1 Mult..Out host-owned emit **closed**;
Track 2 Mult..Out unit e2e **closed**; Dual SSOT **done**; Track 4 A measure
**closed** (free false). Track 3 B ladder **B2..B40 partial** (see section 0
finished ledger): ownership claimed **true** (B40); perform claimed **true** (B39);
StillUsesFreestandingEmit **false** (B38); WithoutLake **true** (B30); FullBar
CapableLakeFree **true**; Full **false**; complete **false**.
Partial ladder does not flip A-E true. Living tip SSoT: `SelfApplyFs.lean`.

**Per-slice Lean quality (skill ladder, not claim flip):** no sorry in agreed
scope; Lake green; project gates green; optional axiom scan clean for new theorems.

---

## 11. First implement slices -- Track 3 B evidence + B2

### 11a. Freestanding kernel rebuild evidence -- **DONE (partial)**

**Name:** Freestanding kernel rebuild evidence (Track 3 / claim **B**)

**Status:** **done (partial)** 2026-07-29. Acceptance + measured Mult..Out
regenerate landed. `freestandingProductSelfHostComplete` **stayed false**.

**Evidence home:** `src/systems/self-host.md` SELF-HOST-KERNEL-REBUILD;
`RESIDUAL-systems.md` Done archive.

### 11b. Product kernel self-application -- **DONE (partial B2)**

**Name:** Product kernel self-application (Track 3 / claim **B** stronger bar)

**Status:** **done (partial B2)** 2026-07-29. Acceptance SELF-HOST-KERNEL-SELF-APPLY
(default Mult+Linear+Erasure+Types+IR program); SelfApplyFs
`productKernelSelfApplyPartialReady`; pure Nix emit/out match when release
present; product wire exercise via check.sh. Complete **stayed false**. Full
freestanding product compiler of Slake sources **deferred**.

**Evidence home:** `src/systems/self-host.md` SELF-HOST-KERNEL-SELF-APPLY;
`SelfApplyFs.lean`; `nix/systems-emit-wire/default.nix`.

### 11b2. Product Out kernel evidence -- **DONE (partial B3)**

**Name:** Product Out kernel evidence (Track 3 / claim **B** Out-first bar)

**Status:** **done (partial B3)** 2026-07-29. Acceptance SELF-HOST-PRODUCT-OUT;
SelfApplyFs `productOutKernelEvidencePartialReady`; pure Nix
productOutKernelEvidenceGreen (B-bar HOST-EMIT + APIs on Out when release
present). Complete **stayed false**. Full freestanding product compiler of Slake
sources **still open** under Freestanding product self-host residual.

**Evidence home:** `src/systems/self-host.md` SELF-HOST-PRODUCT-OUT;
`SelfApplyFs.lean`; `nix/systems-emit-wire/default.nix` + `emit-product.nix`.

### 11b3. Product path Lake bootstrap honesty -- **DONE (partial B4)**

**Name:** Product path Lake bootstrap honesty (Track 3 / claim **B** writer role)

**Status:** **done (partial B4)** 2026-07-29. Acceptance
SELF-HOST-PRODUCT-PATH-BOOTSTRAP; SelfApplyFs
`productPathHostLakeBootstrapPartialReady` + `productPathHostLakeBootstrapRemains`
true; pure Nix `productPathHostLakeBootstrapGreen` (FreestandingEmit still
product writer + acceptance honesty; Out B-bar when release present). Complete
**stayed false**. Remaining complete gap: freestanding product **writer** path.

**Evidence home:** `src/systems/self-host.md` SELF-HOST-PRODUCT-PATH-BOOTSTRAP;
`SelfApplyFs.lean`; `nix/systems-emit-wire/default.nix` + `emit-product.nix`.

### 11b4. Product writer surface substrate -- **DONE (partial B5)**

**Name:** Product writer surface substrate (Track 3 / claim **B** writer contracts)

**Status:** **done (partial B5)** 2026-07-29. Acceptance
SELF-HOST-PRODUCT-WRITER-SURFACE; SelfApplyFs `productWriterSurfacePartialReady`
(B4 + plan/apply/body + B-bar surface + complete false); pure Nix
`productWriterSurfaceGreen` (HOST-EMIT-PLAN/APPLY/BODY +
`slake_emit_*_from_compose` on Out when release present; B3 B-bar green;
acceptance + host pin). Complete **stayed false**. Lake bootstrap **remains**
true. Remaining complete gap: freestanding product **writer path** (not surface
alone).

**Evidence home:** `src/systems/self-host.md` SELF-HOST-PRODUCT-WRITER-SURFACE;
`SelfApplyFs.lean`; `nix/systems-emit-wire/default.nix` + `emit-product.nix`.

### 11b5. Product writer path plan substrate -- **DONE (partial B6)**

**Name:** Product writer path plan substrate (Track 3 / claim **B** regenerate plan)

**Status:** **done (partial B6)** 2026-07-29. Acceptance
SELF-HOST-PRODUCT-WRITER-PATH-PLAN; SelfApplyFs `productWriterPathPlanPartialReady`
(B5 + ordered WRITER-PATH-STEP-READ-SSOT / COMPOSE-PLAN-APPLY-BODY /
WRITE-FREESTANDING-HC / INSTALL-OUT + bootstrap remains + complete false); pure
Nix `productWriterPathPlanGreen` (plan steps + FreestandingEmit bootstrap
honesty + B5 surface when release present). Complete **stayed false**. Lake
bootstrap **remains** true. Plan is **not** freestanding ownership. Remaining
complete gap after B7: freestanding product path **owns** regenerate without
classic Lean as product path (not plan-id re-list alone).

**Evidence home:** `src/systems/self-host.md` SELF-HOST-PRODUCT-WRITER-PATH-PLAN;
`SelfApplyFs.lean`; `nix/systems-emit-wire/default.nix` + `emit-product.nix`.

### 11b6. Product writer path execution evidence -- **DONE (partial B7)**

**Name:** Product writer path execution evidence (Track 3 / claim **B** host performs plan)

**Status:** **done (partial B7)** 2026-07-29. Acceptance
SELF-HOST-PRODUCT-WRITER-PATH-EXEC; SelfApplyFs `productWriterPathExecutionPartialReady`
(B6 plan + loadMultSsot / renderHeader / renderSource / IO.FS.writeFile /
emitAtRoot / just build cites + bootstrap remains + complete false);
pure Nix `productWriterPathExecutionGreen` (FreestandingEmit perform tokens +
just install tokens + B6 plan when release present). Complete **stayed false**.
Lake bootstrap **remains** true. Host-driven execution evidence is **not**
freestanding product path ownership. Remaining complete gap: freestanding product
path owns plan steps without classic Lean as product path (not B6 re-land; not
host-exec re-grep theater).

**Evidence home:** `src/systems/self-host.md` SELF-HOST-PRODUCT-WRITER-PATH-EXEC;
`SelfApplyFs.lean`; `nix/systems-emit-wire/default.nix` + `emit-product.nix`.

### 11b7. Product path ownership inputs -- **DONE (partial B8)**

**Name:** Product path ownership inputs (Track 3 / claim **B** ownership bar surface)

**Status:** **done (partial B8)** 2026-07-29. Acceptance
SELF-HOST-PRODUCT-PATH-OWNERSHIP; SelfApplyFs `productPathOwnershipInputsPartialReady`
(B7 exec + Mult..Out host_emit_*.ssot.txt path cites + emit
slake_freestanding.{h,c} + Out path + dual SSOT cite + bootstrap remains +
complete false + ownership claimed false); `productPathFreestandingOwnershipClaimed`
proved false; pure Nix `productPathOwnershipInputsGreen` (SSOT files present +
emit wire + acceptance + host pin non-claim + B7 when release present). Complete
**stayed false**. Lake bootstrap **remains** true. Ownership inputs bar is **not**
freestanding ownership claimed. Remaining complete gap: freestanding-capable path
**performs** ownership bar without classic Lean as product path (not B6/B7/B8
theater).

**Evidence home:** `src/systems/self-host.md` SELF-HOST-PRODUCT-PATH-OWNERSHIP;
`SelfApplyFs.lean`; `nix/systems-emit-wire/default.nix` + `emit-product.nix`.

### 11b8. Product path perform gap -- **DONE (partial B9)**

**Name:** Product path perform gap (Track 3 / claim **B** perform bar surface)

**Status:** **done (partial B9)** 2026-07-29. Acceptance
SELF-HOST-PRODUCT-PATH-PERFORM; SelfApplyFs `productPathPerformPartialReady`
(B8 ownership inputs + `productPathPerformDependsOnLake` true +
`productPathPerformInstallStepLakeFreeCapable` true + FREESTANDING-PERFORM-GAP-*
+ bootstrap remains + complete false + ownership claimed false + perform claimed
false); `productPathFreestandingPerformClaimed` proved false; pure Nix
`productPathPerformGreen` (FreestandingEmit still writer tokens + justfile
install cp + acceptance + host pin non-claim + B8 when release present). Complete
**stayed false**. Lake bootstrap **remains** true. Perform gap bar is **not**
freestanding perform claimed. Remaining complete gap: freestanding-capable
perform of read/compose/write without classic Lean as product path (not
B6/B7/B8/B9 theater). B10 lands install-only entrypoint isolation.

**Evidence home:** `src/systems/self-host.md` SELF-HOST-PRODUCT-PATH-PERFORM;
`SelfApplyFs.lean`; `nix/systems-emit-wire/default.nix` + `emit-product.nix`.

### 11b9. Product path perform step -- **DONE (partial B10)**

**Name:** Product path perform step (Track 3 / claim **B** first perform step)

**Status:** **done (partial B10)** 2026-07-29. Acceptance
SELF-HOST-PRODUCT-PATH-PERFORM-STEP; SelfApplyFs `productPathPerformStepPartialReady`
(B9 perform gap + Lake-free install entrypoint claimed true + recipe
`install-freestanding-c-out` + perform claimed false + DependsOnLake true +
bootstrap remains + complete false + ownership claimed false); pure Nix
`productPathPerformStepGreen` (justfile Lake-free install-only tokens + host pin
entrypoint true + B9 when release present). Complete **stayed false**. Install-
only is **not** freestanding perform of read/compose/write. Remaining complete
gap: freestanding-capable FREESTANDING-PERFORM-GAP-READ-SSOT / COMPOSE / WRITE-HC
(not B6/B7/B8/B9/B10 theater).

**Evidence home:** `src/systems/self-host.md` SELF-HOST-PRODUCT-PATH-PERFORM-STEP;
`SelfApplyFs.lean`; `justfile` install-freestanding-c-out;
`nix/systems-emit-wire/default.nix` + `emit-product.nix`.

### 11b10. Host product-path READ SSOT -- **DONE (partial B11)**

**Name:** Host product-path READ SSOT (Track 3 / claim **B** READ-SSOT substrate)

**Status:** **done (partial B11)** 2026-07-29 (review APPROVE, 0 open issues).
Acceptance SELF-HOST-PRODUCT-PATH-PERFORM-READ; SelfApplyFs
`productPathPerformReadPartialReady` (B10 + ProductPathReadSsot
`readDualSsotAtRoot` + entrypoint claimed true + ReadDependsOnLake true +
perform claimed false + complete false + ownership claimed false); recipe
`just read-product-ssot` / lake exe `slake-read-product-ssot`; pure Nix
`productPathPerformReadGreen` (read module forbids FreestandingEmit write
symbols + B10 when release present). Complete **stayed false**. Host Lake
presence/token READ is **not** freestanding close of FREESTANDING-PERFORM-GAP-READ-SSOT
and is **not** dual SSOT equality. Remaining complete gap: freestanding-capable
READ (without classic Lean as product path) and/or COMPOSE / WRITE-HC without
Lake FreestandingEmit as product path (not B6..B11 theater).

**Evidence home:** `src/systems/self-host.md` SELF-HOST-PRODUCT-PATH-PERFORM-READ;
`SystemsLean/ProductPathReadSsot.lean`; `SelfApplyFs.lean`; `justfile`
read-product-ssot; `nix/systems-emit-wire/`.

### 11b11. Host product-path WRITE-HC -- **DONE (partial B13)**

**Name:** Host product-path WRITE-HC (Track 3 / claim **B** WRITE freestanding .h/.c substrate)

**Status:** **done (partial B13)** 2026-07-30. Acceptance
SELF-HOST-PRODUCT-PATH-PERFORM-WRITE-HC; SelfApplyFs
`productPathPerformWriteHcPartialReady` (B12 + ProductPathWriteHc
`writeFreestandingHcAtRoot` -> FreestandingEmit.emitAtRoot + entrypoint claimed
true + WriteHcDependsOnLake true + perform claimed false + complete false +
ownership claimed false); recipe `just write-freestanding-hc` / lake exe
`slake-write-freestanding-hc`; pure Nix `productPathPerformWriteHcGreen` (write
module requires FreestandingEmit import + emitAtRoot + B12 when release present;
forbids install-out theater + dishonest complete/perform/ownership true).
Complete **stayed false**. Host Lake WRITE-HC is **not** freestanding close of
FREESTANDING-PERFORM-GAP-WRITE-HC. Remaining complete gap: freestanding-capable
READ/COMPOSE/WRITE-HC without Lake FreestandingEmit as product path (not B6..B13 theater).

**Evidence home:** `src/systems/self-host.md` SELF-HOST-PRODUCT-PATH-PERFORM-WRITE-HC;
`SystemsLean/ProductPathWriteHc.lean`; `SelfApplyFs.lean`; `justfile`
write-freestanding-hc; `nix/systems-emit-wire/`.

### 11b12. Freestanding-capable gap measure -- **DONE (partial B14)**

**Name:** Freestanding-capable gap measure (Track 3 / claim **B** freestanding-capable step measure + ordered pipeline join)

**Status:** **done (partial B14)** 2026-07-30. Acceptance
SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-GAP; SelfApplyFs
`productPathFreestandingCapablePartialReady` (B13 + install-only true +
READ/COMPOSE/WRITE false + full perform false + perform claimed false + complete
false + ownership claimed false); recipes `just regenerate-product-path` (ordered
host join: read -> compose -> write-hc -> install) + `just freestanding-capable-gap`
/ lake exe `slake-freestanding-capable-gap`; module ProductPathFreestandingCapable;
pure Nix `productPathFreestandingCapableGreen` (gap module forbids dishonest
full/step true + B13 when release present). Complete **stayed false**. Gap measure
and host pipeline join are **not** freestanding close of FREESTANDING-PERFORM-GAP-*.
Remaining complete gap: freestanding-capable READ/COMPOSE/WRITE-HC without Lake
FreestandingEmit as product path (not B6..B14 theater).

**Evidence home:** `src/systems/self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-GAP;
`SystemsLean/ProductPathFreestandingCapable.lean`; `SelfApplyFs.lean`; `justfile`
regenerate-product-path + freestanding-capable-gap; `nix/systems-emit-wire/`.

### 11b13. Freestanding-capable step contract -- **DONE (partial B15)**

**Name:** Freestanding-capable step contract (Track 3 / claim **B** freestanding-capable step contract / interface)

**Status:** **done (partial B15)** 2026-07-30. Acceptance
SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-STEP-CONTRACT; SelfApplyFs
`productPathFreestandingCapableStepContractPartialReady` (B14 + authority-not-emit
true + READ/COMPOSE/WRITE/Full satisfied false + perform claimed false + complete
false + ownership claimed false); recipe `just freestanding-capable-step-contract`
/ lake exe `slake-freestanding-capable-step-contract`; module
ProductPathFreestandingCapableStepContract; pure Nix
`productPathFreestandingCapableStepContractGreen` (step contract module forbids
dishonest satisfied true + B14 when release present). Named
FREESTANDING-CAPABLE-STEP-CONTRACT-READ/COMPOSE/WRITE-HC + freestandingCapable*
API surface + BLOCKER-* (incl. FREESTANDING-MUST-OWN-REGENERATE). Complete
**stayed false**. Step contract is **not** freestanding close of
FREESTANDING-PERFORM-GAP-*. Remaining complete gap: satisfy freestanding-capable
READ then COMPOSE then WRITE-HC without Lake FreestandingEmit as product path
(not B6..B15 theater).

**Evidence home:** `src/systems/self-host.md` SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-STEP-CONTRACT;
`SystemsLean/ProductPathFreestandingCapableStepContract.lean`; `SelfApplyFs.lean`;
`justfile` freestanding-capable-step-contract; `nix/systems-emit-wire/`.

### 11c. Product residual free gate -- **DONE (measurement; free still false)**

**Name:** Product residual free gate (Track 4 / claim **A**)

**Status:** **done (measurement)** 2026-07-29. Acceptance
SELF-HOST-RESIDUAL-FREE-MEASURE; pure Nix PRODUCT-RESIDUAL-FREE-MEASURE on
`out/freestanding-c` when present; DualResidual measure cites +
`residualFreeClaimed` **stayed false**. Free flip not auto-queued.

**Evidence home:** `src/systems/self-host.md` SELF-HOST-RESIDUAL-FREE-MEASURE;
`DualResidual.lean`; `nix/systems-emit-wire/residual-free-measure.nix`.

---

## 12. One-line summary

Track 1 Mult..Out + banner **CLOSED**, Track 2 Mult..Out unit e2e **CLOSED**,
Dual SSOT **done**, Track 4 A free **measurement closed**, Track 3 B ladder
**B2..B40 partial** (ownership claimed **true**; perform claimed **true**;
StillUsesFreestandingEmit **false**; Full/complete **false**). Claim
**A free** and claim **B complete** stay **false**. **Out-first:** product =
`out/freestanding-c/`; `emit/` = host freestanding workspace. Living Open Name:
**Freestanding product self-host**. Next cliff: **Full** step-contract, then
complete only on full bar. Unknown finish horizon -- bar-driven, not stage-driven.
New Lean modules: short role names only; surgical rename only for the 19 long
basenames later. Free flip only per policy; llvm only after B complete (**E**);
CompCert when real (**D**); duals on named gaps (**F**); global proof complete
late (**C**).

---

## 13. Compaction reseed (where to look)

After chat compaction, reseed in this order for planned product work:

1. This file section **0** (living tip + finished B30..B40 ledger + unknown
   horizon + module names) + scorecard section 10 + defaults section 8
2. `WATCHER.md` (next implement tip -- Partial Full after B40 until advanced)
3. `RESIDUAL-systems.md` Open Name **Freestanding product self-host** + Done archive
4. `doc/SESSION-HANDOFF.md` Status + Decisions table
5. `src/systems/SystemsLean/SelfApplyFs.lean` claim-bool SSoT
6. `src/systems/self-host.md` (flip policy + B38/B39/B40 measured path)
7. `src/systems/emit/host-owned-emit.md` (ownership + Out-first + Dual SSOT note)
8. `AGENTS.md` Product Lean module names + Project agent skills + plan defaults

Plan defaults with checkable Done when **are** residual work: open the Name and
implement. Do **not** invent unrelated Names or forge free/complete/PROVABLY/llvm.
