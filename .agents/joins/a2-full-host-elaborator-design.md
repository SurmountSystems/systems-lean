# Join: A2 Full host elaborator design

**Status:** GREEN (design research + residual Open next; DualResidual not touched)
**Date:** 2026-08-03
**Program:** `.agents/plans/plan-slake-replaces-lake.md` section A2 / Step 2

## Goal met

Durable design maps every Lake host develop job under `src/systems/` to staged
Slake ownership. Six-unit Mult..Compose path stays foundation only. Ordered
residual queue opens next implementable Name. Dialect vs real Lean posture
documented. DualResidual host residual remains true / free claimed false
**unchanged**.

## Files changed

| Path | Change |
|------|--------|
| `doc/dev/research/full-host-elaborator-design-2026-08-03.md` | **New** A2 design (Kind: analysis / design): L1-L10 Lake jobs; staged ownership S1-S9; gap table foundation vs full package; queue A3..A-last; dialect hybrid recommendation |
| `RESIDUAL-systems.md` | A2 Done; Open primary **Package roots without Lake** (A3); P1 supporting remains open |
| `RESIDUAL.md` | Systems Open living tip + table: A2 done; Package roots without Lake open |
| `WATCHER.md` | `/implement` Package roots without Lake |
| `doc/SESSION-HANDOFF.md` | Active / Next / Open residual / recommended next reseed |
| `src/systems/self-host.md` | Living tip host finish line cites A2 design + next Name |
| `doc/dev/research/host-residual-free-bar-inventory-2026-08-03.md` | Next Name + A2 done pointers |
| `doc/dev/research/host-residual-free-design-2026-08-03.md` | Living tip after A2 |

## DualResidual unchanged (confirmed)

| Def | Value | File |
|-----|-------|------|
| `hostElaboratorResidualRemains` | `true` | `src/systems/SystemsLean/DualResidual.lean` |
| `hostElaboratorResidualFreeClaimed` | `false` | same |

No edit to DualResidual.lean. No host free flip. No product pin changes. No Lean
claim modules edited.

## Design summary (short)

1. **Lake jobs today:** package load, import graph, elaborate modules, theorems,
   library build, lean_exe tools (48), run tools, claim-proof lake, diagnostic
   lake recipes, bootstrap prebuild for foundation bins.
2. **Foundation (met):** HostTerm/Front/Check/Graph/PackageWrite + three
   without-Lake hot-path recipes for Mult..Compose dialect/package only.
3. **Gap:** real package roots, ~256-module graph/elaborate, theorems, tool
   ownership, day-to-day cold elaborate retire.
4. **Posture:** hybrid dialect harness + staged real-module coverage; not
   mathlib day-one; theorems **in** long checklist.
5. **Queue next:** Package roots without Lake (A3) -> Real Mult import graph ->
   First real host module check -> expand -> tools -> A-last free claim only.

## Open Names after join

1. **Package roots without Lake** (primary; A3)
2. **Host-cc helper tools without C printed from Lean** (supporting P1)

## Watcher next prompt text

```
/implement --effort 1 Package roots without Lake

Context: Finish line is Slake does everything Lake does for Systems Lean host
develop under src/systems/. A2 full host elaborator design is done
(doc/dev/research/full-host-elaborator-design-2026-08-03.md). This Name is A3:
first full-package host surface beyond the six-unit Mult..Compose foundation.
Load package library roots / module list for src/systems/ without calling Lake
on the measured step. Operator choice (a) full host develop. DualResidual host
residual remains true; host free claimed false -- do not flip. Product
free/complete/PROVABLY true; product Lake pins false. Supporting Open (may
parallel if disjoint): Host-cc helper tools without C printed from Lean (P1).

Done when: measured recipe green without Lake on that step (or freestanding
path); inventory lists roots or module-count source; HostResidualShrink or
successor cites coverage; DualResidual unchanged; free/complete/PROVABLY and
product Lake pins unchanged.

Out of scope: host free flip; full SystemsLean elaborate; full ~256-module
import graph; mathlib; forge complete/PROVABLY; product C growth as progress.

Gates: just hygiene; dual-pin presence if new modules; lake elaborator proof
if Lean claim / HostResidualShrink modules edited.

Join: .agents/joins/a3-package-roots-without-lake.md
```

## Gates

- DualResidual claim bools: confirmed unchanged via read (no DualResidual edit)
- No claim-bool Lean edits this slice (design + residual prose only)
- `just hygiene` / `just professional-tone`: recommended on worktree for novel
  markdown (research note + residual/handoff edits)

## GREEN criteria

| Criterion | Met |
|-----------|-----|
| Research note path | yes |
| Gap table + Lake job map + ordered queue | yes |
| Dialect posture documented | yes |
| Residual Open next Name checkable | Package roots without Lake |
| DualResidual untouched | yes |
