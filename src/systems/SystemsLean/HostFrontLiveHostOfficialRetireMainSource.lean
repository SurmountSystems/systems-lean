/-
  SYSTEMS_LEAN_HOST partial -- dual-pin live OfficialRetireMain.lean bytes.
  Side: classic Lean elaborator under src/systems/ (not freestanding C).
  Short role: HostFrontLiveHostOfficialRetireMainSource. Not occupancy name 50.
  Not mill 17 remill (just seventeenth-host-tool / inventory row 21). Mill stays 69 of 69.
  This wrap parses OfficialRetireMain.lean only. Do not wrap OfficialRetire.lean.
  Do not steal HostFrontLiveOfficialRetireMain (library companion).
  Do not wrap OfficialPathMain. Do not wrap OfficialPath.lean.
  DualEqWrite.lean does not exist and is not invented.
  CapableWrite.lean does not exist. HostResidualShrinkFacts.lean does not exist.
  Do not split DualPinOk.
  Unique needles use trailing newline so HostFrontLiveHostOfficialRetireMain is not a
  prefix hit on HostFrontLiveHostOfficialRetireMainSource.
  Occupancy leftover Term files are not this wrap.
  Unique needles (trailing newline so HostFrontLiveHostOfficialRetireMain is not a prefix):
  HostFrontLiveHostOfficialRetireMainSource
  PARSE-LIVE-OFFICIAL-RETIRE-MAIN
  HOST-FRONT-LIVE-OFFICIAL-RETIRE-MAIN
  OFFICIAL-RETIRE-MAIN
  HOST-OFFICIAL-RETIRE-MAIN
  Live product needles:
  OfficialRetireMain
  slake-freestanding-retire-official
  import SystemsLean.OfficialRetire
  productPathOfficialPathRetireOfficial
  OfficialRetire
  SKELETON
  Greppable: SYSTEMS_LEAN_HOST, liveOfficialRetireMainSource,
  UNIT_SURFACE host surface.
  Module: SystemsLean.HostFrontLiveHostOfficialRetireMainSource
  Not FullHost. Not freestanding residual free. Not PROVABLY.
  Module must stay ASCII.
-/

namespace SystemsLean.HostFrontLiveHostOfficialRetireMain

/-- Dual-pinned live OfficialRetireMain.lean bytes (must match on-disk file).
    Greppable: liveOfficialRetireMainSource, PARSE-LIVE-OFFICIAL-RETIRE-MAIN. -/
def liveOfficialRetireMainSource : String := r#"/-
  SYSTEMS_LEAN_HOST partial -- thin Lake exe main for RETIRE-OFFICIAL
  (SKELETON). Root for lake exe slake-freestanding-retire-official. Body lives in
  SystemsLean.OfficialRetire (Ok / PartialReady bulk). Thin main so
  SelfApplyFs may import the RETIRE-OFFICIAL API without top-level main clash
  (batch-5 SelfApplyFs split; same pattern as PerformClaimedMain /
  OwnershipClaimedMain / StepContractFullMain).
  Greppable: slake-freestanding-retire-official,
  OfficialRetireMain, productPathOfficialPathRetireOfficial, OfficialRetire,
  SYSTEMS_LEAN_HOST, SKELETON.
  Module must stay ASCII. Not freestanding residual free. Not PROVABLY.
-/

import SystemsLean.OfficialRetire

/-- Lake / lean --run entry: forward argv. -/
def main (args : List String) : IO UInt32 :=
  SystemsLean.OfficialRetire.main args
"#

end SystemsLean.HostFrontLiveHostOfficialRetireMain
