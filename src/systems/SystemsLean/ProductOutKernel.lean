/-
  SYSTEMS_LEAN_HOST partial -- freestanding product Out kernel evidence (B3).
  SLAKE_SELF_HOST_PRODUCT_OUT_V0 -- Out-first B-bar consumer evidence home
  out/freestanding-c/ + Mult..IR program freestanding API cites (not freestanding
  product self-host complete). Side: classic Lean elaborator under src/systems/
  (not freestanding C runtime).
  Honest scope: documents product Out as claim-B evidence home for greppable
  B-bar kernel APIs beyond B2 emit/out match alone. Distinct from B4 Lake
  bootstrap honesty (writer remains) and from B2 kernel self-apply inputs.
  Living tip SelfApplyFs owns claim-bool SSoT (perform/ownership/complete) and
  the B2 chain fold into this PartialReady. Dual-pin thin batch 2: this module
  owns historical stage/path/API/module-cite string defs (tip no longer
  duplicates them). This land-time module does NOT define those claim bools
  true (emit-wire forbid). Does not import FreestandingEmit (path cite strings
  only). Not residual free. Not PROVABLY. Not freestanding emit residual free.
  Theorems (PRODUCT-OUT-THEOREM / HOST-PRODUCT-OUT-THEOREM +
  PRODUCT-OUT-SMOKE / HOST-PRODUCT-OUT-SMOKE):
  SystemsLean.ProductOutKernelTheorems (same namespace; long-file split).
  theorem productOutKernelEvidencePartialReady_true.

  Greppable: SYSTEMS_LEAN_HOST, SLAKE_SELF_HOST_PRODUCT_OUT_V0,
  HOST-SELF-HOST-PRODUCT-OUT, SELF-HOST-PRODUCT-OUT,
  product Out kernel evidence, productOutKernelEvidencePartialReady,
  productOutKernelBarSurfaceOk, productOutPath, out/freestanding-c/,
  slake_mult_is_valid, slake_linear_consume, slake_erasure_is_runtime_absent,
  slake_type_tag, slake_ir_program,
  theorem productOutKernelEvidencePartialReady_true,
  PRODUCT-OUT-THEOREM, HOST-PRODUCT-OUT-THEOREM, PRODUCT-OUT-SMOKE,
  HOST-PRODUCT-OUT-SMOKE, ProductOutKernelTheorems,
  RUNTIME-FS, UNIT_SURFACE host surface, ProductOutKernel.
  Module: SystemsLean.ProductOutKernel
  Long-file split: PRODUCT-OUT-THEOREM + SMOKE in ProductOutKernelTheorems.
  Not freestanding emit residual free. Not residual free. Not PROVABLY.
  Module must stay ASCII.
-/

namespace SystemsLean.ProductOutKernel

/-- Greppable primary stage id (partial B3 product Out kernel evidence). -/
def stageId : String := "SLAKE_SELF_HOST_PRODUCT_OUT_V0"

/-- Greppable host map id. -/
def hostId : String := "HOST-SELF-HOST-PRODUCT-OUT"

/-- Greppable short map id. -/
def selfHostId : String := "SELF-HOST-PRODUCT-OUT"

/-- Module cite. Greppable: ProductOutKernel. -/
def productOutKernelModuleCite : String := "ProductOutKernel"

/-- Product Out path cite -- release surface (not host workspace emit/).
    Greppable: productOutPath, out/freestanding-c/, product Out. -/
def productOutPath : String := "out/freestanding-c/"

/-- Default B-bar product Out API cites (Mult+Linear+Erasure+Types+IR program).
    Greppable evidence tokens on product Out (not Lake-only substitutes). -/
def productOutApiMult : String := "slake_mult_is_valid"
def productOutApiLinear : String := "slake_linear_consume"
def productOutApiErasure : String := "slake_erasure_is_runtime_absent"
def productOutApiTypes : String := "slake_type_tag"
def productOutApiProgram : String := "slake_ir_program"

/-- Acceptance path cite. Greppable: acceptancePath. -/
def acceptancePath : String := "src/systems/self-host.md"

/-- productOutKernelBarSurfaceOk -- Out-first B-bar path + API cites + stage ids.
    Land-time surface only (no living-tip perform/ownership/complete claim bools
    on this module -- emit-wire forbids those true defs here).
    Greppable: productOutKernelBarSurfaceOk, SELF-HOST-PRODUCT-OUT. -/
def productOutKernelBarSurfaceOk : Bool :=
  (stageId == "SLAKE_SELF_HOST_PRODUCT_OUT_V0")
    && (hostId == "HOST-SELF-HOST-PRODUCT-OUT")
    && (selfHostId == "SELF-HOST-PRODUCT-OUT")
    && (productOutKernelModuleCite == "ProductOutKernel")
    && (productOutPath == "out/freestanding-c/")
    && (productOutApiMult == "slake_mult_is_valid")
    && (productOutApiLinear == "slake_linear_consume")
    && (productOutApiErasure == "slake_erasure_is_runtime_absent")
    && (productOutApiTypes == "slake_type_tag")
    && (productOutApiProgram == "slake_ir_program")
    && (acceptancePath == "src/systems/self-host.md")

/-- PRODUCT-OUT PartialReady fold (closed B3 substrate: Out-first B-bar surface).
    Historical readiness bulk for product Out kernel evidence. SelfApplyFs living
    tip re-exports a chain fold that includes B2 kernel self-apply PartialReady
    plus this def for greppable dual-pin. Land-time module pins: surface Ok only
    (living tip allows ownership / perform / stepContractFull / complete true
    after later cliffs). Does not flip residual free / llvm / PROVABLY.
    Greppable: productOutKernelEvidencePartialReady, SELF-HOST-PRODUCT-OUT,
    product Out kernel evidence. -/
def productOutKernelEvidencePartialReady : Bool :=
  productOutKernelBarSurfaceOk

/- Theorems + smoke live in SystemsLean.ProductOutKernelTheorems
   (same namespace SystemsLean.ProductOutKernel; long-file split).
   Parent keeps stage / Out path / B-bar API cites / Ok / PartialReady only --
   do not import ProductOutKernelTheorems here (import cycle).
   Greppable: theorem productOutKernelEvidencePartialReady_true,
   PRODUCT-OUT-THEOREM, ProductOutKernelTheorems. -/

end SystemsLean.ProductOutKernel

-- Theorems + smoke: SystemsLean.ProductOutKernelTheorems (same namespace).
-- No Lake exe for B3 (Out-first B-bar consumer evidence cites only; live product
-- wire remains FreestandingEmit + just build). No top-level main so
-- SelfApplyFs may import Ok / PartialReady bulk without main clash.
