/-
  SYSTEMS_LEAN_HOST partial -- dual-pin live CapableCompose.lean bytes.
  Side: classic Lean elaborator under src/systems/ (not freestanding C).
  Short role: HostFrontLiveCapableComposeSource. Not occupancy name 50. Not mill 70.
  This wrap is CapableCompose.lean. It is not CapableComposeTheorems.lean.
  Greppable: SYSTEMS_LEAN_HOST, liveCapableComposeSource,
  HOST-FRONT-LIVE-CAPABLE-COMPOSE, liveRel, UNIT_SURFACE host surface.
  Module: SystemsLean.HostFrontLiveCapableComposeSource
  Not FullHost. Not freestanding residual free. Not PROVABLY.
  Not a backend. FullBackend stays false in the wrap.
  Module must stay ASCII.
-/

namespace SystemsLean.HostFrontLiveCapableCompose

/-- Dual-pinned live CapableCompose.lean bytes (must match on-disk file).
    Greppable: liveCapableComposeSource, HOST-FRONT-LIVE-CAPABLE-COMPOSE. -/
def liveCapableComposeSource : String := r##"/-
  SYSTEMS_LEAN_HOST partial -- freestanding-capable COMPOSE plan/apply/body (B17).
  SLAKE_SELF_HOST_PRODUCT_PATH_FREESTANDING_CAPABLE_COMPOSE_V0 -- freestanding-capable
  product-path COMPOSE (FREESTANDING-CAPABLE-STEP-CONTRACT-COMPOSE) without
  FreestandingEmit as product authority. Side: classic Lean elaborator under
  src/systems/ (not freestanding C runtime).
  Real IO: freestandingCapableComposePlanApplyBody reads plan/apply/body dual SSOT
  (IO.FS.readFile + HOST-EMIT tokens + HEADER/BODY), builds ordered in-memory
  compose marker. No FreestandingEmit import; no emit-at-root / render / write.
  Deeper than B12 ProductPathComposePlan: HEADER/BODY on plan/apply/body. Not
  Mult..Apply template embed. Not freestanding perform claimed.
  Not freestanding emit residual free. Not residual free. Not PROVABLY.
  B14 productPathFreestandingCapableCompose true after B27 via Lake-free
  freestanding-capable-compose-lake-free; this module is Lake diagnostic
  (productPathFreestandingCapableComposeDependsOnLake true).
  productPathFreestandingCapableStepContractComposeSatisfied true (no-emit
  plan/apply/body structural compose). Full unsatisfied here; WriteHc is B18.
  Perform claimed false; complete false on this land-time home.
  Lake exe: slake-freestanding-capable-compose (just freestanding-capable-compose).
  Lake-free B27: just freestanding-capable-compose-lake-free.

  Theorems (COMPOSE-THEOREM / HOST-COMPOSE-THEOREM + COMPOSE-SMOKE /
  HOST-COMPOSE-SMOKE): SystemsLean.CapableComposeTheorems (same namespace;
  long-file split). theorem productPathFreestandingCapableComposePartialReady_true /
  productPathFreestandingCapableComposeOk_true / stageId_eq + surface pins.

  Greppable: SYSTEMS_LEAN_HOST,
  SLAKE_SELF_HOST_PRODUCT_PATH_FREESTANDING_CAPABLE_COMPOSE_V0,
  HOST-SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-COMPOSE,
  SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-COMPOSE,
  product path freestanding capable compose,
  productPathFreestandingCapableComposePartialReady,
  productPathFreestandingCapableComposeOk,
  freestandingCapableComposePlanApplyBody,
  FREESTANDING-CAPABLE-STEP-CONTRACT-COMPOSE,
  productPathFreestandingCapableStepContractComposeSatisfied,
  FREESTANDING-PERFORM-GAP-COMPOSE, WRITER-PATH-STEP-COMPOSE-PLAN-APPLY-BODY,
  host_emit_plan.ssot.txt, host_emit_apply.ssot.txt, host_emit_body_fragment.ssot.txt,
  freestanding-capable-compose, slake-freestanding-capable-compose,
  freestanding-capable-compose-lake-free,
  COMPOSE-THEOREM, HOST-COMPOSE-THEOREM, COMPOSE-SMOKE, HOST-COMPOSE-SMOKE,
  CapableComposeTheorems, theorem productPathFreestandingCapableComposePartialReady_true,
  CapableCompose, RUNTIME-FS, UNIT_SURFACE host surface.
  Module: SystemsLean.CapableCompose
  Dual-pin batch 16 home-primary; short-name batch 9 CapableCompose.
  Long-file split: COMPOSE-THEOREM + SMOKE in CapableComposeTheorems.
  Module must stay ASCII.
-/

namespace SystemsLean.CapableCompose

/-- Greppable primary stage id (partial B17 freestanding-capable COMPOSE). -/
def stageId : String :=
  "SLAKE_SELF_HOST_PRODUCT_PATH_FREESTANDING_CAPABLE_COMPOSE_V0"

/-- Greppable host map id. -/
def hostId : String :=
  "HOST-SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-COMPOSE"

/-- Greppable short map id. -/
def selfHostId : String :=
  "SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-COMPOSE"

/-- Named freestanding-capable step contract this step satisfies (partial).
    Greppable: FREESTANDING-CAPABLE-STEP-CONTRACT-COMPOSE. -/
def contractStepCompose : String := "FREESTANDING-CAPABLE-STEP-CONTRACT-COMPOSE"

/-- Named gap this step advances (not closed for full freestanding measure B14).
    Greppable: FREESTANDING-PERFORM-GAP-COMPOSE. -/
def gapCompose : String := "FREESTANDING-PERFORM-GAP-COMPOSE"

/-- Ordered B6 plan step id this substrate advances. Greppable:
    WRITER-PATH-STEP-COMPOSE-PLAN-APPLY-BODY. -/
def writerPathStepCompose : String := "WRITER-PATH-STEP-COMPOSE-PLAN-APPLY-BODY"

/-- Lake exe name for this freestanding-capable COMPOSE entrypoint. -/
def lakeExeName : String := "slake-freestanding-capable-compose"

/-- just recipe name (Lake-hosted diagnostic). -/
def justRecipeName : String := "freestanding-capable-compose"

/-- Lake-free freestanding-capable COMPOSE recipe (partial B27 CapableCompose).
    Greppable: freestanding-capable-compose-lake-free,
    productPathFreestandingCapableComposeLakeFreeRecipe. -/
def productPathFreestandingCapableComposeLakeFreeRecipe : String :=
  "freestanding-capable-compose-lake-free"

/-- Real freestanding-capable COMPOSE API name (implemented in this module).
    Greppable: freestandingCapableComposePlanApplyBody. -/
def freestandingCapableComposeApi : String :=
  "freestandingCapableComposePlanApplyBody"

/-- Product authority is not FreestandingEmit (this module never imports it).
    Greppable: productPathFreestandingCapableComposeAuthorityNotEmit. -/
def productPathFreestandingCapableComposeAuthorityNotEmit : Bool := true

/-- This B17 COMPOSE still runs as a classic Lean Lake exe host (honest diagnostic).
    B14 productPathFreestandingCapableCompose is true after B27 via separate Lake-
    free freestanding-capable-compose-lake-free measure. Greppable:
    productPathFreestandingCapableComposeDependsOnLake. -/
def productPathFreestandingCapableComposeDependsOnLake : Bool := true

/-- Plan/apply/body dual SSOT basename + token + HEADER/BODY + role. -/
structure ComposeSsotSpec where
  base : String
  token : String
  headerBlock : String
  bodyBlock : String
  role : String
  deriving Repr

/-- True when s contains needle as a contiguous substring (empty needle => false). -/
def containsStr (s needle : String) : Bool :=
  if needle.isEmpty then false
  else (s.splitOn needle).length > 1

/-- Multi-line block between "# NAME_BEGIN" and "# NAME_END" (markers excluded).
    Greppable structural dual SSOT shape (not Lean-fragment DUAL-SSOT-EQUALITY). -/
def ssotBlock (content name : String) : Option String :=
  let beginMark := "# " ++ name ++ "_BEGIN"
  let endMark := "# " ++ name ++ "_END"
  let rec go (grab : Bool) (acc : List String) : List String -> Option String
    | [] =>
      if grab || acc.isEmpty then none
      else some (String.intercalate "\n" acc.reverse ++ "\n")
    | line :: rest =>
      if !grab && line == beginMark then go true acc rest
      else if grab && line == endMark then
        some (String.intercalate "\n" acc.reverse ++ "\n")
      else if grab then go true (line :: acc) rest
      else go false acc rest
  go false [] (content.splitOn "\n")

/-- Fail closed when path missing. -/
def requireFile (path : System.FilePath) (label : String) : IO Unit := do
  unless (<- path.pathExists) do
    IO.eprintln s!"error: missing {label}: {path}"
    throw (IO.userError s!"missing {label}")

/-- Plan / apply / body dual SSOT only (compose substrate of B6 COMPOSE step).
    HEADER/BODY names are structural dual SSOT markers on disk -- not Lean embeds.
    Greppable: host_emit_plan.ssot.txt, host_emit_apply.ssot.txt,
    host_emit_body_fragment.ssot.txt, HOST-EMIT-PLAN, HOST-EMIT-APPLY, HOST-EMIT-BODY. -/
def planApplyBodySpecs : List ComposeSsotSpec := [
  { base := "host_emit_plan.ssot.txt", token := "HOST-EMIT-PLAN",
    headerBlock := "PLAN_C_HEADER", bodyBlock := "PLAN_C_BODY", role := "plan" },
  { base := "host_emit_apply.ssot.txt", token := "HOST-EMIT-APPLY",
    headerBlock := "APPLY_C_HEADER", bodyBlock := "APPLY_C_BODY", role := "apply" },
  { base := "host_emit_body_fragment.ssot.txt", token := "HOST-EMIT-BODY",
    headerBlock := "BODY_C_HEADER", bodyBlock := "BODY_C_BODY", role := "body" }
]

/-- Read one plan/apply/body dual SSOT: non-empty + greppable host token + non-empty
    HEADER/BODY structural blocks. Greppable: freestandingCapableComposeReadOneSsot,
    IO.FS.readFile (read-only for compose; no product write). -/
def freestandingCapableComposeReadOneSsot (path : System.FilePath)
    (spec : ComposeSsotSpec) : IO String := do
  requireFile path s!"dual SSOT ({spec.token})"
  let content <- IO.FS.readFile path
  if content.isEmpty then
    IO.eprintln s!"error: empty dual SSOT: {path}"
    throw (IO.userError s!"empty {path}")
  unless containsStr content spec.token do
    IO.eprintln s!"error: dual SSOT missing token {spec.token}: {path}"
    throw (IO.userError s!"missing token {spec.token}")
  match ssotBlock content spec.headerBlock with
  | none =>
    IO.eprintln s!"error: dual SSOT missing HEADER block {spec.headerBlock}: {path}"
    throw (IO.userError s!"missing HEADER {spec.headerBlock}")
  | some h =>
    if h.isEmpty then
      IO.eprintln s!"error: empty HEADER block {spec.headerBlock}: {path}"
      throw (IO.userError s!"empty HEADER {spec.headerBlock}")
  match ssotBlock content spec.bodyBlock with
  | none =>
    IO.eprintln s!"error: dual SSOT missing BODY block {spec.bodyBlock}: {path}"
    throw (IO.userError s!"missing BODY {spec.bodyBlock}")
  | some b =>
    if b.isEmpty then
      IO.eprintln s!"error: empty BODY block {spec.bodyBlock}: {path}"
      throw (IO.userError s!"empty BODY {spec.bodyBlock}")
  pure content

/-- Build ordered compose marker string from plan/apply/body contents.
    In-memory only -- not FreestandingEmit renderHeader/renderSource template embed,
    not product-wire write. Greppable: buildComposeMarker,
    WRITER-PATH-STEP-COMPOSE-PLAN-APPLY-BODY. -/
def buildComposeMarker (plan apply body : String) : String :=
  s!"{writerPathStepCompose}\n" ++
    s!"ROLE-plan:\n{plan}\n" ++
    s!"ROLE-apply:\n{apply}\n" ++
    s!"ROLE-body:\n{body}\n" ++
    s!"STAGE:{stageId}\n" ++
    s!"CONTRACT:{contractStepCompose}\n" ++
    s!"GAP:{gapCompose}\n"

/-- freestandingCapableComposePlanApplyBody -- freestanding-capable product-path
    COMPOSE of plan/apply/body dual SSOT under root/src/systems/emit without
    FreestandingEmit as product authority. Structural dual SSOT (token + HEADER/BODY)
    then ordered in-memory marker; not Mult..Apply template embed; not write.
    Greppable: freestandingCapableComposePlanApplyBody,
    FREESTANDING-CAPABLE-STEP-CONTRACT-COMPOSE,
    FREESTANDING-PERFORM-GAP-COMPOSE. -/
def freestandingCapableComposePlanApplyBody (root : System.FilePath) : IO Unit := do
  let emitDir := root / "src" / "systems" / "emit"
  IO.println s!"== {stageId}: freestanding-capable COMPOSE plan/apply/body =="
  IO.println s!"  host: {hostId} / {selfHostId}"
  IO.println s!"  contract: {contractStepCompose} (productPathFreestandingCapableStepContractComposeSatisfied)"
  IO.println s!"  gap: {gapCompose} (B14 CapableCompose true after B27 Lake-free freestanding-capable-compose-lake-free; this path is Lake exe diagnostic)"
  IO.println s!"  step: {writerPathStepCompose}"
  IO.println s!"  API: {freestandingCapableComposeApi}"
  IO.println "  authority: not FreestandingEmit (no import; no emit-at-root/render)"
  IO.println s!"    productPathFreestandingCapableComposeAuthorityNotEmit: {productPathFreestandingCapableComposeAuthorityNotEmit}"
  IO.println s!"    productPathFreestandingCapableComposeDependsOnLake: {productPathFreestandingCapableComposeDependsOnLake} (Lake exe host diagnostic; Lake-free measure is freestanding-capable-compose-lake-free)"
  IO.println "  depth: IO.FS.readFile + token + HEADER/BODY + ordered plan/apply/body marker"
  IO.println "  not: Mult..Apply template embed; not write; not perform claimed"
  let mut planText : String := ""
  let mut applyText : String := ""
  let mut bodyText : String := ""
  for spec in planApplyBodySpecs do
    let path := emitDir / spec.base
    let content <- freestandingCapableComposeReadOneSsot path spec
    IO.println s!"  compose input ok: {path} ({spec.token}; {spec.headerBlock}/{spec.bodyBlock}; {spec.role})"
    match spec.role with
    | "plan" => planText := content
    | "apply" => applyText := content
    | "body" => bodyText := content
    | other =>
      IO.eprintln s!"error: unknown compose role {other} for {spec.base}"
      throw (IO.userError s!"unknown compose role {other}")
  if planText.isEmpty || applyText.isEmpty || bodyText.isEmpty then
    IO.eprintln "error: plan/apply/body compose inputs incomplete"
    throw (IO.userError "incomplete compose inputs")
  let composed := buildComposeMarker planText applyText bodyText
  unless containsStr composed writerPathStepCompose do
    IO.eprintln s!"error: composed missing step id {writerPathStepCompose}"
    throw (IO.userError "missing compose step id")
  unless containsStr composed contractStepCompose do
    IO.eprintln s!"error: composed missing contract {contractStepCompose}"
    throw (IO.userError "missing compose contract id")
  unless containsStr composed "HOST-EMIT-PLAN" do
    IO.eprintln "error: composed missing HOST-EMIT-PLAN"
    throw (IO.userError "missing HOST-EMIT-PLAN in composed")
  unless containsStr composed "HOST-EMIT-APPLY" do
    IO.eprintln "error: composed missing HOST-EMIT-APPLY"
    throw (IO.userError "missing HOST-EMIT-APPLY in composed")
  unless containsStr composed "HOST-EMIT-BODY" do
    IO.eprintln "error: composed missing HOST-EMIT-BODY"
    throw (IO.userError "missing HOST-EMIT-BODY in composed")
  unless containsStr composed gapCompose do
    IO.eprintln s!"error: composed missing gap {gapCompose}"
    throw (IO.userError "missing compose gap id")
  unless containsStr composed stageId do
    IO.eprintln s!"error: composed missing stage {stageId}"
    throw (IO.userError "missing compose stage id")
  IO.println s!"GREEN {stageId}: freestanding-capable composed plan/apply/body marker ({composed.length} chars)"
  IO.println "  FREESTANDING-CAPABLE-STEP-CONTRACT-COMPOSE advanced (no-emit authority)"
  IO.println "  honest: Full unsatisfied; WriteHcSatisfied is B18; perform claimed false; complete false"
  IO.println "  B14 productPathFreestandingCapableCompose true after B27 (just freestanding-capable-compose-lake-free)"
  IO.println "  note: CapableWriteHc true after B28 freestanding-capable-write-hc-lake-free"

/-- Drop lake/exe separators so root path is first real arg. -/
def filterArgs : List String -> List String
  | [] => []
  | "--" :: rest => filterArgs rest
  | a :: rest => a :: filterArgs rest

/-- Surface ok: stage ids + authority-not-emit + named API + Lake dependency honest.
    Greppable: productPathFreestandingCapableComposeOk. -/
def productPathFreestandingCapableComposeOk : Bool :=
  (stageId == "SLAKE_SELF_HOST_PRODUCT_PATH_FREESTANDING_CAPABLE_COMPOSE_V0")
    && (hostId == "HOST-SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-COMPOSE")
    && (selfHostId == "SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-COMPOSE")
    && (contractStepCompose == "FREESTANDING-CAPABLE-STEP-CONTRACT-COMPOSE")
    && (gapCompose == "FREESTANDING-PERFORM-GAP-COMPOSE")
    && (writerPathStepCompose == "WRITER-PATH-STEP-COMPOSE-PLAN-APPLY-BODY")
    && (freestandingCapableComposeApi == "freestandingCapableComposePlanApplyBody")
    && (productPathFreestandingCapableComposeAuthorityNotEmit == true)
    && (productPathFreestandingCapableComposeDependsOnLake == true)
    && (lakeExeName == "slake-freestanding-capable-compose")
    && (justRecipeName == "freestanding-capable-compose")
    && (productPathFreestandingCapableComposeLakeFreeRecipe
        == "freestanding-capable-compose-lake-free")

/-- Land-time Full step-contract long-name pin (false at B17 land; living tip
    stepContractFull true after later cliffs). Greppable:
    productPathFreestandingCapableStepContractFullSatisfied. -/
def productPathFreestandingCapableStepContractFullSatisfied : Bool := false

/-- COMPOSE PartialReady fold (Ok + authority-not-emit + DependsOnLake + Full
    long-name false). Tip re-exports chain fold. Greppable:
    productPathFreestandingCapableComposePartialReady,
    SELF-HOST-PRODUCT-PATH-FREESTANDING-CAPABLE-COMPOSE,
    freestandingCapableComposePlanApplyBody. -/
def productPathFreestandingCapableComposePartialReady : Bool :=
  productPathFreestandingCapableComposeOk
    && productPathFreestandingCapableComposeAuthorityNotEmit
    && productPathFreestandingCapableComposeDependsOnLake
    && !productPathFreestandingCapableStepContractFullSatisfied

/-- CLI: optional repo root argument (default cwd). -/
def main (args : List String) : IO UInt32 := do
  let root : System.FilePath :=
    match filterArgs args with
    | r :: _ => System.FilePath.mk r
    | [] => "."
  try
    unless productPathFreestandingCapableComposeOk do
      IO.eprintln s!"error: {stageId} surface not ok"
      throw (IO.userError "productPathFreestandingCapableComposeOk false")
    freestandingCapableComposePlanApplyBody root
    pure 0
  catch e =>
    IO.eprintln s!"{e}"
    pure 1

/- Theorems + COMPOSE-SMOKE: SystemsLean.CapableComposeTheorems (same namespace).
   Parent does not import Theorems (import cycle). -/

end SystemsLean.CapableCompose

-- Lake entry: SystemsLean.CapableComposeMain (no top-level main; regenerate /
-- SelfApplyFs import API without main clash).
"##

end SystemsLean.HostFrontLiveCapableCompose
