/-
  SYSTEMS_LEAN_HOST partial -- dual-pin live OwnershipClaimed.lean bytes.
  Side: classic Lean elaborator under src/systems/ (not freestanding C).
  Short role: HostFrontLiveOwnershipClaimedSource. Not occupancy name 50. Not mill 70.
  This wrap parses OwnershipClaimed.lean only.
  Unique needles use trailing newline so HostFrontLiveOwnershipClaimed is not a
  prefix hit on HostFrontLiveOwnershipClaimedSource or a Theorems peel.
  Occupancy leftover is not this wrap.
  Occupancy stays 49. Mill stays 69 of 69. FullHost false.
  Unique needles (trailing newline so HostFrontLiveOwnershipClaimed is not a prefix):
  HostFrontLiveOwnershipClaimedSource
  PARSE-LIVE-OWNERSHIP-CLAIMED
  HOST-FRONT-LIVE-OWNERSHIP-CLAIMED
  OWNERSHIP-CLAIMED
  Greppable: SYSTEMS_LEAN_HOST, liveOwnershipClaimedSource,
  UNIT_SURFACE host surface.
  Module: SystemsLean.HostFrontLiveOwnershipClaimedSource
  Not FullHost. Not freestanding residual free. Not PROVABLY.
  Module must stay ASCII.
-/

namespace SystemsLean.HostFrontLiveOwnershipClaimed

/-- Dual-pinned live OwnershipClaimed.lean bytes (must match on-disk file).
    Greppable: liveOwnershipClaimedSource, PARSE-LIVE-OWNERSHIP-CLAIMED. -/
def liveOwnershipClaimedSource : String := r#"/-
  SYSTEMS_LEAN_HOST partial -- freestanding product path OWNERSHIP-CLAIMED (B40).
  SLAKE_SELF_HOST_PRODUCT_PATH_OWNERSHIP_CLAIMED_V0 -- greppable close-path step
  OWNERSHIP-CLAIMED beyond B39 PERFORM-CLAIMED. Evidence: perform-claimed + official
  dual-eq WRITE after B38 FreestandingEmit retirement. SelfApplyFs claim-bool SSoT
  productPathFreestandingOwnershipClaimed true (FULL-BAR-REQ-OWNERSHIP-CLAIMED-WITH-EVIDENCE
  closed). Measured via just freestanding-ownership-claimed. StillUsesLake /
  DependsOnLake true; StillUsesFreestandingEmit / Blocks / RetireRequired false.
  freestandingProductSelfHostComplete false (land-time). Full long-name false.
  free / llvm / PROVABLY false. CAPABLE-GAP closed. Lake exe
  slake-freestanding-ownership-claimed (just freestanding-ownership-claimed-measure).

  Theorems (OWNERSHIP-CLAIMED-THEOREM / HOST-OWNERSHIP-CLAIMED-THEOREM +
  OWNERSHIP-CLAIMED-SMOKE / HOST-OWNERSHIP-CLAIMED-SMOKE): live in
  SystemsLean.OwnershipClaimedTheorems (same namespace; long-file peel).
  theorem productPathFreestandingOwnershipClaimedPartialReady_true /
  productPathFreestandingOwnershipClaimedOk_true / stageId_eq / hostId_eq /
  selfHostId_eq + surface pins. Do not flip residual free / llvm / PROVABLY.

  Greppable: SYSTEMS_LEAN_HOST,
  SLAKE_SELF_HOST_PRODUCT_PATH_OWNERSHIP_CLAIMED_V0,
  HOST-SELF-HOST-PRODUCT-PATH-OWNERSHIP-CLAIMED,
  SELF-HOST-PRODUCT-PATH-OWNERSHIP-CLAIMED,
  product path freestanding ownership claimed,
  productPathFreestandingOwnershipClaimedMeasured,
  productPathFreestandingOwnershipClaimedPartialReady,
  productPathFreestandingOwnershipClaimedOk,
  productPathFreestandingOwnershipClaimedStepAdvanced,
  productPathFreestandingOwnershipClaimed,
  productPathFreestandingPerformClaimed, productPathPerformDependsOnLake,
  productPathOfficialPathRetireOfficialMeasured,
  productPathOfficialPathUsesDualEqualityWrite,
  productPathOfficialPathStillUsesFreestandingEmit,
  productPathOfficialPathStillUsesLake,
  productPathOfficialPathRetireFreestandingEmitRequired,
  productPathOfficialPathDualEqualityBlocksRetirement,
  productPathFreestandingPerformDualEqualityWriteCapableGapMeasured,
  FREESTANDING-OWNERSHIP-CLAIMED, FREESTANDING-OWNERSHIP-CLAIMED-MEASURED,
  FREESTANDING-OWNERSHIP-CLAIMED-WITH-PERFORM-CLAIMED-DUAL-EQ-WRITE,
  FREESTANDING-OWNERSHIP-CLAIMED-STEP-ADVANCED,
  FREESTANDING-DUAL-EQUALITY-WRITE-CLOSE-STEP-OWNERSHIP-CLAIMED,
  FREESTANDING-DUAL-EQUALITY-WRITE-CLOSE-STEP-PERFORM-CLAIMED,
  FULL-BAR-REQ-OWNERSHIP-CLAIMED-WITH-EVIDENCE,
  FULL-BAR-REQ-PERFORM-CLAIMED-WITH-EVIDENCE,
  OWNERSHIP-CLAIMED-THEOREM, HOST-OWNERSHIP-CLAIMED-THEOREM,
  OWNERSHIP-CLAIMED-SMOKE, HOST-OWNERSHIP-CLAIMED-SMOKE,
  OwnershipClaimedTheorems,
  theorem productPathFreestandingOwnershipClaimedPartialReady_true,
  just build, freestanding-capable-regenerate,
  freestandingCapableOrderedRegenerate, freestandingCapableWriteFreestandingHc,
  CapableWriteHc, CapableRegenerate, DUAL-SSOT-EQUALITY, requireDualSsotEqual,
  freestanding-ownership-claimed, freestanding-ownership-claimed-measure,
  slake-freestanding-ownership-claimed, OwnershipClaimed,
  freestanding-perform-claimed, PerformClaimed,
  freestanding-retire-official, OfficialRetire,
  freestanding-perform-dual-equality-write-capable-gap,
  RUNTIME-FS, UNIT_SURFACE host surface.
  Not freestanding emit residual free. Not residual free. Not PROVABLY.
  Module: SystemsLean.OwnershipClaimed
  Short role name (not ProductPathFreestanding*). Batch-3 PartialReady bulk here;
  tip re-exports. Dual-pin thin batch 31: Ok/stage/recipe/exe home-primary.
  Long-file peel: OWNERSHIP-CLAIMED-THEOREM + SMOKE in OwnershipClaimedTheorems.
  Module must stay ASCII.
-/

namespace SystemsLean.OwnershipClaimed

/-- Greppable primary stage id (partial B40 official OWNERSHIP-CLAIMED). -/
def stageId : String :=
  "SLAKE_SELF_HOST_PRODUCT_PATH_OWNERSHIP_CLAIMED_V0"

/-- Greppable host map id. -/
def hostId : String :=
  "HOST-SELF-HOST-PRODUCT-PATH-OWNERSHIP-CLAIMED"

/-- Greppable short map id. -/
def selfHostId : String :=
  "SELF-HOST-PRODUCT-PATH-OWNERSHIP-CLAIMED"

/-- B40 OWNERSHIP-CLAIMED measured (perform-claimed + official dual-eq
    WRITE evidence; SelfApplyFs claim-bool SSoT true). just freestanding-ownership-claimed
    dual-pins official dual-eq WRITE + B39 PERFORM-CLAIMED + ownership claimed true.
    Greppable: productPathFreestandingOwnershipClaimedMeasured,
    FREESTANDING-OWNERSHIP-CLAIMED. -/
def productPathFreestandingOwnershipClaimedMeasured : Bool := true

/-- Close-path step OWNERSHIP-CLAIMED advanced (Full next).
    Greppable: productPathFreestandingOwnershipClaimedStepAdvanced,
    FREESTANDING-OWNERSHIP-CLAIMED-STEP-ADVANCED,
    FREESTANDING-DUAL-EQUALITY-WRITE-CLOSE-STEP-OWNERSHIP-CLAIMED. -/
def productPathFreestandingOwnershipClaimedStepAdvanced : Bool := true

/-- Official product path uses freestanding dual-equality WRITE as authority. -/
def productPathOfficialPathUsesDualEqualityWrite : Bool := true

/-- productPathFreestandingOwnershipClaimed true with perform-claimed + dual-eq
    WRITE evidence. Claim-bool SSoT is SelfApplyFs; this module dual-pins true
    for Ok / pure Nix. Greppable: productPathFreestandingOwnershipClaimed. -/
def productPathFreestandingOwnershipClaimed : Bool := true

/-- B39 PERFORM-CLAIMED remains true (prerequisite evidence). -/
def productPathFreestandingPerformClaimed : Bool := true

/-- Official product path regenerate still DependsOnLake (Lake host elaborator for
    freestanding-capable-regenerate). Greppable: productPathPerformDependsOnLake. -/
def productPathPerformDependsOnLake : Bool := false

/-- B38 RETIRE-OFFICIAL remains measured (prerequisite substrate). -/
def productPathOfficialPathRetireOfficialMeasured : Bool := true

/-- B39 PERFORM-CLAIMED remains measured (prerequisite substrate). -/
def productPathFreestandingPerformClaimedMeasured : Bool := true

/-- B37 CAPABLE-GAP remains measured (prerequisite substrate). -/
def productPathFreestandingPerformDualEqualityWriteCapableGapMeasured : Bool := true

/-- Dual-equality WRITE parity gap stays closed after B37. -/
def productPathDualEqualityWriteParityGapOpen : Bool := false

/-- Capable WRITE dual-equality live (B37). -/
def productPathCapableWriteDualEqualityLive : Bool := true

/-- Official path no longer uses FreestandingEmit as product writer (B38). -/
def productPathOfficialPathStillUsesFreestandingEmit : Bool := false

/-- Official path still uses classic Lean Lake host elaborator. -/
def productPathOfficialPathStillUsesLake : Bool := false

/-- FreestandingEmit retired as official product authority (B38). -/
def productPathOfficialPathRetireFreestandingEmitRequired : Bool := false

/-- Dual-equality no longer blocks retirement. -/
def productPathOfficialPathDualEqualityBlocksRetirement : Bool := false

/-- Full freestanding-capable step-contract stays unsatisfied (next cliff). -/
def productPathFreestandingCapableStepContractFullSatisfied : Bool := false

/-- Claim B complete stays false. -/
def freestandingProductSelfHostComplete : Bool := false

/-- Named OWNERSHIP-CLAIMED tokens. -/
def ownershipClaimedId : String :=
  "FREESTANDING-OWNERSHIP-CLAIMED"
def ownershipClaimedMeasuredId : String :=
  "FREESTANDING-OWNERSHIP-CLAIMED-MEASURED"
def ownershipClaimedWithPerformClaimedDualEqWriteId : String :=
  "FREESTANDING-OWNERSHIP-CLAIMED-WITH-PERFORM-CLAIMED-DUAL-EQ-WRITE"
def ownershipClaimedStepAdvancedId : String :=
  "FREESTANDING-OWNERSHIP-CLAIMED-STEP-ADVANCED"

/-- Close-path step this partial advances / prior closed. -/
def closeStepOwnershipClaimed : String :=
  "FREESTANDING-DUAL-EQUALITY-WRITE-CLOSE-STEP-OWNERSHIP-CLAIMED"
def closeStepPerformClaimed : String :=
  "FREESTANDING-DUAL-EQUALITY-WRITE-CLOSE-STEP-PERFORM-CLAIMED"
def closeStepRetireOfficial : String :=
  "FREESTANDING-DUAL-EQUALITY-WRITE-CLOSE-STEP-RETIRE-OFFICIAL"
def closeStepCapableGap : String :=
  "FREESTANDING-DUAL-EQUALITY-WRITE-CLOSE-STEP-CAPABLE-GAP"

/-- Full-bar ownership claimed requirement (closed with evidence this slice). -/
def fullBarReqOwnershipClaimedWithEvidence : String :=
  "FULL-BAR-REQ-OWNERSHIP-CLAIMED-WITH-EVIDENCE"
def fullBarReqPerformClaimedWithEvidence : String :=
  "FULL-BAR-REQ-PERFORM-CLAIMED-WITH-EVIDENCE"

/-- Official product path dual-equality writer cites. -/
def officialPathRecipe : String := "build"
def officialPathWriter : String := "freestandingCapableOrderedRegenerate"
def officialPathLakeExe : String := "slake-freestanding-capable-regenerate"
def officialPathJustRecipe : String := "freestanding-capable-regenerate"
def dualEqualityGateId : String := "DUAL-SSOT-EQUALITY"
def dualEqualityApiName : String := "requireDualSsotEqual"
def capableWriteApi : String := "freestandingCapableWriteFreestandingHc"
def capableWriteModule : String := "CapableWriteHc"

/-- Lake-free OWNERSHIP-CLAIMED recipe (B40). -/
def lakeFreeOwnershipClaimedRecipe : String := "freestanding-ownership-claimed"

/-- Diagnostic Lake measure recipe. -/
def justMeasureRecipe : String := "freestanding-ownership-claimed-measure"
def lakeExeName : String := "slake-freestanding-ownership-claimed"

/-- B39 PERFORM-CLAIMED recipe (prerequisite). -/
def b39PerformClaimedRecipe : String := "freestanding-perform-claimed"

/-- B38 RETIRE-OFFICIAL recipe (prerequisite). -/
def b38RetireOfficialRecipe : String := "freestanding-retire-official"

/-- B37 CAPABLE-GAP recipe (prerequisite). -/
def b37CapableGapRecipe : String :=
  "freestanding-perform-dual-equality-write-capable-gap"

/-- OWNERSHIP-CLAIMED surface ok: measured true; step advanced; ownership claimed
    true; perform claimed true; official dual-eq WRITE true; StillUsesFreestandingEmit
    false; Blocks false; RetireRequired false; StillUsesLake true; DependsOnLake true;
    B39 measured; B38 measured; B37 measured; gap open false; dual-eq live; Full false;
    complete false.
    Greppable: productPathFreestandingOwnershipClaimedOk. -/
def productPathFreestandingOwnershipClaimedOk : Bool :=
  (stageId
      == "SLAKE_SELF_HOST_PRODUCT_PATH_OWNERSHIP_CLAIMED_V0")
    && (hostId
        == "HOST-SELF-HOST-PRODUCT-PATH-OWNERSHIP-CLAIMED")
    && (selfHostId
        == "SELF-HOST-PRODUCT-PATH-OWNERSHIP-CLAIMED")
    && (productPathFreestandingOwnershipClaimedMeasured == true)
    && (productPathFreestandingOwnershipClaimedStepAdvanced == true)
    && (productPathFreestandingOwnershipClaimed == true)
    && (productPathFreestandingPerformClaimed == true)
    && (productPathFreestandingPerformClaimedMeasured == true)
    && (productPathOfficialPathUsesDualEqualityWrite == true)
    && (productPathOfficialPathStillUsesFreestandingEmit == false)
    && (productPathOfficialPathStillUsesLake == false)
    && (productPathOfficialPathRetireFreestandingEmitRequired == false)
    && (productPathOfficialPathDualEqualityBlocksRetirement == false)
    && (productPathOfficialPathRetireOfficialMeasured == true)
    && (productPathPerformDependsOnLake == false)
    && (productPathFreestandingPerformDualEqualityWriteCapableGapMeasured == true)
    && (productPathDualEqualityWriteParityGapOpen == false)
    && (productPathCapableWriteDualEqualityLive == true)
    && (productPathFreestandingCapableStepContractFullSatisfied == false)
    && (freestandingProductSelfHostComplete == false)
    && (ownershipClaimedId == "FREESTANDING-OWNERSHIP-CLAIMED")
    && (ownershipClaimedMeasuredId
        == "FREESTANDING-OWNERSHIP-CLAIMED-MEASURED")
    && (ownershipClaimedWithPerformClaimedDualEqWriteId
        == "FREESTANDING-OWNERSHIP-CLAIMED-WITH-PERFORM-CLAIMED-DUAL-EQ-WRITE")
    && (ownershipClaimedStepAdvancedId
        == "FREESTANDING-OWNERSHIP-CLAIMED-STEP-ADVANCED")
    && (closeStepOwnershipClaimed
        == "FREESTANDING-DUAL-EQUALITY-WRITE-CLOSE-STEP-OWNERSHIP-CLAIMED")
    && (closeStepPerformClaimed
        == "FREESTANDING-DUAL-EQUALITY-WRITE-CLOSE-STEP-PERFORM-CLAIMED")
    && (closeStepRetireOfficial
        == "FREESTANDING-DUAL-EQUALITY-WRITE-CLOSE-STEP-RETIRE-OFFICIAL")
    && (closeStepCapableGap
        == "FREESTANDING-DUAL-EQUALITY-WRITE-CLOSE-STEP-CAPABLE-GAP")
    && (fullBarReqOwnershipClaimedWithEvidence
        == "FULL-BAR-REQ-OWNERSHIP-CLAIMED-WITH-EVIDENCE")
    && (fullBarReqPerformClaimedWithEvidence
        == "FULL-BAR-REQ-PERFORM-CLAIMED-WITH-EVIDENCE")
    && (officialPathRecipe == "build")
    && (officialPathWriter == "freestandingCapableOrderedRegenerate")
    && (officialPathLakeExe == "slake-freestanding-capable-regenerate")
    && (officialPathJustRecipe == "freestanding-capable-regenerate")
    && (dualEqualityGateId == "DUAL-SSOT-EQUALITY")
    && (dualEqualityApiName == "requireDualSsotEqual")
    && (capableWriteApi == "freestandingCapableWriteFreestandingHc")
    && (capableWriteModule == "CapableWriteHc")
    && (lakeFreeOwnershipClaimedRecipe == "freestanding-ownership-claimed")
    && (justMeasureRecipe == "freestanding-ownership-claimed-measure")
    && (lakeExeName == "slake-freestanding-ownership-claimed")
    && (b39PerformClaimedRecipe == "freestanding-perform-claimed")
    && (b38RetireOfficialRecipe == "freestanding-retire-official")
    && (b37CapableGapRecipe
        == "freestanding-perform-dual-equality-write-capable-gap")

/-- Print OWNERSHIP-CLAIMED measure (diagnostic; not Full / complete). -/
def printOwnershipClaimedMeasure : IO Unit := do
  IO.println s!"== {stageId}: freestanding product path ownership claimed (B40) =="
  IO.println s!"  host: {hostId} / {selfHostId}"
  IO.println s!"  productPathFreestandingOwnershipClaimedMeasured: {productPathFreestandingOwnershipClaimedMeasured} ({ownershipClaimedId})"
  IO.println "  OWNERSHIP-CLAIMED with perform-claimed + official dual-eq WRITE evidence:"
  IO.println s!"    step advanced: {productPathFreestandingOwnershipClaimedStepAdvanced} ({ownershipClaimedStepAdvancedId})"
  IO.println s!"    ownership claimed: {productPathFreestandingOwnershipClaimed} ({ownershipClaimedWithPerformClaimedDualEqWriteId})"
  IO.println s!"    perform claimed: {productPathFreestandingPerformClaimed} (B39 evidence)"
  IO.println s!"    uses dual-eq WRITE: {productPathOfficialPathUsesDualEqualityWrite}"
  IO.println s!"    close-path step: {closeStepOwnershipClaimed}"
  IO.println s!"    prior PERFORM-CLAIMED: {closeStepPerformClaimed} (closed B39)"
  IO.println s!"    prior RETIRE-OFFICIAL: {closeStepRetireOfficial} (closed B38)"
  IO.println s!"    prior CAPABLE-GAP: {closeStepCapableGap} (closed B37)"
  IO.println s!"    Capable WRITE API: {capableWriteApi}"
  IO.println s!"    Capable WRITE module: {capableWriteModule}"
  IO.println s!"    gate: {dualEqualityGateId} / {dualEqualityApiName}"
  IO.println "  official product path freestanding dual-eq writer (FreestandingEmit retired B38):"
  IO.println s!"    recipe: {officialPathRecipe}"
  IO.println s!"    writer: {officialPathWriter}"
  IO.println s!"    Lake exe: {officialPathLakeExe}"
  IO.println s!"    just recipe: {officialPathJustRecipe}"
  IO.println s!"    still FreestandingEmit: {productPathOfficialPathStillUsesFreestandingEmit} (false = retired)"
  IO.println s!"    still Lake: {productPathOfficialPathStillUsesLake}"
  IO.println s!"    retire FreestandingEmit required: {productPathOfficialPathRetireFreestandingEmitRequired} (false = retired)"
  IO.println s!"    dual-equality blocks retirement: {productPathOfficialPathDualEqualityBlocksRetirement} (false = not blocked)"
  IO.println s!"  productPathFreestandingPerformClaimedMeasured: {productPathFreestandingPerformClaimedMeasured} (B39 remains)"
  IO.println s!"  productPathOfficialPathRetireOfficialMeasured: {productPathOfficialPathRetireOfficialMeasured} (B38 remains)"
  IO.println s!"  productPathFreestandingPerformDualEqualityWriteCapableGapMeasured: {productPathFreestandingPerformDualEqualityWriteCapableGapMeasured} (B37 remains)"
  IO.println s!"  productPathDualEqualityWriteParityGapOpen: {productPathDualEqualityWriteParityGapOpen} (false = CAPABLE-GAP closed)"
  IO.println s!"  productPathCapableWriteDualEqualityLive: {productPathCapableWriteDualEqualityLive}"
  IO.println s!"  productPathPerformDependsOnLake: {productPathPerformDependsOnLake}"
  IO.println s!"  Full-bar req closed with evidence: {fullBarReqOwnershipClaimedWithEvidence}"
  IO.println s!"  prior Full-bar perform req: {fullBarReqPerformClaimedWithEvidence}"
  IO.println s!"  productPathFreestandingCapableStepContractFullSatisfied: {productPathFreestandingCapableStepContractFullSatisfied} (stays false; Full next)"
  IO.println s!"  freestandingProductSelfHostComplete: {freestandingProductSelfHostComplete}"
  IO.println s!"  Lake-free OWNERSHIP-CLAIMED recipe: just {lakeFreeOwnershipClaimedRecipe}"
  IO.println s!"  measure: just {justMeasureRecipe} / lake exe {lakeExeName}"
  IO.println s!"  B39 PERFORM-CLAIMED recipe: just {b39PerformClaimedRecipe}"
  IO.println s!"  B38 RETIRE-OFFICIAL recipe: just {b38RetireOfficialRecipe}"
  IO.println s!"  B37 CAPABLE-GAP recipe: just {b37CapableGapRecipe}"
  IO.println "  honest: OWNERSHIP-CLAIMED != Full / complete; free/llvm/PROVABLY false"
  IO.println "  honest: CAPABLE-GAP closed; land-time complete false; short name OwnershipClaimed"
  if productPathFreestandingOwnershipClaimedOk then
    IO.println s!"GREEN {stageId}: OWNERSHIP-CLAIMED surface ok (measured; ownership+perform claimed; dual-eq WRITE; StillUsesFreestandingEmit false; DependsOnLake true; Full false; complete false)"
  else
    IO.eprintln s!"error: {stageId} OWNERSHIP-CLAIMED surface not ok"
    throw (IO.userError "productPathFreestandingOwnershipClaimedOk false")

/-- OWNERSHIP-CLAIMED PartialReady fold (closed B40). SelfApplyFs tip re-exports
    chain fold. Land-time: Full long-name false; complete false. Does not flip
    residual free / llvm / PROVABLY.
    Greppable: productPathFreestandingOwnershipClaimedPartialReady,
    SELF-HOST-PRODUCT-PATH-OWNERSHIP-CLAIMED, FREESTANDING-OWNERSHIP-CLAIMED. -/
def productPathFreestandingOwnershipClaimedPartialReady : Bool :=
  productPathFreestandingOwnershipClaimedOk
    && productPathFreestandingOwnershipClaimedMeasured
    && productPathFreestandingOwnershipClaimedStepAdvanced
    && productPathFreestandingOwnershipClaimed
    && productPathFreestandingPerformClaimed
    && productPathOfficialPathUsesDualEqualityWrite
    && !productPathOfficialPathStillUsesFreestandingEmit
    && !productPathOfficialPathStillUsesLake
    && !productPathOfficialPathRetireFreestandingEmitRequired
    && !productPathOfficialPathDualEqualityBlocksRetirement
    && productPathOfficialPathRetireOfficialMeasured
    && productPathFreestandingPerformClaimedMeasured
    && !productPathPerformDependsOnLake
    && !productPathFreestandingCapableStepContractFullSatisfied
    && !freestandingProductSelfHostComplete

/-- CLI: print OWNERSHIP-CLAIMED measure. Fail-closed if Ok false. -/
def main (_args : List String) : IO UInt32 := do
  try
    printOwnershipClaimedMeasure
    pure 0
  catch e =>
    IO.eprintln s!"{e}"
    pure 1

end SystemsLean.OwnershipClaimed

-- Theorems + smoke: SystemsLean.OwnershipClaimedTheorems (same namespace).
-- Lake entry: SystemsLean.OwnershipClaimedMain (no top-level main clash).
"#

end SystemsLean.HostFrontLiveOwnershipClaimed

