/-
  SYSTEMS_LEAN_HOST partial -- thin driver for PARSE-LIVE-SLAKE-HOST-TOOL-ELF-MAIN-CATCH-DEST.
  Root for optional lean --run of live SlakeHostToolElfMainCatchDest.lean parse + kernelCheck.
  Body lives in SystemsLean.HostFrontLiveSlakeHostToolElfMainCatchDest.
  Greppable: SystemsLean.HostFrontLiveSlakeHostToolElfMainCatchDestMain,
  HostFrontLiveSlakeHostToolElfMainCatchDestMain,
  HOST-FRONT-LIVE-SLAKE-HOST-TOOL-ELF-MAIN-CATCH-DEST,
  SLAKE_HOST_FRONT_LIVE_SLAKE_HOST_TOOL_ELF_MAIN_CATCH_DEST_V0,
  PARSE-LIVE-SLAKE-HOST-TOOL-ELF-MAIN-CATCH-DEST,
  parseLiveSlakeHostToolElfMainCatchDestSource, kernelCheckLiveSlakeHostToolElfMainCatchDestSource,
  SYSTEMS_LEAN_HOST.
  Module: SystemsLean.HostFrontLiveSlakeHostToolElfMainCatchDestMain
  Not mill 70. Not occupancy name 50. Not FullHost. Occupancy stays 49. Mill stays 69 of 69.
  Module must stay ASCII. Not freestanding residual free. Not PROVABLY. Not Lake-gone.
  FullBackend stays false. slakeOwnsPackageTypecheck stays false. SKELETON.
-/

import SystemsLean.HostFrontLiveSlakeHostToolElfMainCatchDest

/-- lean --run entry: forward argv (repo root). -/
def main (args : List String) : IO UInt32 :=
  SystemsLean.HostFrontLiveSlakeHostToolElfMainCatchDest.main args
