/-
  SYSTEMS_LEAN_HOST partial -- freestanding product writer path execution (B7).
  SLAKE_SELF_HOST_PRODUCT_WRITER_PATH_EXEC_V0 -- host FreestandingEmit +
  just build **execution cites** substrate (function names / recipe /
  Out path), not freestanding product path ownership without classic Lean.
  Side: classic Lean elaborator under src/systems/ (not freestanding C runtime).
  Honest scope: documents exact host execution cites for B6 ordered plan steps
  (read SSOT, compose, write .h/.c, install Out) that Lake FreestandingEmit +
  just build perform today. This is **execution evidence + non-claim
  bar**, NOT freestanding product path complete / free / ownership claimed.
  Lake FreestandingEmit still writes full text today (B4 remains on living tip).
  Living tip SelfApplyFs owns claim-bool SSoT (perform/ownership/complete).
  This land-time module does NOT define those claim bools true (emit-wire forbid).
  Dual-pin thin batch 6: historical stage/fn/module-cite dual-pins live here;
  tip keeps chain fold only. Not residual free. Not PROVABLY.
  Not freestanding emit residual free.
  Theorems (WRITER-PATH-EXEC-THEOREM / HOST-WRITER-PATH-EXEC-THEOREM +
  WRITER-PATH-EXEC-SMOKE / HOST-WRITER-PATH-EXEC-SMOKE):
  SystemsLean.ProductPathWriterPathExecTheorems (same namespace; long-file split).
  theorem productWriterPathExecutionPartialReady_true /
  productWriterPathExecutionOk_true / stageId_eq + surface pins.

  Greppable: SYSTEMS_LEAN_HOST, SLAKE_SELF_HOST_PRODUCT_WRITER_PATH_EXEC_V0,
  HOST-SELF-HOST-PRODUCT-WRITER-PATH-EXEC, SELF-HOST-PRODUCT-WRITER-PATH-EXEC,
  product writer path execution, productWriterPathExecutionOk,
  productWriterPathExecutionPartialReady, productWriterPathExecReadSsotFn,
  productWriterPathExecComposeFn, productWriterPathExecWriteFn,
  productWriterPathExecEmitEntry, productWriterPathExecInstallRecipe,
  productWriterPathExecDualSsot, loadMultSsot, loadBodySsot, loadPlanSsot,
  requireDualSsotEqual, renderHeader, renderSource, emitAtRoot,
  IO.FS.writeFile, just build, FreestandingEmit,
  slake-emit-freestanding-c, SLAKE_EMIT_FREESTANDING_C_V0,
  theorem productWriterPathExecutionPartialReady_true,
  WRITER-PATH-EXEC-THEOREM, HOST-WRITER-PATH-EXEC-THEOREM,
  WRITER-PATH-EXEC-SMOKE, HOST-WRITER-PATH-EXEC-SMOKE,
  ProductPathWriterPathExecTheorems,
  RUNTIME-FS, UNIT_SURFACE host surface, ProductPathWriterPathExec.
  Module: SystemsLean.ProductPathWriterPathExec
  Long-file split: WRITER-PATH-EXEC-THEOREM + SMOKE in ProductPathWriterPathExecTheorems.
  Not freestanding emit residual free. Not residual free. Not PROVABLY.
  Module must stay ASCII.
-/

namespace SystemsLean.ProductPathWriterPathExec

/-- Greppable primary stage id (partial B7 writer path execution substrate). -/
def stageId : String := "SLAKE_SELF_HOST_PRODUCT_WRITER_PATH_EXEC_V0"

/-- Greppable host map id. -/
def hostId : String := "HOST-SELF-HOST-PRODUCT-WRITER-PATH-EXEC"

/-- Greppable short map id. -/
def selfHostId : String := "SELF-HOST-PRODUCT-WRITER-PATH-EXEC"

/-- Module cite. Greppable: ProductPathWriterPathExec. -/
def writerPathExecModuleCite : String := "ProductPathWriterPathExec"

/-- Host path performs WRITER-PATH-STEP-READ-SSOT today via FreestandingEmit load*Ssot.
    Greppable: productWriterPathExecReadSsotFn, loadMultSsot (exec cite, not plan id). -/
def productWriterPathExecReadSsotFn : String := "loadMultSsot"

/-- Body SSOT load cite (read-step companion; Mult..Body load*Ssot family).
    Greppable: productWriterPathExecReadBodyFn, loadBodySsot. -/
def productWriterPathExecReadBodyFn : String := "loadBodySsot"

/-- Plan SSOT load cite (compose inputs for plan/apply product text).
    Greppable: productWriterPathExecReadPlanFn, loadPlanSsot. -/
def productWriterPathExecReadPlanFn : String := "loadPlanSsot"

/-- Host path dual SSOT equality gate during read step.
    Greppable: productWriterPathExecDualSsot, requireDualSsotEqual. -/
def productWriterPathExecDualSsot : String := "requireDualSsotEqual"

/-- Host path performs WRITER-PATH-STEP-COMPOSE-PLAN-APPLY-BODY via renderHeader.
    Greppable: productWriterPathExecComposeFn, renderHeader. -/
def productWriterPathExecComposeFn : String := "renderHeader"

/-- Companion compose source path (renderSource embeds Mult..Body into .c).
    Greppable: productWriterPathExecComposeSourceFn, renderSource. -/
def productWriterPathExecComposeSourceFn : String := "renderSource"

/-- Host path performs WRITER-PATH-STEP-WRITE-FREESTANDING-HC via IO.FS.writeFile.
    Greppable: productWriterPathExecWriteFn, IO.FS.writeFile. -/
def productWriterPathExecWriteFn : String := "IO.FS.writeFile"

/-- FreestandingEmit emit entry that sequences read/compose/write.
    Greppable: productWriterPathExecEmitEntry, emitAtRoot. -/
def productWriterPathExecEmitEntry : String := "emitAtRoot"

/-- Host path performs WRITER-PATH-STEP-INSTALL-OUT via just build.
    Greppable: productWriterPathExecInstallRecipe, just build. -/
def productWriterPathExecInstallRecipe : String := "build"

/-- Product Out path cite. Greppable: productOutPath, out/freestanding-c/. -/
def productOutPath : String := "out/freestanding-c/"

/-- Host Lake emit writer path cite (classic Lean FreestandingEmit).
    Greppable: hostLakeEmitWriterPath, FreestandingEmit. -/
def hostLakeEmitWriterPath : String := "src/systems/SystemsLean/FreestandingEmit.lean"

/-- Host Lake emit exe cite. Greppable: hostLakeEmitExe, slake-emit-freestanding-c. -/
def hostLakeEmitExe : String := "slake-emit-freestanding-c"

/-- Freestanding emit stage cite still owned by host Lake writer.
    Greppable: hostLakeEmitStageCite, SLAKE_EMIT_FREESTANDING_C_V0. -/
def hostLakeEmitStageCite : String := "SLAKE_EMIT_FREESTANDING_C_V0"

/-- Acceptance path cite. Greppable: acceptancePath. -/
def acceptancePath : String := "src/systems/self-host.md"

/-- productWriterPathExecutionOk -- B7 stage ids + host execution cites (function
    names / recipe / Out path) + Lake emit path cites. Cites are DISTINCT from
    mere WRITER-PATH-STEP-* plan ids (plan-id re-list theater banned).
    Land-time surface only (no living-tip perform/ownership/complete claim
    bools on this module -- emit-wire forbids those true defs here). Greppable:
    productWriterPathExecutionOk, SELF-HOST-PRODUCT-WRITER-PATH-EXEC,
    product writer path execution. -/
def productWriterPathExecutionOk : Bool :=
  (stageId == "SLAKE_SELF_HOST_PRODUCT_WRITER_PATH_EXEC_V0")
    && (hostId == "HOST-SELF-HOST-PRODUCT-WRITER-PATH-EXEC")
    && (selfHostId == "SELF-HOST-PRODUCT-WRITER-PATH-EXEC")
    && (writerPathExecModuleCite == "ProductPathWriterPathExec")
    && (productWriterPathExecReadSsotFn == "loadMultSsot")
    && (productWriterPathExecReadBodyFn == "loadBodySsot")
    && (productWriterPathExecReadPlanFn == "loadPlanSsot")
    && (productWriterPathExecDualSsot == "requireDualSsotEqual")
    && (productWriterPathExecComposeFn == "renderHeader")
    && (productWriterPathExecComposeSourceFn == "renderSource")
    && (productWriterPathExecWriteFn == "IO.FS.writeFile")
    && (productWriterPathExecEmitEntry == "emitAtRoot")
    && (productWriterPathExecInstallRecipe == "build")
    && (productOutPath == "out/freestanding-c/")
    && (hostLakeEmitWriterPath == "src/systems/SystemsLean/FreestandingEmit.lean")
    && (hostLakeEmitExe == "slake-emit-freestanding-c")
    && (hostLakeEmitStageCite == "SLAKE_EMIT_FREESTANDING_C_V0")
    && (acceptancePath == "src/systems/self-host.md")

/-- WRITER-PATH-EXEC PartialReady fold (closed B7 substrate: Ok execution cites).
    Historical readiness bulk for writer path execution evidence. SelfApplyFs
    living tip re-exports a chain fold that includes this def for greppable
    dual-pin + B6 plan PartialReady chain. Land-time module pins: Ok surface
    only (living tip allows ownership / perform / stepContractFull / complete
    true after later cliffs). Does not flip residual free / llvm / PROVABLY.
    Greppable: productWriterPathExecutionPartialReady,
    SELF-HOST-PRODUCT-WRITER-PATH-EXEC, product writer path execution. -/
def productWriterPathExecutionPartialReady : Bool :=
  productWriterPathExecutionOk

/- Theorems + smoke live in SystemsLean.ProductPathWriterPathExecTheorems
   (same namespace SystemsLean.ProductPathWriterPathExec; long-file split).
   Parent keeps stage / cites / Ok / PartialReady only -- do not import
   ProductPathWriterPathExecTheorems here (import cycle).
   Greppable: theorem productWriterPathExecutionPartialReady_true,
   WRITER-PATH-EXEC-THEOREM, ProductPathWriterPathExecTheorems. -/

end SystemsLean.ProductPathWriterPathExec

-- Theorems + smoke: SystemsLean.ProductPathWriterPathExecTheorems (same namespace).
-- No Lake exe for B7 (execution cites + honesty pins only; host Lake
-- FreestandingEmit + just build remain the live performers).
-- No top-level main so SelfApplyFs may import Ok / PartialReady bulk without
-- main clash.
