/-
  SYSTEMS_LEAN_HOST partial -- real Mult import graph without Lake on the
  measured step (plan-slake-replaces-lake A4 / design S2 Import graph).
  Side: classic Lean elaborator under src/systems/ (not freestanding C).
  Long-file peel import hub: HostImportGraphSeeds; HostImportGraphModel;
  HostImportGraphMods; HostImportGraphModsLater; HostImportGraphLoadOk;
  HostImportGraphTheorems; HostImportGraphWalkLater; HostImportGraphWalk;
  HostImportGraphDriver.
  Same namespace SystemsLean.HostImportGraph.

  Intentional non-claims (local to this family; do not misread DualResidual):
  - PARTIAL-STRUCTURAL only -- not full elaborator typecheck.
  - Local stillUsesLake / host residual remains pins are host-check honesty;
    DualResidual free claimed true / remains false lives on DualResidual.
  - Not freestanding residual free. Not PROVABLY. Not llvm unlock. Not full
    elaborator parity. Host elaborator residual remains (Lake bootstrap).

  Greppable: SYSTEMS_LEAN_HOST, HOST-IMPORT-GRAPH, HostImportGraph,
  HostImportGraphSeeds, HostImportGraphModel, HostImportGraphMods,
  HostImportGraphModsLater, HostImportGraphLoadOk, HostImportGraphTheorems,
  HostImportGraphWalkLater, HostImportGraphWalk, HostImportGraphDriver,
  SLAKE_HOST_IMPORT_GRAPH_V0,
  PARTIAL-STRUCTURAL, UNIT_SURFACE host surface, MULT-0, MULT-1, MULT-OMEGA (peer Mult-first cite).
  Module: SystemsLean.HostImportGraph
  Red/green: just systems-host; lake build SystemsLean.HostImportGraph;
  just host-import-graph-without-lake.
  Module must stay ASCII.
  Not freestanding residual free. Not freestanding emit residual free. Not PROVABLY.
  Host elaborator residual remains.
-/

import SystemsLean.HostImportGraphSeeds
import SystemsLean.HostImportGraphModel
import SystemsLean.HostImportGraphMods
import SystemsLean.HostImportGraphModsLater
import SystemsLean.HostImportGraphLoadOk
import SystemsLean.HostImportGraphTheorems
import SystemsLean.HostImportGraphWalkLater
import SystemsLean.HostImportGraphWalk
import SystemsLean.HostImportGraphDriver

namespace SystemsLean.HostImportGraph

-- Import hub only (defs live in companions listed above).

end SystemsLean.HostImportGraph
