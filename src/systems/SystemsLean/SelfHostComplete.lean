/-
  SYSTEMS_LEAN_HOST partial -- freestanding product self-host complete (claim B).
  SLAKE_SELF_HOST_FREESTANDING_PRODUCT_COMPLETE_V0 -- greppable close-path
  complete beyond Full step-contract. Evidence: stepContractFull true (Full) +
  ownership-claimed + perform-claimed + official dual-eq WRITE. SelfApplyFs
  claim-bool SSoT freestandingProductSelfHostComplete true. Measured via just
  freestanding-self-host-complete. StillUsesLake / DependsOnLake false after
  M6 product-path Lake pins flip (host elaborator residual may remain);
  StillUsesFreestandingEmit / Blocks / RetireRequired false. residual free /
  llvm / PROVABLY false. CAPABLE-GAP closed. Lake exe
  slake-freestanding-self-host-complete (just freestanding-self-host-complete-measure).

  Theorems (COMPLETE-THEOREM / HOST-COMPLETE-THEOREM +
  COMPLETE-SMOKE / HOST-COMPLETE-SMOKE): live in
  SystemsLean.SelfHostCompleteTheorems (same namespace; long-file peel).
  theorem freestandingProductSelfHostCompletePartialReady_true /
  freestandingProductSelfHostCompleteOk_true / stageId_eq / hostId_eq /
  selfHostId_eq + surface pins. Do not flip residual free / llvm / PROVABLY.

  Greppable: SYSTEMS_LEAN_HOST,
  SLAKE_SELF_HOST_FREESTANDING_PRODUCT_COMPLETE_V0,
  HOST-SELF-HOST-FREESTANDING-PRODUCT-COMPLETE,
  SELF-HOST-FREESTANDING-PRODUCT-COMPLETE,
  freestanding product self-host complete,
  freestandingProductSelfHostCompleteMeasured,
  freestandingProductSelfHostCompletePartialReady,
  freestandingProductSelfHostCompleteOk,
  freestandingProductSelfHostCompleteStepAdvanced,
  freestandingProductSelfHostComplete, freestandingProductSelfHostComplete_true,
  stepContractFull, productPathFreestandingOwnershipClaimed,
  productPathFreestandingPerformClaimed, productPathPerformDependsOnLake,
  productPathOfficialPathRetireOfficialMeasured,
  productPathOfficialPathUsesDualEqualityWrite,
  productPathOfficialPathStillUsesFreestandingEmit,
  productPathOfficialPathStillUsesLake,
  productPathOfficialPathRetireFreestandingEmitRequired,
  productPathOfficialPathDualEqualityBlocksRetirement,
  productPathFreestandingPerformDualEqualityWriteCapableGapMeasured,
  FREESTANDING-PRODUCT-SELF-HOST-COMPLETE,
  FREESTANDING-PRODUCT-SELF-HOST-COMPLETE-MEASURED,
  FREESTANDING-PRODUCT-SELF-HOST-COMPLETE-WITH-FULL-OWNERSHIP-PERFORM-DUAL-EQ-WRITE,
  FREESTANDING-PRODUCT-SELF-HOST-COMPLETE-STEP-ADVANCED,
  COMPLETE-THEOREM, HOST-COMPLETE-THEOREM,
  COMPLETE-SMOKE, HOST-COMPLETE-SMOKE, SelfHostCompleteTheorems,
  theorem freestandingProductSelfHostCompletePartialReady_true,
  just build, freestanding-capable-regenerate,
  freestandingCapableOrderedRegenerate, freestandingCapableWriteFreestandingHc,
  CapableWriteHc, CapableRegenerate, DUAL-SSOT-EQUALITY, requireDualSsotEqual,
  freestanding-self-host-complete, freestanding-self-host-complete-measure,
  slake-freestanding-self-host-complete, SelfHostComplete,
  freestanding-step-contract-full, StepContractFull,
  freestanding-ownership-claimed, OwnershipClaimed,
  freestanding-perform-claimed, PerformClaimed,
  freestanding-retire-official, OfficialRetire,
  RUNTIME-FS, UNIT_SURFACE host surface.
  Not freestanding emit residual free. Not residual free. Not PROVABLY.
  Not llvm unlock. Module: SystemsLean.SelfHostComplete
  Short role name (not ProductPathFreestanding*). Batch-1 PartialReady bulk here;
  tip re-exports. Dual-pin thin batch 33: Ok/stage/recipe/exe home-primary.
  Long-file peel: COMPLETE-THEOREM + SMOKE in SelfHostCompleteTheorems.
  Module must stay ASCII. NO FreestandingEmit import.
-/

namespace SystemsLean.SelfHostComplete

/-- Greppable primary stage id (claim B complete). -/
def stageId : String :=
  "SLAKE_SELF_HOST_FREESTANDING_PRODUCT_COMPLETE_V0"

/-- Greppable host map id. -/
def hostId : String :=
  "HOST-SELF-HOST-FREESTANDING-PRODUCT-COMPLETE"

/-- Greppable short map id. -/
def selfHostId : String :=
  "SELF-HOST-FREESTANDING-PRODUCT-COMPLETE"

/-- Complete measured (Full + ownership + perform + dual-eq WRITE; tip SSoT true).
    Greppable: freestandingProductSelfHostCompleteMeasured,
    FREESTANDING-PRODUCT-SELF-HOST-COMPLETE. -/
def freestandingProductSelfHostCompleteMeasured : Bool := true

/-- Close-path step complete advanced (claim B closed).
    Greppable: freestandingProductSelfHostCompleteStepAdvanced,
    FREESTANDING-PRODUCT-SELF-HOST-COMPLETE-STEP-ADVANCED. -/
def freestandingProductSelfHostCompleteStepAdvanced : Bool := true

/-- Official product path uses freestanding dual-equality WRITE as authority. -/
def productPathOfficialPathUsesDualEqualityWrite : Bool := true

/-- Complete true with Full + ownership + perform + dual-eq WRITE evidence.
    Tip SSoT SelfApplyFs; dual-pin true for Ok / pure Nix.
    Greppable: freestandingProductSelfHostComplete. -/
def freestandingProductSelfHostComplete : Bool := true

/-- Full remains true (prerequisite evidence). -/
def stepContractFull : Bool := true

/-- B40 OWNERSHIP-CLAIMED remains true (prerequisite evidence). -/
def productPathFreestandingOwnershipClaimed : Bool := true

/-- B39 PERFORM-CLAIMED remains true (prerequisite evidence). -/
def productPathFreestandingPerformClaimed : Bool := true

/-- Official product path no longer DependsOnLake as living driver (M6).
    Host elaborator residual may remain (DualResidual). Greppable:
    productPathPerformDependsOnLake. -/
def productPathPerformDependsOnLake : Bool := false

/-- B38 RETIRE-OFFICIAL remains measured (prerequisite substrate). -/
def productPathOfficialPathRetireOfficialMeasured : Bool := true

/-- Full remains measured (prerequisite substrate). -/
def stepContractFullMeasured : Bool := true

/-- B40 OWNERSHIP-CLAIMED remains measured (prerequisite substrate). -/
def productPathFreestandingOwnershipClaimedMeasured : Bool := true

/-- B39 PERFORM-CLAIMED remains measured (prerequisite substrate). -/
def productPathFreestandingPerformClaimedMeasured : Bool := true

/-- B37 CAPABLE-GAP remains measured (prerequisite substrate). -/
def productPathFreestandingPerformDualEqualityWriteCapableGapMeasured : Bool := true

/-- Dual-equality WRITE parity gap stays closed after B37. -/
def productPathDualEqualityWriteParityGapOpen : Bool := false

/-- Capable WRITE dual-equality live (B37). -/
def productPathCapableWriteDualEqualityLive : Bool := true

/-- Official path no longer uses FreestandingEmit as product writer (B38). -/
def productPathOfficialPathStillUsesFreestandingEmit : Bool := false

/-- Official product path no longer uses Lake as living driver (M6).
    Host elaborator residual may remain for SystemsLean development. -/
def productPathOfficialPathStillUsesLake : Bool := false

/-- FreestandingEmit retired as official product authority (B38). -/
def productPathOfficialPathRetireFreestandingEmitRequired : Bool := false

/-- Dual-equality no longer blocks retirement. -/
def productPathOfficialPathDualEqualityBlocksRetirement : Bool := false

/-- residual free true (claim A; DualResidual product free SSoT aligned). -/
def residualFreeClaimed : Bool := true

/-- llvm unlock stays false (LlvmHold non-claim). -/
def llvmUnlocked : Bool := false

/-- PROVABLY unlock true after evidence residual (LlvmHold dual-pin). -/
def provablyUnlocked : Bool := true

/-- Named complete tokens. -/
def completeId : String :=
  "FREESTANDING-PRODUCT-SELF-HOST-COMPLETE"
def completeMeasuredId : String :=
  "FREESTANDING-PRODUCT-SELF-HOST-COMPLETE-MEASURED"
def completeWithFullOwnershipPerformDualEqWriteId : String :=
  "FREESTANDING-PRODUCT-SELF-HOST-COMPLETE-WITH-FULL-OWNERSHIP-PERFORM-DUAL-EQ-WRITE"
def completeStepAdvancedId : String :=
  "FREESTANDING-PRODUCT-SELF-HOST-COMPLETE-STEP-ADVANCED"
def selfHostCompleteToken : String :=
  "SELF-HOST-FREESTANDING-PRODUCT-COMPLETE"

/-- Official product path dual-equality writer cites. -/
def officialPathRecipe : String := "build"
def officialPathWriter : String := "freestandingCapableOrderedRegenerate"
def officialPathLakeExe : String := "slake-freestanding-capable-regenerate"
def officialPathJustRecipe : String := "freestanding-capable-regenerate"
def dualEqualityGateId : String := "DUAL-SSOT-EQUALITY"
def dualEqualityApiName : String := "requireDualSsotEqual"
def capableWriteApi : String := "freestandingCapableWriteFreestandingHc"
def capableWriteModule : String := "CapableWriteHc"

/-- Lake-free complete recipe. -/
def lakeFreeCompleteRecipe : String := "freestanding-self-host-complete"

/-- Diagnostic Lake measure recipe. -/
def justMeasureRecipe : String := "freestanding-self-host-complete-measure"
def lakeExeName : String := "slake-freestanding-self-host-complete"

/-- Full recipe (prerequisite). -/
def fullRecipe : String := "freestanding-step-contract-full"

/-- B40 OWNERSHIP-CLAIMED recipe (prerequisite). -/
def b40OwnershipClaimedRecipe : String := "freestanding-ownership-claimed"

/-- B39 PERFORM-CLAIMED recipe (prerequisite). -/
def b39PerformClaimedRecipe : String := "freestanding-perform-claimed"

/-- B38 RETIRE-OFFICIAL recipe (prerequisite). -/
def b38RetireOfficialRecipe : String := "freestanding-retire-official"

/-- Complete surface ok: measured/step advanced/complete true; Full + ownership +
    perform + dual-eq WRITE; StillUsesFreestandingEmit/Blocks/RetireRequired false;
    StillUsesLake/DependsOnLake false (M6 product path); free may be true (claim A);
    llvm false; PROVABLY true after evidence residual.
    Greppable: freestandingProductSelfHostCompleteOk. -/
def freestandingProductSelfHostCompleteOk : Bool :=
  (stageId
      == "SLAKE_SELF_HOST_FREESTANDING_PRODUCT_COMPLETE_V0")
    && (hostId
        == "HOST-SELF-HOST-FREESTANDING-PRODUCT-COMPLETE")
    && (selfHostId
        == "SELF-HOST-FREESTANDING-PRODUCT-COMPLETE")
    && (freestandingProductSelfHostCompleteMeasured == true)
    && (freestandingProductSelfHostCompleteStepAdvanced == true)
    && (freestandingProductSelfHostComplete == true)
    && (stepContractFull == true)
    && (stepContractFullMeasured == true)
    && (productPathFreestandingOwnershipClaimed == true)
    && (productPathFreestandingOwnershipClaimedMeasured == true)
    && (productPathFreestandingPerformClaimed == true)
    && (productPathFreestandingPerformClaimedMeasured == true)
    && (productPathOfficialPathUsesDualEqualityWrite == true)
    && (productPathOfficialPathStillUsesFreestandingEmit == false)
    && (productPathOfficialPathStillUsesLake == false)
    && (productPathOfficialPathRetireFreestandingEmitRequired == false)
    && (productPathOfficialPathDualEqualityBlocksRetirement == false)
    && (productPathOfficialPathRetireOfficialMeasured == true)
    && (productPathPerformDependsOnLake == false)
    && (productPathFreestandingPerformDualEqualityWriteCapableGapMeasured == true)
    && (productPathDualEqualityWriteParityGapOpen == false)
    && (productPathCapableWriteDualEqualityLive == true)
    && (residualFreeClaimed == true)
    && (llvmUnlocked == false)
    && (provablyUnlocked == true)
    && (completeId == "FREESTANDING-PRODUCT-SELF-HOST-COMPLETE")
    && (completeMeasuredId
        == "FREESTANDING-PRODUCT-SELF-HOST-COMPLETE-MEASURED")
    && (completeWithFullOwnershipPerformDualEqWriteId
        == "FREESTANDING-PRODUCT-SELF-HOST-COMPLETE-WITH-FULL-OWNERSHIP-PERFORM-DUAL-EQ-WRITE")
    && (completeStepAdvancedId
        == "FREESTANDING-PRODUCT-SELF-HOST-COMPLETE-STEP-ADVANCED")
    && (selfHostCompleteToken
        == "SELF-HOST-FREESTANDING-PRODUCT-COMPLETE")
    && (officialPathRecipe == "build")
    && (officialPathWriter == "freestandingCapableOrderedRegenerate")
    && (officialPathLakeExe == "slake-freestanding-capable-regenerate")
    && (officialPathJustRecipe == "freestanding-capable-regenerate")
    && (dualEqualityGateId == "DUAL-SSOT-EQUALITY")
    && (dualEqualityApiName == "requireDualSsotEqual")
    && (capableWriteApi == "freestandingCapableWriteFreestandingHc")
    && (capableWriteModule == "CapableWriteHc")
    && (lakeFreeCompleteRecipe == "freestanding-self-host-complete")
    && (justMeasureRecipe == "freestanding-self-host-complete-measure")
    && (lakeExeName == "slake-freestanding-self-host-complete")
    && (fullRecipe == "freestanding-step-contract-full")
    && (b40OwnershipClaimedRecipe == "freestanding-ownership-claimed")
    && (b39PerformClaimedRecipe == "freestanding-perform-claimed")
    && (b38RetireOfficialRecipe == "freestanding-retire-official")

/-- Print complete measure (diagnostic; free/llvm stay false). -/
def printSelfHostCompleteMeasure : IO Unit := do
  IO.println s!"== {stageId}: freestanding product self-host complete =="
  IO.println s!"  host: {hostId} / {selfHostId}"
  IO.println s!"  freestandingProductSelfHostCompleteMeasured: {freestandingProductSelfHostCompleteMeasured} ({completeId})"
  IO.println "  complete with Full + ownership-claimed + perform-claimed + official dual-eq WRITE evidence:"
  IO.println s!"    step advanced: {freestandingProductSelfHostCompleteStepAdvanced} ({completeStepAdvancedId})"
  IO.println s!"    freestandingProductSelfHostComplete: {freestandingProductSelfHostComplete} ({completeWithFullOwnershipPerformDualEqWriteId})"
  IO.println s!"    stepContractFull: {stepContractFull} (Full evidence)"
  IO.println s!"    ownership claimed: {productPathFreestandingOwnershipClaimed} (B40 evidence)"
  IO.println s!"    perform claimed: {productPathFreestandingPerformClaimed} (B39 evidence)"
  IO.println s!"    uses dual-eq WRITE: {productPathOfficialPathUsesDualEqualityWrite}"
  IO.println "  official product path freestanding dual-eq writer (FreestandingEmit retired B38):"
  IO.println s!"    recipe: {officialPathRecipe}"
  IO.println s!"    writer: {officialPathWriter}"
  IO.println s!"    Lake exe: {officialPathLakeExe}"
  IO.println s!"    just recipe: {officialPathJustRecipe}"
  IO.println s!"    still FreestandingEmit: {productPathOfficialPathStillUsesFreestandingEmit} (false = retired)"
  IO.println s!"    still Lake: {productPathOfficialPathStillUsesLake} (false = product path retired Lake)"
  IO.println s!"    retire FreestandingEmit required: {productPathOfficialPathRetireFreestandingEmitRequired} (false = retired)"
  IO.println s!"    dual-equality blocks retirement: {productPathOfficialPathDualEqualityBlocksRetirement} (false = not blocked)"
  IO.println s!"  stepContractFullMeasured: {stepContractFullMeasured} (Full remains)"
  IO.println s!"  productPathFreestandingOwnershipClaimedMeasured: {productPathFreestandingOwnershipClaimedMeasured} (B40 remains)"
  IO.println s!"  productPathFreestandingPerformClaimedMeasured: {productPathFreestandingPerformClaimedMeasured} (B39 remains)"
  IO.println s!"  productPathOfficialPathRetireOfficialMeasured: {productPathOfficialPathRetireOfficialMeasured} (B38 remains)"
  IO.println s!"  productPathFreestandingPerformDualEqualityWriteCapableGapMeasured: {productPathFreestandingPerformDualEqualityWriteCapableGapMeasured} (B37 remains)"
  IO.println s!"  productPathDualEqualityWriteParityGapOpen: {productPathDualEqualityWriteParityGapOpen} (false = CAPABLE-GAP closed)"
  IO.println s!"  productPathCapableWriteDualEqualityLive: {productPathCapableWriteDualEqualityLive}"
  IO.println s!"  productPathPerformDependsOnLake: {productPathPerformDependsOnLake}"
  IO.println s!"  residualFreeClaimed: {residualFreeClaimed} (claim A product free)"
  IO.println s!"  llvmUnlocked: {llvmUnlocked} (stays false)"
  IO.println s!"  provablyUnlocked: {provablyUnlocked} (true after PROVABLY residual)"
  IO.println s!"  Lake-free complete recipe: just {lakeFreeCompleteRecipe}"
  IO.println s!"  measure: just {justMeasureRecipe} / lake exe {lakeExeName}"
  IO.println s!"  Full recipe: just {fullRecipe}"
  IO.println s!"  B40 OWNERSHIP-CLAIMED recipe: just {b40OwnershipClaimedRecipe}"
  IO.println s!"  B39 PERFORM-CLAIMED recipe: just {b39PerformClaimedRecipe}"
  IO.println s!"  B38 RETIRE-OFFICIAL recipe: just {b38RetireOfficialRecipe}"
  IO.println "  honest: complete with Full bar is independent of residual free"
  IO.println "  honest: product path Lake retired (M6); host elaborator residual may remain"
  IO.println "  honest: FreestandingEmit is not official writer; free may be true"
  IO.println "  honest: CAPABLE-GAP stays closed; llvm false; PROVABLY true after matrix"
  IO.println "  short module name SelfHostComplete (not ProductPathFreestanding* kitchen-sink)"
  if freestandingProductSelfHostCompleteOk then
    IO.println s!"GREEN {stageId}: complete surface ok (measured true; complete true; stepContractFull true; ownership claimed true; perform claimed true; official dual-eq WRITE; StillUsesFreestandingEmit false; Blocks false; DependsOnLake false; StillUsesLake false; residual free true; llvm false; PROVABLY true)"
  else
    IO.eprintln s!"error: {stageId} complete surface not ok"
    throw (IO.userError "freestandingProductSelfHostCompleteOk false")

/-- Complete PartialReady fold (closed claim B). Tip re-exports chain fold.
    residual free may be true (claim A); llvm stays false; PROVABLY true after
    evidence residual. Product StillUsesLake / DependsOnLake false (M6).
    Greppable: freestandingProductSelfHostCompletePartialReady,
    SELF-HOST-FREESTANDING-PRODUCT-COMPLETE,
    FREESTANDING-PRODUCT-SELF-HOST-COMPLETE. -/
def freestandingProductSelfHostCompletePartialReady : Bool :=
  freestandingProductSelfHostCompleteOk
    && freestandingProductSelfHostCompleteMeasured
    && freestandingProductSelfHostCompleteStepAdvanced
    && freestandingProductSelfHostComplete
    && stepContractFull
    && productPathFreestandingOwnershipClaimed
    && productPathFreestandingPerformClaimed
    && productPathOfficialPathUsesDualEqualityWrite
    && !productPathOfficialPathStillUsesFreestandingEmit
    && !productPathOfficialPathStillUsesLake
    && !productPathOfficialPathRetireFreestandingEmitRequired
    && !productPathOfficialPathDualEqualityBlocksRetirement
    && productPathOfficialPathRetireOfficialMeasured
    && productPathFreestandingOwnershipClaimedMeasured
    && productPathFreestandingPerformClaimedMeasured
    && stepContractFullMeasured
    && !productPathPerformDependsOnLake
    && residualFreeClaimed
    && !llvmUnlocked
    && provablyUnlocked

/-- CLI: print complete measure. Fail-closed if Ok false. -/
def main (_args : List String) : IO UInt32 := do
  try
    printSelfHostCompleteMeasure
    pure 0
  catch e =>
    IO.eprintln s!"{e}"
    pure 1

end SystemsLean.SelfHostComplete

-- Theorems + smoke: SystemsLean.SelfHostCompleteTheorems (same namespace).
/-- Lake / lean --run entry: forward argv. -/
def main (args : List String) : IO UInt32 :=
  SystemsLean.SelfHostComplete.main args
