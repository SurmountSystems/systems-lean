/-
  SYSTEMS_LEAN_HOST partial -- dual-pin live ProductPathComposePlanMain.lean bytes.
  Side: classic Lean elaborator under src/systems/ (not freestanding C).
  Short role: HostFrontLiveProductPathComposePlanMainSource. Not occupancy name 50.
  Not mill 21 remill (just twenty-first-host-tool / inventory table row 3). Mill stays 69 of 69.
  This wrap parses ProductPathComposePlanMain.lean only. Do not wrap ProductPathComposePlan.lean.
  Short name is free because HostFrontLiveProductPathComposePlan (library wrap) does not exist.
  Do not mint HostFrontLiveHostProductPathComposePlanMain.
  DualEqWrite.lean does not exist and is not invented.
  CapableWrite.lean does not exist. HostResidualShrinkFacts.lean does not exist.
  Do not split DualPinOk.
  Unique needles use trailing newline so HostFrontLiveProductPathComposePlanMain is not a
  prefix hit on HostFrontLiveProductPathComposePlanMainSource.
  Occupancy leftover Term files are not this wrap.
  Unique needles (trailing newline so HostFrontLiveProductPathComposePlanMain is not a prefix):
  HostFrontLiveProductPathComposePlanMainSource
  PARSE-LIVE-PRODUCT-PATH-COMPOSE-PLAN-MAIN
  HOST-FRONT-LIVE-PRODUCT-PATH-COMPOSE-PLAN-MAIN
  PRODUCT-PATH-COMPOSE-PLAN-MAIN
  HOST-PRODUCT-PATH-COMPOSE-PLAN-MAIN
  Live product needles:
  ProductPathComposePlanMain
  slake-compose-product-plan
  import SystemsLean.ProductPathComposePlan
  composePlanApplyBodyAtRoot
  ProductPathComposePlan
  SKELETON
  Greppable: SYSTEMS_LEAN_HOST, liveProductPathComposePlanMainSource,
  UNIT_SURFACE host surface.
  Module: SystemsLean.HostFrontLiveProductPathComposePlanMainSource
  Not FullHost. Not freestanding residual free. Not PROVABLY.
  Module must stay ASCII.
-/

namespace SystemsLean.HostFrontLiveProductPathComposePlanMain

/-- Dual-pinned live ProductPathComposePlanMain.lean bytes (must match on-disk file).
    Greppable: liveProductPathComposePlanMainSource, PARSE-LIVE-PRODUCT-PATH-COMPOSE-PLAN-MAIN. -/
def liveProductPathComposePlanMainSource : String := r#"/-
  SYSTEMS_LEAN_HOST partial -- thin Lake exe main for B12 COMPOSE perform
  substrate (SKELETON). Root for lake exe slake-compose-product-plan.
  Body lives in SystemsLean.ProductPathComposePlan
  (composePlanApplyBodyAtRoot / Ok / PartialReady bulk). Thin main so SelfApplyFs
  may import the COMPOSE API without top-level main clash.
  Greppable: slake-compose-product-plan,
  ProductPathComposePlanMain, composePlanApplyBodyAtRoot,
  SYSTEMS_LEAN_HOST, SKELETON.
  Module must stay ASCII. Not freestanding residual free. Not PROVABLY.
-/

import SystemsLean.ProductPathComposePlan

/-- Lake / lean --run entry: forward argv. -/
def main (args : List String) : IO UInt32 :=
  SystemsLean.ProductPathComposePlan.main args
"#

end SystemsLean.HostFrontLiveProductPathComposePlanMain
