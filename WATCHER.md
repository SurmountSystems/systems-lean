# WATCHER.md -- next residual implement action only

Contract: the watcher session (separate from Systems / Slake implement) reads the
fenced block and may auto-continue. Keep this file short. Detail lives in
`RESIDUAL-systems.md` and `doc/SESSION-HANDOFF.md`.

<!-- WATCHER_BEGIN -->
DONE-FOR-NOW

Open empty. Linear subset emit + rebuild under Lake are done
(`LinearSubsetRebuild` / `just linear-subset-rebuild` GREEN).

Plan follow-on is not a single checkable default:
- Types subset emit (continue M1 unit surfaces under Lake), or
- Mult without-Lake driver design (ideal M2).

Open a residual Name in chat when ready. Do not auto-invent work.

Held: S4 / M6 Lake retire (never forge StillUsesLake false); Step 4
PROVABLY / LLVM. Living tip: complete true; free true; host residual remains.
<!-- WATCHER_END -->
